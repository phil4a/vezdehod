(function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);throw new Error("Cannot find module '"+o+"'")}var f=n[o]={exports:{}};t[o][0].call(f.exports,function(e){var n=t[o][1][e];return s(n?n:e)},f,f.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
(function (process,global,Buffer,__argument0,__argument1,__argument2,__argument3,__filename,__dirname){
var lookup = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';

;(function (exports) {
	'use strict';

  var Arr = (typeof Uint8Array !== 'undefined')
    ? Uint8Array
    : Array

	var PLUS   = '+'.charCodeAt(0)
	var SLASH  = '/'.charCodeAt(0)
	var NUMBER = '0'.charCodeAt(0)
	var LOWER  = 'a'.charCodeAt(0)
	var UPPER  = 'A'.charCodeAt(0)
	var PLUS_URL_SAFE = '-'.charCodeAt(0)
	var SLASH_URL_SAFE = '_'.charCodeAt(0)

	function decode (elt) {
		var code = elt.charCodeAt(0)
		if (code === PLUS ||
		    code === PLUS_URL_SAFE)
			return 62 // '+'
		if (code === SLASH ||
		    code === SLASH_URL_SAFE)
			return 63 // '/'
		if (code < NUMBER)
			return -1 //no match
		if (code < NUMBER + 10)
			return code - NUMBER + 26 + 26
		if (code < UPPER + 26)
			return code - UPPER
		if (code < LOWER + 26)
			return code - LOWER + 26
	}

	function b64ToByteArray (b64) {
		var i, j, l, tmp, placeHolders, arr

		if (b64.length % 4 > 0) {
			throw new Error('Invalid string. Length must be a multiple of 4')
		}

		// the number of equal signs (place holders)
		// if there are two placeholders, than the two characters before it
		// represent one byte
		// if there is only one, then the three characters before it represent 2 bytes
		// this is just a cheap hack to not do indexOf twice
		var len = b64.length
		placeHolders = '=' === b64.charAt(len - 2) ? 2 : '=' === b64.charAt(len - 1) ? 1 : 0

		// base64 is 4/3 + up to two characters of the original data
		arr = new Arr(b64.length * 3 / 4 - placeHolders)

		// if there are placeholders, only get up to the last complete 4 chars
		l = placeHolders > 0 ? b64.length - 4 : b64.length

		var L = 0

		function push (v) {
			arr[L++] = v
		}

		for (i = 0, j = 0; i < l; i += 4, j += 3) {
			tmp = (decode(b64.charAt(i)) << 18) | (decode(b64.charAt(i + 1)) << 12) | (decode(b64.charAt(i + 2)) << 6) | decode(b64.charAt(i + 3))
			push((tmp & 0xFF0000) >> 16)
			push((tmp & 0xFF00) >> 8)
			push(tmp & 0xFF)
		}

		if (placeHolders === 2) {
			tmp = (decode(b64.charAt(i)) << 2) | (decode(b64.charAt(i + 1)) >> 4)
			push(tmp & 0xFF)
		} else if (placeHolders === 1) {
			tmp = (decode(b64.charAt(i)) << 10) | (decode(b64.charAt(i + 1)) << 4) | (decode(b64.charAt(i + 2)) >> 2)
			push((tmp >> 8) & 0xFF)
			push(tmp & 0xFF)
		}

		return arr
	}

	function uint8ToBase64 (uint8) {
		var i,
			extraBytes = uint8.length % 3, // if we have 1 byte left, pad 2 bytes
			output = "",
			temp, length

		function encode (num) {
			return lookup.charAt(num)
		}

		function tripletToBase64 (num) {
			return encode(num >> 18 & 0x3F) + encode(num >> 12 & 0x3F) + encode(num >> 6 & 0x3F) + encode(num & 0x3F)
		}

		// go through the array every three bytes, we'll deal with trailing stuff later
		for (i = 0, length = uint8.length - extraBytes; i < length; i += 3) {
			temp = (uint8[i] << 16) + (uint8[i + 1] << 8) + (uint8[i + 2])
			output += tripletToBase64(temp)
		}

		// pad the end with zeros, but make sure to not forget the extra bytes
		switch (extraBytes) {
			case 1:
				temp = uint8[uint8.length - 1]
				output += encode(temp >> 2)
				output += encode((temp << 4) & 0x3F)
				output += '=='
				break
			case 2:
				temp = (uint8[uint8.length - 2] << 8) + (uint8[uint8.length - 1])
				output += encode(temp >> 10)
				output += encode((temp >> 4) & 0x3F)
				output += encode((temp << 2) & 0x3F)
				output += '='
				break
		}

		return output
	}

	exports.toByteArray = b64ToByteArray
	exports.fromByteArray = uint8ToBase64
}(typeof exports === 'undefined' ? (this.base64js = {}) : exports))

}).call(this,require("e/U+97"),typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : {},require("buffer").Buffer,arguments[3],arguments[4],arguments[5],arguments[6],"/..\\..\\node_modules\\browserify\\node_modules\\base64-js\\lib\\b64.js","/..\\..\\node_modules\\browserify\\node_modules\\base64-js\\lib")
},{"buffer":2,"e/U+97":4}],2:[function(require,module,exports){
(function (process,global,Buffer,__argument0,__argument1,__argument2,__argument3,__filename,__dirname){
/*!
 * The buffer module from node.js, for the browser.
 *
 * @author   Feross Aboukhadijeh <feross@feross.org> <http://feross.org>
 * @license  MIT
 */

var base64 = require('base64-js')
var ieee754 = require('ieee754')

exports.Buffer = Buffer
exports.SlowBuffer = Buffer
exports.INSPECT_MAX_BYTES = 50
Buffer.poolSize = 8192

/**
 * If `Buffer._useTypedArrays`:
 *   === true    Use Uint8Array implementation (fastest)
 *   === false   Use Object implementation (compatible down to IE6)
 */
Buffer._useTypedArrays = (function () {
  // Detect if browser supports Typed Arrays. Supported browsers are IE 10+, Firefox 4+,
  // Chrome 7+, Safari 5.1+, Opera 11.6+, iOS 4.2+. If the browser does not support adding
  // properties to `Uint8Array` instances, then that's the same as no `Uint8Array` support
  // because we need to be able to add all the node Buffer API methods. This is an issue
  // in Firefox 4-29. Now fixed: https://bugzilla.mozilla.org/show_bug.cgi?id=695438
  try {
    var buf = new ArrayBuffer(0)
    var arr = new Uint8Array(buf)
    arr.foo = function () { return 42 }
    return 42 === arr.foo() &&
        typeof arr.subarray === 'function' // Chrome 9-10 lack `subarray`
  } catch (e) {
    return false
  }
})()

/**
 * Class: Buffer
 * =============
 *
 * The Buffer constructor returns instances of `Uint8Array` that are augmented
 * with function properties for all the node `Buffer` API functions. We use
 * `Uint8Array` so that square bracket notation works as expected -- it returns
 * a single octet.
 *
 * By augmenting the instances, we can avoid modifying the `Uint8Array`
 * prototype.
 */
function Buffer (subject, encoding, noZero) {
  if (!(this instanceof Buffer))
    return new Buffer(subject, encoding, noZero)

  var type = typeof subject

  // Workaround: node's base64 implementation allows for non-padded strings
  // while base64-js does not.
  if (encoding === 'base64' && type === 'string') {
    subject = stringtrim(subject)
    while (subject.length % 4 !== 0) {
      subject = subject + '='
    }
  }

  // Find the length
  var length
  if (type === 'number')
    length = coerce(subject)
  else if (type === 'string')
    length = Buffer.byteLength(subject, encoding)
  else if (type === 'object')
    length = coerce(subject.length) // assume that object is array-like
  else
    throw new Error('First argument needs to be a number, array or string.')

  var buf
  if (Buffer._useTypedArrays) {
    // Preferred: Return an augmented `Uint8Array` instance for best performance
    buf = Buffer._augment(new Uint8Array(length))
  } else {
    // Fallback: Return THIS instance of Buffer (created by `new`)
    buf = this
    buf.length = length
    buf._isBuffer = true
  }

  var i
  if (Buffer._useTypedArrays && typeof subject.byteLength === 'number') {
    // Speed optimization -- use set if we're copying from a typed array
    buf._set(subject)
  } else if (isArrayish(subject)) {
    // Treat array-ish objects as a byte array
    for (i = 0; i < length; i++) {
      if (Buffer.isBuffer(subject))
        buf[i] = subject.readUInt8(i)
      else
        buf[i] = subject[i]
    }
  } else if (type === 'string') {
    buf.write(subject, 0, encoding)
  } else if (type === 'number' && !Buffer._useTypedArrays && !noZero) {
    for (i = 0; i < length; i++) {
      buf[i] = 0
    }
  }

  return buf
}

// STATIC METHODS
// ==============

Buffer.isEncoding = function (encoding) {
  switch (String(encoding).toLowerCase()) {
    case 'hex':
    case 'utf8':
    case 'utf-8':
    case 'ascii':
    case 'binary':
    case 'base64':
    case 'raw':
    case 'ucs2':
    case 'ucs-2':
    case 'utf16le':
    case 'utf-16le':
      return true
    default:
      return false
  }
}

Buffer.isBuffer = function (b) {
  return !!(b !== null && b !== undefined && b._isBuffer)
}

Buffer.byteLength = function (str, encoding) {
  var ret
  str = str + ''
  switch (encoding || 'utf8') {
    case 'hex':
      ret = str.length / 2
      break
    case 'utf8':
    case 'utf-8':
      ret = utf8ToBytes(str).length
      break
    case 'ascii':
    case 'binary':
    case 'raw':
      ret = str.length
      break
    case 'base64':
      ret = base64ToBytes(str).length
      break
    case 'ucs2':
    case 'ucs-2':
    case 'utf16le':
    case 'utf-16le':
      ret = str.length * 2
      break
    default:
      throw new Error('Unknown encoding')
  }
  return ret
}

Buffer.concat = function (list, totalLength) {
  assert(isArray(list), 'Usage: Buffer.concat(list, [totalLength])\n' +
      'list should be an Array.')

  if (list.length === 0) {
    return new Buffer(0)
  } else if (list.length === 1) {
    return list[0]
  }

  var i
  if (typeof totalLength !== 'number') {
    totalLength = 0
    for (i = 0; i < list.length; i++) {
      totalLength += list[i].length
    }
  }

  var buf = new Buffer(totalLength)
  var pos = 0
  for (i = 0; i < list.length; i++) {
    var item = list[i]
    item.copy(buf, pos)
    pos += item.length
  }
  return buf
}

// BUFFER INSTANCE METHODS
// =======================

function _hexWrite (buf, string, offset, length) {
  offset = Number(offset) || 0
  var remaining = buf.length - offset
  if (!length) {
    length = remaining
  } else {
    length = Number(length)
    if (length > remaining) {
      length = remaining
    }
  }

  // must be an even number of digits
  var strLen = string.length
  assert(strLen % 2 === 0, 'Invalid hex string')

  if (length > strLen / 2) {
    length = strLen / 2
  }
  for (var i = 0; i < length; i++) {
    var byte = parseInt(string.substr(i * 2, 2), 16)
    assert(!isNaN(byte), 'Invalid hex string')
    buf[offset + i] = byte
  }
  Buffer._charsWritten = i * 2
  return i
}

function _utf8Write (buf, string, offset, length) {
  var charsWritten = Buffer._charsWritten =
    blitBuffer(utf8ToBytes(string), buf, offset, length)
  return charsWritten
}

function _asciiWrite (buf, string, offset, length) {
  var charsWritten = Buffer._charsWritten =
    blitBuffer(asciiToBytes(string), buf, offset, length)
  return charsWritten
}

function _binaryWrite (buf, string, offset, length) {
  return _asciiWrite(buf, string, offset, length)
}

function _base64Write (buf, string, offset, length) {
  var charsWritten = Buffer._charsWritten =
    blitBuffer(base64ToBytes(string), buf, offset, length)
  return charsWritten
}

function _utf16leWrite (buf, string, offset, length) {
  var charsWritten = Buffer._charsWritten =
    blitBuffer(utf16leToBytes(string), buf, offset, length)
  return charsWritten
}

Buffer.prototype.write = function (string, offset, length, encoding) {
  // Support both (string, offset, length, encoding)
  // and the legacy (string, encoding, offset, length)
  if (isFinite(offset)) {
    if (!isFinite(length)) {
      encoding = length
      length = undefined
    }
  } else {  // legacy
    var swap = encoding
    encoding = offset
    offset = length
    length = swap
  }

  offset = Number(offset) || 0
  var remaining = this.length - offset
  if (!length) {
    length = remaining
  } else {
    length = Number(length)
    if (length > remaining) {
      length = remaining
    }
  }
  encoding = String(encoding || 'utf8').toLowerCase()

  var ret
  switch (encoding) {
    case 'hex':
      ret = _hexWrite(this, string, offset, length)
      break
    case 'utf8':
    case 'utf-8':
      ret = _utf8Write(this, string, offset, length)
      break
    case 'ascii':
      ret = _asciiWrite(this, string, offset, length)
      break
    case 'binary':
      ret = _binaryWrite(this, string, offset, length)
      break
    case 'base64':
      ret = _base64Write(this, string, offset, length)
      break
    case 'ucs2':
    case 'ucs-2':
    case 'utf16le':
    case 'utf-16le':
      ret = _utf16leWrite(this, string, offset, length)
      break
    default:
      throw new Error('Unknown encoding')
  }
  return ret
}

Buffer.prototype.toString = function (encoding, start, end) {
  var self = this

  encoding = String(encoding || 'utf8').toLowerCase()
  start = Number(start) || 0
  end = (end !== undefined)
    ? Number(end)
    : end = self.length

  // Fastpath empty strings
  if (end === start)
    return ''

  var ret
  switch (encoding) {
    case 'hex':
      ret = _hexSlice(self, start, end)
      break
    case 'utf8':
    case 'utf-8':
      ret = _utf8Slice(self, start, end)
      break
    case 'ascii':
      ret = _asciiSlice(self, start, end)
      break
    case 'binary':
      ret = _binarySlice(self, start, end)
      break
    case 'base64':
      ret = _base64Slice(self, start, end)
      break
    case 'ucs2':
    case 'ucs-2':
    case 'utf16le':
    case 'utf-16le':
      ret = _utf16leSlice(self, start, end)
      break
    default:
      throw new Error('Unknown encoding')
  }
  return ret
}

Buffer.prototype.toJSON = function () {
  return {
    type: 'Buffer',
    data: Array.prototype.slice.call(this._arr || this, 0)
  }
}

// copy(targetBuffer, targetStart=0, sourceStart=0, sourceEnd=buffer.length)
Buffer.prototype.copy = function (target, target_start, start, end) {
  var source = this

  if (!start) start = 0
  if (!end && end !== 0) end = this.length
  if (!target_start) target_start = 0

  // Copy 0 bytes; we're done
  if (end === start) return
  if (target.length === 0 || source.length === 0) return

  // Fatal error conditions
  assert(end >= start, 'sourceEnd < sourceStart')
  assert(target_start >= 0 && target_start < target.length,
      'targetStart out of bounds')
  assert(start >= 0 && start < source.length, 'sourceStart out of bounds')
  assert(end >= 0 && end <= source.length, 'sourceEnd out of bounds')

  // Are we oob?
  if (end > this.length)
    end = this.length
  if (target.length - target_start < end - start)
    end = target.length - target_start + start

  var len = end - start

  if (len < 100 || !Buffer._useTypedArrays) {
    for (var i = 0; i < len; i++)
      target[i + target_start] = this[i + start]
  } else {
    target._set(this.subarray(start, start + len), target_start)
  }
}

function _base64Slice (buf, start, end) {
  if (start === 0 && end === buf.length) {
    return base64.fromByteArray(buf)
  } else {
    return base64.fromByteArray(buf.slice(start, end))
  }
}

function _utf8Slice (buf, start, end) {
  var res = ''
  var tmp = ''
  end = Math.min(buf.length, end)

  for (var i = start; i < end; i++) {
    if (buf[i] <= 0x7F) {
      res += decodeUtf8Char(tmp) + String.fromCharCode(buf[i])
      tmp = ''
    } else {
      tmp += '%' + buf[i].toString(16)
    }
  }

  return res + decodeUtf8Char(tmp)
}

function _asciiSlice (buf, start, end) {
  var ret = ''
  end = Math.min(buf.length, end)

  for (var i = start; i < end; i++)
    ret += String.fromCharCode(buf[i])
  return ret
}

function _binarySlice (buf, start, end) {
  return _asciiSlice(buf, start, end)
}

function _hexSlice (buf, start, end) {
  var len = buf.length

  if (!start || start < 0) start = 0
  if (!end || end < 0 || end > len) end = len

  var out = ''
  for (var i = start; i < end; i++) {
    out += toHex(buf[i])
  }
  return out
}

function _utf16leSlice (buf, start, end) {
  var bytes = buf.slice(start, end)
  var res = ''
  for (var i = 0; i < bytes.length; i += 2) {
    res += String.fromCharCode(bytes[i] + bytes[i+1] * 256)
  }
  return res
}

Buffer.prototype.slice = function (start, end) {
  var len = this.length
  start = clamp(start, len, 0)
  end = clamp(end, len, len)

  if (Buffer._useTypedArrays) {
    return Buffer._augment(this.subarray(start, end))
  } else {
    var sliceLen = end - start
    var newBuf = new Buffer(sliceLen, undefined, true)
    for (var i = 0; i < sliceLen; i++) {
      newBuf[i] = this[i + start]
    }
    return newBuf
  }
}

// `get` will be removed in Node 0.13+
Buffer.prototype.get = function (offset) {
  console.log('.get() is deprecated. Access using array indexes instead.')
  return this.readUInt8(offset)
}

// `set` will be removed in Node 0.13+
Buffer.prototype.set = function (v, offset) {
  console.log('.set() is deprecated. Access using array indexes instead.')
  return this.writeUInt8(v, offset)
}

Buffer.prototype.readUInt8 = function (offset, noAssert) {
  if (!noAssert) {
    assert(offset !== undefined && offset !== null, 'missing offset')
    assert(offset < this.length, 'Trying to read beyond buffer length')
  }

  if (offset >= this.length)
    return

  return this[offset]
}

function _readUInt16 (buf, offset, littleEndian, noAssert) {
  if (!noAssert) {
    assert(typeof littleEndian === 'boolean', 'missing or invalid endian')
    assert(offset !== undefined && offset !== null, 'missing offset')
    assert(offset + 1 < buf.length, 'Trying to read beyond buffer length')
  }

  var len = buf.length
  if (offset >= len)
    return

  var val
  if (littleEndian) {
    val = buf[offset]
    if (offset + 1 < len)
      val |= buf[offset + 1] << 8
  } else {
    val = buf[offset] << 8
    if (offset + 1 < len)
      val |= buf[offset + 1]
  }
  return val
}

Buffer.prototype.readUInt16LE = function (offset, noAssert) {
  return _readUInt16(this, offset, true, noAssert)
}

Buffer.prototype.readUInt16BE = function (offset, noAssert) {
  return _readUInt16(this, offset, false, noAssert)
}

function _readUInt32 (buf, offset, littleEndian, noAssert) {
  if (!noAssert) {
    assert(typeof littleEndian === 'boolean', 'missing or invalid endian')
    assert(offset !== undefined && offset !== null, 'missing offset')
    assert(offset + 3 < buf.length, 'Trying to read beyond buffer length')
  }

  var len = buf.length
  if (offset >= len)
    return

  var val
  if (littleEndian) {
    if (offset + 2 < len)
      val = buf[offset + 2] << 16
    if (offset + 1 < len)
      val |= buf[offset + 1] << 8
    val |= buf[offset]
    if (offset + 3 < len)
      val = val + (buf[offset + 3] << 24 >>> 0)
  } else {
    if (offset + 1 < len)
      val = buf[offset + 1] << 16
    if (offset + 2 < len)
      val |= buf[offset + 2] << 8
    if (offset + 3 < len)
      val |= buf[offset + 3]
    val = val + (buf[offset] << 24 >>> 0)
  }
  return val
}

Buffer.prototype.readUInt32LE = function (offset, noAssert) {
  return _readUInt32(this, offset, true, noAssert)
}

Buffer.prototype.readUInt32BE = function (offset, noAssert) {
  return _readUInt32(this, offset, false, noAssert)
}

Buffer.prototype.readInt8 = function (offset, noAssert) {
  if (!noAssert) {
    assert(offset !== undefined && offset !== null,
        'missing offset')
    assert(offset < this.length, 'Trying to read beyond buffer length')
  }

  if (offset >= this.length)
    return

  var neg = this[offset] & 0x80
  if (neg)
    return (0xff - this[offset] + 1) * -1
  else
    return this[offset]
}

function _readInt16 (buf, offset, littleEndian, noAssert) {
  if (!noAssert) {
    assert(typeof littleEndian === 'boolean', 'missing or invalid endian')
    assert(offset !== undefined && offset !== null, 'missing offset')
    assert(offset + 1 < buf.length, 'Trying to read beyond buffer length')
  }

  var len = buf.length
  if (offset >= len)
    return

  var val = _readUInt16(buf, offset, littleEndian, true)
  var neg = val & 0x8000
  if (neg)
    return (0xffff - val + 1) * -1
  else
    return val
}

Buffer.prototype.readInt16LE = function (offset, noAssert) {
  return _readInt16(this, offset, true, noAssert)
}

Buffer.prototype.readInt16BE = function (offset, noAssert) {
  return _readInt16(this, offset, false, noAssert)
}

function _readInt32 (buf, offset, littleEndian, noAssert) {
  if (!noAssert) {
    assert(typeof littleEndian === 'boolean', 'missing or invalid endian')
    assert(offset !== undefined && offset !== null, 'missing offset')
    assert(offset + 3 < buf.length, 'Trying to read beyond buffer length')
  }

  var len = buf.length
  if (offset >= len)
    return

  var val = _readUInt32(buf, offset, littleEndian, true)
  var neg = val & 0x80000000
  if (neg)
    return (0xffffffff - val + 1) * -1
  else
    return val
}

Buffer.prototype.readInt32LE = function (offset, noAssert) {
  return _readInt32(this, offset, true, noAssert)
}

Buffer.prototype.readInt32BE = function (offset, noAssert) {
  return _readInt32(this, offset, false, noAssert)
}

function _readFloat (buf, offset, littleEndian, noAssert) {
  if (!noAssert) {
    assert(typeof littleEndian === 'boolean', 'missing or invalid endian')
    assert(offset + 3 < buf.length, 'Trying to read beyond buffer length')
  }

  return ieee754.read(buf, offset, littleEndian, 23, 4)
}

Buffer.prototype.readFloatLE = function (offset, noAssert) {
  return _readFloat(this, offset, true, noAssert)
}

Buffer.prototype.readFloatBE = function (offset, noAssert) {
  return _readFloat(this, offset, false, noAssert)
}

function _readDouble (buf, offset, littleEndian, noAssert) {
  if (!noAssert) {
    assert(typeof littleEndian === 'boolean', 'missing or invalid endian')
    assert(offset + 7 < buf.length, 'Trying to read beyond buffer length')
  }

  return ieee754.read(buf, offset, littleEndian, 52, 8)
}

Buffer.prototype.readDoubleLE = function (offset, noAssert) {
  return _readDouble(this, offset, true, noAssert)
}

Buffer.prototype.readDoubleBE = function (offset, noAssert) {
  return _readDouble(this, offset, false, noAssert)
}

Buffer.prototype.writeUInt8 = function (value, offset, noAssert) {
  if (!noAssert) {
    assert(value !== undefined && value !== null, 'missing value')
    assert(offset !== undefined && offset !== null, 'missing offset')
    assert(offset < this.length, 'trying to write beyond buffer length')
    verifuint(value, 0xff)
  }

  if (offset >= this.length) return

  this[offset] = value
}

function _writeUInt16 (buf, value, offset, littleEndian, noAssert) {
  if (!noAssert) {
    assert(value !== undefined && value !== null, 'missing value')
    assert(typeof littleEndian === 'boolean', 'missing or invalid endian')
    assert(offset !== undefined && offset !== null, 'missing offset')
    assert(offset + 1 < buf.length, 'trying to write beyond buffer length')
    verifuint(value, 0xffff)
  }

  var len = buf.length
  if (offset >= len)
    return

  for (var i = 0, j = Math.min(len - offset, 2); i < j; i++) {
    buf[offset + i] =
        (value & (0xff << (8 * (littleEndian ? i : 1 - i)))) >>>
            (littleEndian ? i : 1 - i) * 8
  }
}

Buffer.prototype.writeUInt16LE = function (value, offset, noAssert) {
  _writeUInt16(this, value, offset, true, noAssert)
}

Buffer.prototype.writeUInt16BE = function (value, offset, noAssert) {
  _writeUInt16(this, value, offset, false, noAssert)
}

function _writeUInt32 (buf, value, offset, littleEndian, noAssert) {
  if (!noAssert) {
    assert(value !== undefined && value !== null, 'missing value')
    assert(typeof littleEndian === 'boolean', 'missing or invalid endian')
    assert(offset !== undefined && offset !== null, 'missing offset')
    assert(offset + 3 < buf.length, 'trying to write beyond buffer length')
    verifuint(value, 0xffffffff)
  }

  var len = buf.length
  if (offset >= len)
    return

  for (var i = 0, j = Math.min(len - offset, 4); i < j; i++) {
    buf[offset + i] =
        (value >>> (littleEndian ? i : 3 - i) * 8) & 0xff
  }
}

Buffer.prototype.writeUInt32LE = function (value, offset, noAssert) {
  _writeUInt32(this, value, offset, true, noAssert)
}

Buffer.prototype.writeUInt32BE = function (value, offset, noAssert) {
  _writeUInt32(this, value, offset, false, noAssert)
}

Buffer.prototype.writeInt8 = function (value, offset, noAssert) {
  if (!noAssert) {
    assert(value !== undefined && value !== null, 'missing value')
    assert(offset !== undefined && offset !== null, 'missing offset')
    assert(offset < this.length, 'Trying to write beyond buffer length')
    verifsint(value, 0x7f, -0x80)
  }

  if (offset >= this.length)
    return

  if (value >= 0)
    this.writeUInt8(value, offset, noAssert)
  else
    this.writeUInt8(0xff + value + 1, offset, noAssert)
}

function _writeInt16 (buf, value, offset, littleEndian, noAssert) {
  if (!noAssert) {
    assert(value !== undefined && value !== null, 'missing value')
    assert(typeof littleEndian === 'boolean', 'missing or invalid endian')
    assert(offset !== undefined && offset !== null, 'missing offset')
    assert(offset + 1 < buf.length, 'Trying to write beyond buffer length')
    verifsint(value, 0x7fff, -0x8000)
  }

  var len = buf.length
  if (offset >= len)
    return

  if (value >= 0)
    _writeUInt16(buf, value, offset, littleEndian, noAssert)
  else
    _writeUInt16(buf, 0xffff + value + 1, offset, littleEndian, noAssert)
}

Buffer.prototype.writeInt16LE = function (value, offset, noAssert) {
  _writeInt16(this, value, offset, true, noAssert)
}

Buffer.prototype.writeInt16BE = function (value, offset, noAssert) {
  _writeInt16(this, value, offset, false, noAssert)
}

function _writeInt32 (buf, value, offset, littleEndian, noAssert) {
  if (!noAssert) {
    assert(value !== undefined && value !== null, 'missing value')
    assert(typeof littleEndian === 'boolean', 'missing or invalid endian')
    assert(offset !== undefined && offset !== null, 'missing offset')
    assert(offset + 3 < buf.length, 'Trying to write beyond buffer length')
    verifsint(value, 0x7fffffff, -0x80000000)
  }

  var len = buf.length
  if (offset >= len)
    return

  if (value >= 0)
    _writeUInt32(buf, value, offset, littleEndian, noAssert)
  else
    _writeUInt32(buf, 0xffffffff + value + 1, offset, littleEndian, noAssert)
}

Buffer.prototype.writeInt32LE = function (value, offset, noAssert) {
  _writeInt32(this, value, offset, true, noAssert)
}

Buffer.prototype.writeInt32BE = function (value, offset, noAssert) {
  _writeInt32(this, value, offset, false, noAssert)
}

function _writeFloat (buf, value, offset, littleEndian, noAssert) {
  if (!noAssert) {
    assert(value !== undefined && value !== null, 'missing value')
    assert(typeof littleEndian === 'boolean', 'missing or invalid endian')
    assert(offset !== undefined && offset !== null, 'missing offset')
    assert(offset + 3 < buf.length, 'Trying to write beyond buffer length')
    verifIEEE754(value, 3.4028234663852886e+38, -3.4028234663852886e+38)
  }

  var len = buf.length
  if (offset >= len)
    return

  ieee754.write(buf, value, offset, littleEndian, 23, 4)
}

Buffer.prototype.writeFloatLE = function (value, offset, noAssert) {
  _writeFloat(this, value, offset, true, noAssert)
}

Buffer.prototype.writeFloatBE = function (value, offset, noAssert) {
  _writeFloat(this, value, offset, false, noAssert)
}

function _writeDouble (buf, value, offset, littleEndian, noAssert) {
  if (!noAssert) {
    assert(value !== undefined && value !== null, 'missing value')
    assert(typeof littleEndian === 'boolean', 'missing or invalid endian')
    assert(offset !== undefined && offset !== null, 'missing offset')
    assert(offset + 7 < buf.length,
        'Trying to write beyond buffer length')
    verifIEEE754(value, 1.7976931348623157E+308, -1.7976931348623157E+308)
  }

  var len = buf.length
  if (offset >= len)
    return

  ieee754.write(buf, value, offset, littleEndian, 52, 8)
}

Buffer.prototype.writeDoubleLE = function (value, offset, noAssert) {
  _writeDouble(this, value, offset, true, noAssert)
}

Buffer.prototype.writeDoubleBE = function (value, offset, noAssert) {
  _writeDouble(this, value, offset, false, noAssert)
}

// fill(value, start=0, end=buffer.length)
Buffer.prototype.fill = function (value, start, end) {
  if (!value) value = 0
  if (!start) start = 0
  if (!end) end = this.length

  if (typeof value === 'string') {
    value = value.charCodeAt(0)
  }

  assert(typeof value === 'number' && !isNaN(value), 'value is not a number')
  assert(end >= start, 'end < start')

  // Fill 0 bytes; we're done
  if (end === start) return
  if (this.length === 0) return

  assert(start >= 0 && start < this.length, 'start out of bounds')
  assert(end >= 0 && end <= this.length, 'end out of bounds')

  for (var i = start; i < end; i++) {
    this[i] = value
  }
}

Buffer.prototype.inspect = function () {
  var out = []
  var len = this.length
  for (var i = 0; i < len; i++) {
    out[i] = toHex(this[i])
    if (i === exports.INSPECT_MAX_BYTES) {
      out[i + 1] = '...'
      break
    }
  }
  return '<Buffer ' + out.join(' ') + '>'
}

/**
 * Creates a new `ArrayBuffer` with the *copied* memory of the buffer instance.
 * Added in Node 0.12. Only available in browsers that support ArrayBuffer.
 */
Buffer.prototype.toArrayBuffer = function () {
  if (typeof Uint8Array !== 'undefined') {
    if (Buffer._useTypedArrays) {
      return (new Buffer(this)).buffer
    } else {
      var buf = new Uint8Array(this.length)
      for (var i = 0, len = buf.length; i < len; i += 1)
        buf[i] = this[i]
      return buf.buffer
    }
  } else {
    throw new Error('Buffer.toArrayBuffer not supported in this browser')
  }
}

// HELPER FUNCTIONS
// ================

function stringtrim (str) {
  if (str.trim) return str.trim()
  return str.replace(/^\s+|\s+$/g, '')
}

var BP = Buffer.prototype

/**
 * Augment a Uint8Array *instance* (not the Uint8Array class!) with Buffer methods
 */
Buffer._augment = function (arr) {
  arr._isBuffer = true

  // save reference to original Uint8Array get/set methods before overwriting
  arr._get = arr.get
  arr._set = arr.set

  // deprecated, will be removed in node 0.13+
  arr.get = BP.get
  arr.set = BP.set

  arr.write = BP.write
  arr.toString = BP.toString
  arr.toLocaleString = BP.toString
  arr.toJSON = BP.toJSON
  arr.copy = BP.copy
  arr.slice = BP.slice
  arr.readUInt8 = BP.readUInt8
  arr.readUInt16LE = BP.readUInt16LE
  arr.readUInt16BE = BP.readUInt16BE
  arr.readUInt32LE = BP.readUInt32LE
  arr.readUInt32BE = BP.readUInt32BE
  arr.readInt8 = BP.readInt8
  arr.readInt16LE = BP.readInt16LE
  arr.readInt16BE = BP.readInt16BE
  arr.readInt32LE = BP.readInt32LE
  arr.readInt32BE = BP.readInt32BE
  arr.readFloatLE = BP.readFloatLE
  arr.readFloatBE = BP.readFloatBE
  arr.readDoubleLE = BP.readDoubleLE
  arr.readDoubleBE = BP.readDoubleBE
  arr.writeUInt8 = BP.writeUInt8
  arr.writeUInt16LE = BP.writeUInt16LE
  arr.writeUInt16BE = BP.writeUInt16BE
  arr.writeUInt32LE = BP.writeUInt32LE
  arr.writeUInt32BE = BP.writeUInt32BE
  arr.writeInt8 = BP.writeInt8
  arr.writeInt16LE = BP.writeInt16LE
  arr.writeInt16BE = BP.writeInt16BE
  arr.writeInt32LE = BP.writeInt32LE
  arr.writeInt32BE = BP.writeInt32BE
  arr.writeFloatLE = BP.writeFloatLE
  arr.writeFloatBE = BP.writeFloatBE
  arr.writeDoubleLE = BP.writeDoubleLE
  arr.writeDoubleBE = BP.writeDoubleBE
  arr.fill = BP.fill
  arr.inspect = BP.inspect
  arr.toArrayBuffer = BP.toArrayBuffer

  return arr
}

// slice(start, end)
function clamp (index, len, defaultValue) {
  if (typeof index !== 'number') return defaultValue
  index = ~~index;  // Coerce to integer.
  if (index >= len) return len
  if (index >= 0) return index
  index += len
  if (index >= 0) return index
  return 0
}

function coerce (length) {
  // Coerce length to a number (possibly NaN), round up
  // in case it's fractional (e.g. 123.456) then do a
  // double negate to coerce a NaN to 0. Easy, right?
  length = ~~Math.ceil(+length)
  return length < 0 ? 0 : length
}

function isArray (subject) {
  return (Array.isArray || function (subject) {
    return Object.prototype.toString.call(subject) === '[object Array]'
  })(subject)
}

function isArrayish (subject) {
  return isArray(subject) || Buffer.isBuffer(subject) ||
      subject && typeof subject === 'object' &&
      typeof subject.length === 'number'
}

function toHex (n) {
  if (n < 16) return '0' + n.toString(16)
  return n.toString(16)
}

function utf8ToBytes (str) {
  var byteArray = []
  for (var i = 0; i < str.length; i++) {
    var b = str.charCodeAt(i)
    if (b <= 0x7F)
      byteArray.push(str.charCodeAt(i))
    else {
      var start = i
      if (b >= 0xD800 && b <= 0xDFFF) i++
      var h = encodeURIComponent(str.slice(start, i+1)).substr(1).split('%')
      for (var j = 0; j < h.length; j++)
        byteArray.push(parseInt(h[j], 16))
    }
  }
  return byteArray
}

function asciiToBytes (str) {
  var byteArray = []
  for (var i = 0; i < str.length; i++) {
    // Node's code seems to be doing this and not & 0x7F..
    byteArray.push(str.charCodeAt(i) & 0xFF)
  }
  return byteArray
}

function utf16leToBytes (str) {
  var c, hi, lo
  var byteArray = []
  for (var i = 0; i < str.length; i++) {
    c = str.charCodeAt(i)
    hi = c >> 8
    lo = c % 256
    byteArray.push(lo)
    byteArray.push(hi)
  }

  return byteArray
}

function base64ToBytes (str) {
  return base64.toByteArray(str)
}

function blitBuffer (src, dst, offset, length) {
  var pos
  for (var i = 0; i < length; i++) {
    if ((i + offset >= dst.length) || (i >= src.length))
      break
    dst[i + offset] = src[i]
  }
  return i
}

function decodeUtf8Char (str) {
  try {
    return decodeURIComponent(str)
  } catch (err) {
    return String.fromCharCode(0xFFFD) // UTF 8 invalid char
  }
}

/*
 * We have to make sure that the value is a valid integer. This means that it
 * is non-negative. It has no fractional component and that it does not
 * exceed the maximum allowed value.
 */
function verifuint (value, max) {
  assert(typeof value === 'number', 'cannot write a non-number as a number')
  assert(value >= 0, 'specified a negative value for writing an unsigned value')
  assert(value <= max, 'value is larger than maximum value for type')
  assert(Math.floor(value) === value, 'value has a fractional component')
}

function verifsint (value, max, min) {
  assert(typeof value === 'number', 'cannot write a non-number as a number')
  assert(value <= max, 'value larger than maximum allowed value')
  assert(value >= min, 'value smaller than minimum allowed value')
  assert(Math.floor(value) === value, 'value has a fractional component')
}

function verifIEEE754 (value, max, min) {
  assert(typeof value === 'number', 'cannot write a non-number as a number')
  assert(value <= max, 'value larger than maximum allowed value')
  assert(value >= min, 'value smaller than minimum allowed value')
}

function assert (test, message) {
  if (!test) throw new Error(message || 'Failed assertion')
}

}).call(this,require("e/U+97"),typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : {},require("buffer").Buffer,arguments[3],arguments[4],arguments[5],arguments[6],"/..\\..\\node_modules\\browserify\\node_modules\\buffer\\index.js","/..\\..\\node_modules\\browserify\\node_modules\\buffer")
},{"base64-js":1,"buffer":2,"e/U+97":4,"ieee754":3}],3:[function(require,module,exports){
(function (process,global,Buffer,__argument0,__argument1,__argument2,__argument3,__filename,__dirname){
exports.read = function (buffer, offset, isLE, mLen, nBytes) {
  var e, m
  var eLen = (nBytes * 8) - mLen - 1
  var eMax = (1 << eLen) - 1
  var eBias = eMax >> 1
  var nBits = -7
  var i = isLE ? (nBytes - 1) : 0
  var d = isLE ? -1 : 1
  var s = buffer[offset + i]

  i += d

  e = s & ((1 << (-nBits)) - 1)
  s >>= (-nBits)
  nBits += eLen
  for (; nBits > 0; e = (e * 256) + buffer[offset + i], i += d, nBits -= 8) {}

  m = e & ((1 << (-nBits)) - 1)
  e >>= (-nBits)
  nBits += mLen
  for (; nBits > 0; m = (m * 256) + buffer[offset + i], i += d, nBits -= 8) {}

  if (e === 0) {
    e = 1 - eBias
  } else if (e === eMax) {
    return m ? NaN : ((s ? -1 : 1) * Infinity)
  } else {
    m = m + Math.pow(2, mLen)
    e = e - eBias
  }
  return (s ? -1 : 1) * m * Math.pow(2, e - mLen)
}

exports.write = function (buffer, value, offset, isLE, mLen, nBytes) {
  var e, m, c
  var eLen = (nBytes * 8) - mLen - 1
  var eMax = (1 << eLen) - 1
  var eBias = eMax >> 1
  var rt = (mLen === 23 ? Math.pow(2, -24) - Math.pow(2, -77) : 0)
  var i = isLE ? 0 : (nBytes - 1)
  var d = isLE ? 1 : -1
  var s = value < 0 || (value === 0 && 1 / value < 0) ? 1 : 0

  value = Math.abs(value)

  if (isNaN(value) || value === Infinity) {
    m = isNaN(value) ? 1 : 0
    e = eMax
  } else {
    e = Math.floor(Math.log(value) / Math.LN2)
    if (value * (c = Math.pow(2, -e)) < 1) {
      e--
      c *= 2
    }
    if (e + eBias >= 1) {
      value += rt / c
    } else {
      value += rt * Math.pow(2, 1 - eBias)
    }
    if (value * c >= 2) {
      e++
      c /= 2
    }

    if (e + eBias >= eMax) {
      m = 0
      e = eMax
    } else if (e + eBias >= 1) {
      m = ((value * c) - 1) * Math.pow(2, mLen)
      e = e + eBias
    } else {
      m = value * Math.pow(2, eBias - 1) * Math.pow(2, mLen)
      e = 0
    }
  }

  for (; mLen >= 8; buffer[offset + i] = m & 0xff, i += d, m /= 256, mLen -= 8) {}

  e = (e << mLen) | m
  eLen += mLen
  for (; eLen > 0; buffer[offset + i] = e & 0xff, i += d, e /= 256, eLen -= 8) {}

  buffer[offset + i - d] |= s * 128
}

}).call(this,require("e/U+97"),typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : {},require("buffer").Buffer,arguments[3],arguments[4],arguments[5],arguments[6],"/..\\..\\node_modules\\browserify\\node_modules\\ieee754\\index.js","/..\\..\\node_modules\\browserify\\node_modules\\ieee754")
},{"buffer":2,"e/U+97":4}],4:[function(require,module,exports){
(function (process,global,Buffer,__argument0,__argument1,__argument2,__argument3,__filename,__dirname){
// shim for using process in browser

var process = module.exports = {};

process.nextTick = (function () {
    var canSetImmediate = typeof window !== 'undefined'
    && window.setImmediate;
    var canPost = typeof window !== 'undefined'
    && window.postMessage && window.addEventListener
    ;

    if (canSetImmediate) {
        return function (f) { return window.setImmediate(f) };
    }

    if (canPost) {
        var queue = [];
        window.addEventListener('message', function (ev) {
            var source = ev.source;
            if ((source === window || source === null) && ev.data === 'process-tick') {
                ev.stopPropagation();
                if (queue.length > 0) {
                    var fn = queue.shift();
                    fn();
                }
            }
        }, true);

        return function nextTick(fn) {
            queue.push(fn);
            window.postMessage('process-tick', '*');
        };
    }

    return function nextTick(fn) {
        setTimeout(fn, 0);
    };
})();

process.title = 'browser';
process.browser = true;
process.env = {};
process.argv = [];

function noop() {}

process.on = noop;
process.addListener = noop;
process.once = noop;
process.off = noop;
process.removeListener = noop;
process.removeAllListeners = noop;
process.emit = noop;

process.binding = function (name) {
    throw new Error('process.binding is not supported');
}

// TODO(shtylman)
process.cwd = function () { return '/' };
process.chdir = function (dir) {
    throw new Error('process.chdir is not supported');
};

}).call(this,require("e/U+97"),typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : {},require("buffer").Buffer,arguments[3],arguments[4],arguments[5],arguments[6],"/..\\..\\node_modules\\process\\browser.js","/..\\..\\node_modules\\process")
},{"buffer":2,"e/U+97":4}],5:[function(require,module,exports){
(function (process,global,Buffer,__argument0,__argument1,__argument2,__argument3,__filename,__dirname){
"use strict";

function _typeof(obj) { "@babel/helpers - typeof"; if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; } return _typeof(obj); }

!function (e, t) {
  "object" == (typeof exports === "undefined" ? "undefined" : _typeof(exports)) && "undefined" != typeof module ? module.exports = t() : "function" == typeof define && define.amd ? define(t) : (e = "undefined" != typeof globalThis ? globalThis : e || self).Swiper = t();
}(void 0, function () {
  "use strict";

  function e(e, t) {
    for (var i = 0; i < t.length; i++) {
      var a = t[i];
      a.enumerable = a.enumerable || !1, a.configurable = !0, "value" in a && (a.writable = !0), Object.defineProperty(e, a.key, a);
    }
  }

  function t() {
    return (t = Object.assign || function (e) {
      for (var t = 1; t < arguments.length; t++) {
        var i = arguments[t];

        for (var a in i) {
          Object.prototype.hasOwnProperty.call(i, a) && (e[a] = i[a]);
        }
      }

      return e;
    }).apply(this, arguments);
  }

  function i(e) {
    return null !== e && "object" == _typeof(e) && "constructor" in e && e.constructor === Object;
  }

  function a(e, t) {
    void 0 === e && (e = {}), void 0 === t && (t = {}), Object.keys(t).forEach(function (r) {
      void 0 === e[r] ? e[r] = t[r] : i(t[r]) && i(e[r]) && Object.keys(t[r]).length > 0 && a(e[r], t[r]);
    });
  }

  var r = {
    body: {},
    addEventListener: function addEventListener() {},
    removeEventListener: function removeEventListener() {},
    activeElement: {
      blur: function blur() {},
      nodeName: ""
    },
    querySelector: function querySelector() {
      return null;
    },
    querySelectorAll: function querySelectorAll() {
      return [];
    },
    getElementById: function getElementById() {
      return null;
    },
    createEvent: function createEvent() {
      return {
        initEvent: function initEvent() {}
      };
    },
    createElement: function createElement() {
      return {
        children: [],
        childNodes: [],
        style: {},
        setAttribute: function setAttribute() {},
        getElementsByTagName: function getElementsByTagName() {
          return [];
        }
      };
    },
    createElementNS: function createElementNS() {
      return {};
    },
    importNode: function importNode() {
      return null;
    },
    location: {
      hash: "",
      host: "",
      hostname: "",
      href: "",
      origin: "",
      pathname: "",
      protocol: "",
      search: ""
    }
  };

  function n() {
    var e = "undefined" != typeof document ? document : {};
    return a(e, r), e;
  }

  var s = {
    document: r,
    navigator: {
      userAgent: ""
    },
    location: {
      hash: "",
      host: "",
      hostname: "",
      href: "",
      origin: "",
      pathname: "",
      protocol: "",
      search: ""
    },
    history: {
      replaceState: function replaceState() {},
      pushState: function pushState() {},
      go: function go() {},
      back: function back() {}
    },
    CustomEvent: function CustomEvent() {
      return this;
    },
    addEventListener: function addEventListener() {},
    removeEventListener: function removeEventListener() {},
    getComputedStyle: function getComputedStyle() {
      return {
        getPropertyValue: function getPropertyValue() {
          return "";
        }
      };
    },
    Image: function Image() {},
    Date: function Date() {},
    screen: {},
    setTimeout: function setTimeout() {},
    clearTimeout: function clearTimeout() {},
    matchMedia: function matchMedia() {
      return {};
    },
    requestAnimationFrame: function requestAnimationFrame(e) {
      return "undefined" == typeof setTimeout ? (e(), null) : setTimeout(e, 0);
    },
    cancelAnimationFrame: function cancelAnimationFrame(e) {
      "undefined" != typeof setTimeout && clearTimeout(e);
    }
  };

  function o() {
    var e = "undefined" != typeof window ? window : {};
    return a(e, s), e;
  }

  function l(e) {
    return (l = Object.setPrototypeOf ? Object.getPrototypeOf : function (e) {
      return e.__proto__ || Object.getPrototypeOf(e);
    })(e);
  }

  function d(e, t) {
    return (d = Object.setPrototypeOf || function (e, t) {
      return e.__proto__ = t, e;
    })(e, t);
  }

  function u() {
    if ("undefined" == typeof Reflect || !Reflect.construct) return !1;
    if (Reflect.construct.sham) return !1;
    if ("function" == typeof Proxy) return !0;

    try {
      return Date.prototype.toString.call(Reflect.construct(Date, [], function () {})), !0;
    } catch (e) {
      return !1;
    }
  }

  function p(e, t, i) {
    return (p = u() ? Reflect.construct : function (e, t, i) {
      var a = [null];
      a.push.apply(a, t);
      var r = new (Function.bind.apply(e, a))();
      return i && d(r, i.prototype), r;
    }).apply(null, arguments);
  }

  function c(e) {
    var t = "function" == typeof Map ? new Map() : void 0;
    return (c = function c(e) {
      if (null === e || (i = e, -1 === Function.toString.call(i).indexOf("[native code]"))) return e;
      var i;
      if ("function" != typeof e) throw new TypeError("Super expression must either be null or a function");

      if (void 0 !== t) {
        if (t.has(e)) return t.get(e);
        t.set(e, a);
      }

      function a() {
        return p(e, arguments, l(this).constructor);
      }

      return a.prototype = Object.create(e.prototype, {
        constructor: {
          value: a,
          enumerable: !1,
          writable: !0,
          configurable: !0
        }
      }), d(a, e);
    })(e);
  }

  var h = function (e) {
    var t, i;

    function a(t) {
      var i, a, r;
      return a = function (e) {
        if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
        return e;
      }(i = e.call.apply(e, [this].concat(t)) || this), r = a.__proto__, Object.defineProperty(a, "__proto__", {
        get: function get() {
          return r;
        },
        set: function set(e) {
          r.__proto__ = e;
        }
      }), i;
    }

    return i = e, (t = a).prototype = Object.create(i.prototype), t.prototype.constructor = t, t.__proto__ = i, a;
  }(c(Array));

  function f(e) {
    void 0 === e && (e = []);
    var t = [];
    return e.forEach(function (e) {
      Array.isArray(e) ? t.push.apply(t, f(e)) : t.push(e);
    }), t;
  }

  function m(e, t) {
    return Array.prototype.filter.call(e, t);
  }

  function v(e, t) {
    var i = o(),
        a = n(),
        r = [];
    if (!t && e instanceof h) return e;
    if (!e) return new h(r);

    if ("string" == typeof e) {
      var s = e.trim();

      if (s.indexOf("<") >= 0 && s.indexOf(">") >= 0) {
        var l = "div";
        0 === s.indexOf("<li") && (l = "ul"), 0 === s.indexOf("<tr") && (l = "tbody"), 0 !== s.indexOf("<td") && 0 !== s.indexOf("<th") || (l = "tr"), 0 === s.indexOf("<tbody") && (l = "table"), 0 === s.indexOf("<option") && (l = "select");
        var d = a.createElement(l);
        d.innerHTML = s;

        for (var u = 0; u < d.childNodes.length; u += 1) {
          r.push(d.childNodes[u]);
        }
      } else r = function (e, t) {
        if ("string" != typeof e) return [e];

        for (var i = [], a = t.querySelectorAll(e), r = 0; r < a.length; r += 1) {
          i.push(a[r]);
        }

        return i;
      }(e.trim(), t || a);
    } else if (e.nodeType || e === i || e === a) r.push(e);else if (Array.isArray(e)) {
      if (e instanceof h) return e;
      r = e;
    }

    return new h(function (e) {
      for (var t = [], i = 0; i < e.length; i += 1) {
        -1 === t.indexOf(e[i]) && t.push(e[i]);
      }

      return t;
    }(r));
  }

  v.fn = h.prototype;
  var g,
      y,
      w,
      b = {
    addClass: function addClass() {
      for (var e = arguments.length, t = new Array(e), i = 0; i < e; i++) {
        t[i] = arguments[i];
      }

      var a = f(t.map(function (e) {
        return e.split(" ");
      }));
      return this.forEach(function (e) {
        var t;
        (t = e.classList).add.apply(t, a);
      }), this;
    },
    removeClass: function removeClass() {
      for (var e = arguments.length, t = new Array(e), i = 0; i < e; i++) {
        t[i] = arguments[i];
      }

      var a = f(t.map(function (e) {
        return e.split(" ");
      }));
      return this.forEach(function (e) {
        var t;
        (t = e.classList).remove.apply(t, a);
      }), this;
    },
    hasClass: function hasClass() {
      for (var e = arguments.length, t = new Array(e), i = 0; i < e; i++) {
        t[i] = arguments[i];
      }

      var a = f(t.map(function (e) {
        return e.split(" ");
      }));
      return m(this, function (e) {
        return a.filter(function (t) {
          return e.classList.contains(t);
        }).length > 0;
      }).length > 0;
    },
    toggleClass: function toggleClass() {
      for (var e = arguments.length, t = new Array(e), i = 0; i < e; i++) {
        t[i] = arguments[i];
      }

      var a = f(t.map(function (e) {
        return e.split(" ");
      }));
      this.forEach(function (e) {
        a.forEach(function (t) {
          e.classList.toggle(t);
        });
      });
    },
    attr: function attr(e, t) {
      if (1 === arguments.length && "string" == typeof e) return this[0] ? this[0].getAttribute(e) : void 0;

      for (var i = 0; i < this.length; i += 1) {
        if (2 === arguments.length) this[i].setAttribute(e, t);else for (var a in e) {
          this[i][a] = e[a], this[i].setAttribute(a, e[a]);
        }
      }

      return this;
    },
    removeAttr: function removeAttr(e) {
      for (var t = 0; t < this.length; t += 1) {
        this[t].removeAttribute(e);
      }

      return this;
    },
    transform: function transform(e) {
      for (var t = 0; t < this.length; t += 1) {
        this[t].style.transform = e;
      }

      return this;
    },
    transition: function transition(e) {
      for (var t = 0; t < this.length; t += 1) {
        this[t].style.transitionDuration = "string" != typeof e ? e + "ms" : e;
      }

      return this;
    },
    on: function on() {
      for (var e = arguments.length, t = new Array(e), i = 0; i < e; i++) {
        t[i] = arguments[i];
      }

      var a = t[0],
          r = t[1],
          n = t[2],
          s = t[3];

      function o(e) {
        var t = e.target;

        if (t) {
          var i = e.target.dom7EventData || [];
          if (i.indexOf(e) < 0 && i.unshift(e), v(t).is(r)) n.apply(t, i);else for (var a = v(t).parents(), s = 0; s < a.length; s += 1) {
            v(a[s]).is(r) && n.apply(a[s], i);
          }
        }
      }

      function l(e) {
        var t = e && e.target && e.target.dom7EventData || [];
        t.indexOf(e) < 0 && t.unshift(e), n.apply(this, t);
      }

      "function" == typeof t[1] && (a = t[0], n = t[1], s = t[2], r = void 0), s || (s = !1);

      for (var d, u = a.split(" "), p = 0; p < this.length; p += 1) {
        var c = this[p];
        if (r) for (d = 0; d < u.length; d += 1) {
          var h = u[d];
          c.dom7LiveListeners || (c.dom7LiveListeners = {}), c.dom7LiveListeners[h] || (c.dom7LiveListeners[h] = []), c.dom7LiveListeners[h].push({
            listener: n,
            proxyListener: o
          }), c.addEventListener(h, o, s);
        } else for (d = 0; d < u.length; d += 1) {
          var f = u[d];
          c.dom7Listeners || (c.dom7Listeners = {}), c.dom7Listeners[f] || (c.dom7Listeners[f] = []), c.dom7Listeners[f].push({
            listener: n,
            proxyListener: l
          }), c.addEventListener(f, l, s);
        }
      }

      return this;
    },
    off: function off() {
      for (var e = arguments.length, t = new Array(e), i = 0; i < e; i++) {
        t[i] = arguments[i];
      }

      var a = t[0],
          r = t[1],
          n = t[2],
          s = t[3];
      "function" == typeof t[1] && (a = t[0], n = t[1], s = t[2], r = void 0), s || (s = !1);

      for (var o = a.split(" "), l = 0; l < o.length; l += 1) {
        for (var d = o[l], u = 0; u < this.length; u += 1) {
          var p = this[u],
              c = void 0;
          if (!r && p.dom7Listeners ? c = p.dom7Listeners[d] : r && p.dom7LiveListeners && (c = p.dom7LiveListeners[d]), c && c.length) for (var h = c.length - 1; h >= 0; h -= 1) {
            var f = c[h];
            n && f.listener === n || n && f.listener && f.listener.dom7proxy && f.listener.dom7proxy === n ? (p.removeEventListener(d, f.proxyListener, s), c.splice(h, 1)) : n || (p.removeEventListener(d, f.proxyListener, s), c.splice(h, 1));
          }
        }
      }

      return this;
    },
    trigger: function trigger() {
      for (var e = o(), t = arguments.length, i = new Array(t), a = 0; a < t; a++) {
        i[a] = arguments[a];
      }

      for (var r = i[0].split(" "), n = i[1], s = 0; s < r.length; s += 1) {
        for (var l = r[s], d = 0; d < this.length; d += 1) {
          var u = this[d];

          if (e.CustomEvent) {
            var p = new e.CustomEvent(l, {
              detail: n,
              bubbles: !0,
              cancelable: !0
            });
            u.dom7EventData = i.filter(function (e, t) {
              return t > 0;
            }), u.dispatchEvent(p), u.dom7EventData = [], delete u.dom7EventData;
          }
        }
      }

      return this;
    },
    transitionEnd: function transitionEnd(e) {
      var t = this;
      return e && t.on("transitionend", function i(a) {
        a.target === this && (e.call(this, a), t.off("transitionend", i));
      }), this;
    },
    outerWidth: function outerWidth(e) {
      if (this.length > 0) {
        if (e) {
          var t = this.styles();
          return this[0].offsetWidth + parseFloat(t.getPropertyValue("margin-right")) + parseFloat(t.getPropertyValue("margin-left"));
        }

        return this[0].offsetWidth;
      }

      return null;
    },
    outerHeight: function outerHeight(e) {
      if (this.length > 0) {
        if (e) {
          var t = this.styles();
          return this[0].offsetHeight + parseFloat(t.getPropertyValue("margin-top")) + parseFloat(t.getPropertyValue("margin-bottom"));
        }

        return this[0].offsetHeight;
      }

      return null;
    },
    styles: function styles() {
      var e = o();
      return this[0] ? e.getComputedStyle(this[0], null) : {};
    },
    offset: function offset() {
      if (this.length > 0) {
        var e = o(),
            t = n(),
            i = this[0],
            a = i.getBoundingClientRect(),
            r = t.body,
            s = i.clientTop || r.clientTop || 0,
            l = i.clientLeft || r.clientLeft || 0,
            d = i === e ? e.scrollY : i.scrollTop,
            u = i === e ? e.scrollX : i.scrollLeft;
        return {
          top: a.top + d - s,
          left: a.left + u - l
        };
      }

      return null;
    },
    css: function css(e, t) {
      var i,
          a = o();

      if (1 === arguments.length) {
        if ("string" != typeof e) {
          for (i = 0; i < this.length; i += 1) {
            for (var r in e) {
              this[i].style[r] = e[r];
            }
          }

          return this;
        }

        if (this[0]) return a.getComputedStyle(this[0], null).getPropertyValue(e);
      }

      if (2 === arguments.length && "string" == typeof e) {
        for (i = 0; i < this.length; i += 1) {
          this[i].style[e] = t;
        }

        return this;
      }

      return this;
    },
    each: function each(e) {
      return e ? (this.forEach(function (t, i) {
        e.apply(t, [t, i]);
      }), this) : this;
    },
    html: function html(e) {
      if (void 0 === e) return this[0] ? this[0].innerHTML : null;

      for (var t = 0; t < this.length; t += 1) {
        this[t].innerHTML = e;
      }

      return this;
    },
    text: function text(e) {
      if (void 0 === e) return this[0] ? this[0].textContent.trim() : null;

      for (var t = 0; t < this.length; t += 1) {
        this[t].textContent = e;
      }

      return this;
    },
    is: function is(e) {
      var t,
          i,
          a = o(),
          r = n(),
          s = this[0];
      if (!s || void 0 === e) return !1;

      if ("string" == typeof e) {
        if (s.matches) return s.matches(e);
        if (s.webkitMatchesSelector) return s.webkitMatchesSelector(e);
        if (s.msMatchesSelector) return s.msMatchesSelector(e);

        for (t = v(e), i = 0; i < t.length; i += 1) {
          if (t[i] === s) return !0;
        }

        return !1;
      }

      if (e === r) return s === r;
      if (e === a) return s === a;

      if (e.nodeType || e instanceof h) {
        for (t = e.nodeType ? [e] : e, i = 0; i < t.length; i += 1) {
          if (t[i] === s) return !0;
        }

        return !1;
      }

      return !1;
    },
    index: function index() {
      var e,
          t = this[0];

      if (t) {
        for (e = 0; null !== (t = t.previousSibling);) {
          1 === t.nodeType && (e += 1);
        }

        return e;
      }
    },
    eq: function eq(e) {
      if (void 0 === e) return this;
      var t = this.length;
      if (e > t - 1) return v([]);

      if (e < 0) {
        var i = t + e;
        return v(i < 0 ? [] : [this[i]]);
      }

      return v([this[e]]);
    },
    append: function append() {
      for (var e, t = n(), i = 0; i < arguments.length; i += 1) {
        e = i < 0 || arguments.length <= i ? void 0 : arguments[i];

        for (var a = 0; a < this.length; a += 1) {
          if ("string" == typeof e) {
            var r = t.createElement("div");

            for (r.innerHTML = e; r.firstChild;) {
              this[a].appendChild(r.firstChild);
            }
          } else if (e instanceof h) for (var s = 0; s < e.length; s += 1) {
            this[a].appendChild(e[s]);
          } else this[a].appendChild(e);
        }
      }

      return this;
    },
    prepend: function prepend(e) {
      var t,
          i,
          a = n();

      for (t = 0; t < this.length; t += 1) {
        if ("string" == typeof e) {
          var r = a.createElement("div");

          for (r.innerHTML = e, i = r.childNodes.length - 1; i >= 0; i -= 1) {
            this[t].insertBefore(r.childNodes[i], this[t].childNodes[0]);
          }
        } else if (e instanceof h) for (i = 0; i < e.length; i += 1) {
          this[t].insertBefore(e[i], this[t].childNodes[0]);
        } else this[t].insertBefore(e, this[t].childNodes[0]);
      }

      return this;
    },
    next: function next(e) {
      return this.length > 0 ? e ? this[0].nextElementSibling && v(this[0].nextElementSibling).is(e) ? v([this[0].nextElementSibling]) : v([]) : this[0].nextElementSibling ? v([this[0].nextElementSibling]) : v([]) : v([]);
    },
    nextAll: function nextAll(e) {
      var t = [],
          i = this[0];
      if (!i) return v([]);

      for (; i.nextElementSibling;) {
        var a = i.nextElementSibling;
        e ? v(a).is(e) && t.push(a) : t.push(a), i = a;
      }

      return v(t);
    },
    prev: function prev(e) {
      if (this.length > 0) {
        var t = this[0];
        return e ? t.previousElementSibling && v(t.previousElementSibling).is(e) ? v([t.previousElementSibling]) : v([]) : t.previousElementSibling ? v([t.previousElementSibling]) : v([]);
      }

      return v([]);
    },
    prevAll: function prevAll(e) {
      var t = [],
          i = this[0];
      if (!i) return v([]);

      for (; i.previousElementSibling;) {
        var a = i.previousElementSibling;
        e ? v(a).is(e) && t.push(a) : t.push(a), i = a;
      }

      return v(t);
    },
    parent: function parent(e) {
      for (var t = [], i = 0; i < this.length; i += 1) {
        null !== this[i].parentNode && (e ? v(this[i].parentNode).is(e) && t.push(this[i].parentNode) : t.push(this[i].parentNode));
      }

      return v(t);
    },
    parents: function parents(e) {
      for (var t = [], i = 0; i < this.length; i += 1) {
        for (var a = this[i].parentNode; a;) {
          e ? v(a).is(e) && t.push(a) : t.push(a), a = a.parentNode;
        }
      }

      return v(t);
    },
    closest: function closest(e) {
      var t = this;
      return void 0 === e ? v([]) : (t.is(e) || (t = t.parents(e).eq(0)), t);
    },
    find: function find(e) {
      for (var t = [], i = 0; i < this.length; i += 1) {
        for (var a = this[i].querySelectorAll(e), r = 0; r < a.length; r += 1) {
          t.push(a[r]);
        }
      }

      return v(t);
    },
    children: function children(e) {
      for (var t = [], i = 0; i < this.length; i += 1) {
        for (var a = this[i].children, r = 0; r < a.length; r += 1) {
          e && !v(a[r]).is(e) || t.push(a[r]);
        }
      }

      return v(t);
    },
    filter: function filter(e) {
      return v(m(this, e));
    },
    remove: function remove() {
      for (var e = 0; e < this.length; e += 1) {
        this[e].parentNode && this[e].parentNode.removeChild(this[e]);
      }

      return this;
    }
  };

  function x(e, t) {
    return void 0 === t && (t = 0), setTimeout(e, t);
  }

  function T() {
    return Date.now();
  }

  function _(e, t) {
    void 0 === t && (t = "x");
    var i,
        a,
        r,
        n = o(),
        s = n.getComputedStyle(e, null);
    return n.WebKitCSSMatrix ? ((a = s.transform || s.webkitTransform).split(",").length > 6 && (a = a.split(", ").map(function (e) {
      return e.replace(",", ".");
    }).join(", ")), r = new n.WebKitCSSMatrix("none" === a ? "" : a)) : i = (r = s.MozTransform || s.OTransform || s.MsTransform || s.msTransform || s.transform || s.getPropertyValue("transform").replace("translate(", "matrix(1, 0, 0, 1,")).toString().split(","), "x" === t && (a = n.WebKitCSSMatrix ? r.m41 : 16 === i.length ? parseFloat(i[12]) : parseFloat(i[4])), "y" === t && (a = n.WebKitCSSMatrix ? r.m42 : 16 === i.length ? parseFloat(i[13]) : parseFloat(i[5])), a || 0;
  }

  function E(e) {
    return "object" == _typeof(e) && null !== e && e.constructor && e.constructor === Object;
  }

  function C() {
    for (var e = Object(arguments.length <= 0 ? void 0 : arguments[0]), t = 1; t < arguments.length; t += 1) {
      var i = t < 0 || arguments.length <= t ? void 0 : arguments[t];
      if (null != i) for (var a = Object.keys(Object(i)), r = 0, n = a.length; r < n; r += 1) {
        var s = a[r],
            o = Object.getOwnPropertyDescriptor(i, s);
        void 0 !== o && o.enumerable && (E(e[s]) && E(i[s]) ? C(e[s], i[s]) : !E(e[s]) && E(i[s]) ? (e[s] = {}, C(e[s], i[s])) : e[s] = i[s]);
      }
    }

    return e;
  }

  function S(e, t) {
    Object.keys(t).forEach(function (i) {
      E(t[i]) && Object.keys(t[i]).forEach(function (a) {
        "function" == typeof t[i][a] && (t[i][a] = t[i][a].bind(e));
      }), e[i] = t[i];
    });
  }

  function M() {
    return g || (g = function () {
      var e = o(),
          t = n();
      return {
        touch: !!("ontouchstart" in e || e.DocumentTouch && t instanceof e.DocumentTouch),
        pointerEvents: !!e.PointerEvent && "maxTouchPoints" in e.navigator && e.navigator.maxTouchPoints >= 0,
        observer: "MutationObserver" in e || "WebkitMutationObserver" in e,
        passiveListener: function () {
          var t = !1;

          try {
            var i = Object.defineProperty({}, "passive", {
              get: function get() {
                t = !0;
              }
            });
            e.addEventListener("testPassiveListener", null, i);
          } catch (e) {}

          return t;
        }(),
        gestures: "ongesturestart" in e
      };
    }()), g;
  }

  function z(e) {
    return void 0 === e && (e = {}), y || (y = function (e) {
      var t = (void 0 === e ? {} : e).userAgent,
          i = M(),
          a = o(),
          r = a.navigator.platform,
          n = t || a.navigator.userAgent,
          s = {
        ios: !1,
        android: !1
      },
          l = a.screen.width,
          d = a.screen.height,
          u = n.match(/(Android);?[\s\/]+([\d.]+)?/),
          p = n.match(/(iPad).*OS\s([\d_]+)/),
          c = n.match(/(iPod)(.*OS\s([\d_]+))?/),
          h = !p && n.match(/(iPhone\sOS|iOS)\s([\d_]+)/),
          f = "Win32" === r,
          m = "MacIntel" === r;
      return !p && m && i.touch && ["1024x1366", "1366x1024", "834x1194", "1194x834", "834x1112", "1112x834", "768x1024", "1024x768", "820x1180", "1180x820", "810x1080", "1080x810"].indexOf(l + "x" + d) >= 0 && ((p = n.match(/(Version)\/([\d.]+)/)) || (p = [0, 1, "13_0_0"]), m = !1), u && !f && (s.os = "android", s.android = !0), (p || h || c) && (s.os = "ios", s.ios = !0), s;
    }(e)), y;
  }

  function k() {
    return w || (w = function () {
      var e,
          t = o();
      return {
        isEdge: !!t.navigator.userAgent.match(/Edge/g),
        isSafari: (e = t.navigator.userAgent.toLowerCase(), e.indexOf("safari") >= 0 && e.indexOf("chrome") < 0 && e.indexOf("android") < 0),
        isWebView: /(iPhone|iPod|iPad).*AppleWebKit(?!.*Safari)/i.test(t.navigator.userAgent)
      };
    }()), w;
  }

  Object.keys(b).forEach(function (e) {
    v.fn[e] = b[e];
  });
  var P = {
    name: "resize",
    create: function create() {
      var e = this;
      C(e, {
        resize: {
          observer: null,
          createObserver: function createObserver() {
            e && !e.destroyed && e.initialized && (e.resize.observer = new ResizeObserver(function (t) {
              var i = e.width,
                  a = e.height,
                  r = i,
                  n = a;
              t.forEach(function (t) {
                var i = t.contentBoxSize,
                    a = t.contentRect,
                    s = t.target;
                s && s !== e.el || (r = a ? a.width : (i[0] || i).inlineSize, n = a ? a.height : (i[0] || i).blockSize);
              }), r === i && n === a || e.resize.resizeHandler();
            }), e.resize.observer.observe(e.el));
          },
          removeObserver: function removeObserver() {
            e.resize.observer && e.resize.observer.unobserve && e.el && (e.resize.observer.unobserve(e.el), e.resize.observer = null);
          },
          resizeHandler: function resizeHandler() {
            e && !e.destroyed && e.initialized && (e.emit("beforeResize"), e.emit("resize"));
          },
          orientationChangeHandler: function orientationChangeHandler() {
            e && !e.destroyed && e.initialized && e.emit("orientationchange");
          }
        }
      });
    },
    on: {
      init: function init(e) {
        var t = o();
        e.params.resizeObserver && void 0 !== o().ResizeObserver ? e.resize.createObserver() : (t.addEventListener("resize", e.resize.resizeHandler), t.addEventListener("orientationchange", e.resize.orientationChangeHandler));
      },
      destroy: function destroy(e) {
        var t = o();
        e.resize.removeObserver(), t.removeEventListener("resize", e.resize.resizeHandler), t.removeEventListener("orientationchange", e.resize.orientationChangeHandler);
      }
    }
  },
      O = {
    attach: function attach(e, t) {
      void 0 === t && (t = {});
      var i = o(),
          a = this,
          r = new (i.MutationObserver || i.WebkitMutationObserver)(function (e) {
        if (1 !== e.length) {
          var t = function t() {
            a.emit("observerUpdate", e[0]);
          };

          i.requestAnimationFrame ? i.requestAnimationFrame(t) : i.setTimeout(t, 0);
        } else a.emit("observerUpdate", e[0]);
      });
      r.observe(e, {
        attributes: void 0 === t.attributes || t.attributes,
        childList: void 0 === t.childList || t.childList,
        characterData: void 0 === t.characterData || t.characterData
      }), a.observer.observers.push(r);
    },
    init: function init() {
      var e = this;

      if (e.support.observer && e.params.observer) {
        if (e.params.observeParents) for (var t = e.$el.parents(), i = 0; i < t.length; i += 1) {
          e.observer.attach(t[i]);
        }
        e.observer.attach(e.$el[0], {
          childList: e.params.observeSlideChildren
        }), e.observer.attach(e.$wrapperEl[0], {
          attributes: !1
        });
      }
    },
    destroy: function destroy() {
      this.observer.observers.forEach(function (e) {
        e.disconnect();
      }), this.observer.observers = [];
    }
  },
      L = {
    name: "observer",
    params: {
      observer: !1,
      observeParents: !1,
      observeSlideChildren: !1
    },
    create: function create() {
      S(this, {
        observer: t({}, O, {
          observers: []
        })
      });
    },
    on: {
      init: function init(e) {
        e.observer.init();
      },
      destroy: function destroy(e) {
        e.observer.destroy();
      }
    }
  };

  function A(e) {
    var t = this,
        i = n(),
        a = o(),
        r = t.touchEventsData,
        s = t.params,
        l = t.touches;

    if (!t.animating || !s.preventInteractionOnTransition) {
      var d = e;
      d.originalEvent && (d = d.originalEvent);
      var u = v(d.target);
      if (("wrapper" !== s.touchEventsTarget || u.closest(t.wrapperEl).length) && (r.isTouchEvent = "touchstart" === d.type, (r.isTouchEvent || !("which" in d) || 3 !== d.which) && !(!r.isTouchEvent && "button" in d && d.button > 0 || r.isTouched && r.isMoved))) if (!!s.noSwipingClass && "" !== s.noSwipingClass && d.target && d.target.shadowRoot && e.path && e.path[0] && (u = v(e.path[0])), s.noSwiping && u.closest(s.noSwipingSelector ? s.noSwipingSelector : "." + s.noSwipingClass)[0]) t.allowClick = !0;else if (!s.swipeHandler || u.closest(s.swipeHandler)[0]) {
        l.currentX = "touchstart" === d.type ? d.targetTouches[0].pageX : d.pageX, l.currentY = "touchstart" === d.type ? d.targetTouches[0].pageY : d.pageY;
        var p = l.currentX,
            c = l.currentY,
            h = s.edgeSwipeDetection || s.iOSEdgeSwipeDetection,
            f = s.edgeSwipeThreshold || s.iOSEdgeSwipeThreshold;

        if (h && (p <= f || p >= a.innerWidth - f)) {
          if ("prevent" !== h) return;
          e.preventDefault();
        }

        if (C(r, {
          isTouched: !0,
          isMoved: !1,
          allowTouchCallbacks: !0,
          isScrolling: void 0,
          startMoving: void 0
        }), l.startX = p, l.startY = c, r.touchStartTime = T(), t.allowClick = !0, t.updateSize(), t.swipeDirection = void 0, s.threshold > 0 && (r.allowThresholdMove = !1), "touchstart" !== d.type) {
          var m = !0;
          u.is(r.formElements) && (m = !1), i.activeElement && v(i.activeElement).is(r.formElements) && i.activeElement !== u[0] && i.activeElement.blur();
          var g = m && t.allowTouchMove && s.touchStartPreventDefault;
          !s.touchStartForcePreventDefault && !g || u[0].isContentEditable || d.preventDefault();
        }

        t.emit("touchStart", d);
      }
    }
  }

  function $(e) {
    var t = n(),
        i = this,
        a = i.touchEventsData,
        r = i.params,
        s = i.touches,
        o = i.rtlTranslate,
        l = e;

    if (l.originalEvent && (l = l.originalEvent), a.isTouched) {
      if (!a.isTouchEvent || "touchmove" === l.type) {
        var d = "touchmove" === l.type && l.targetTouches && (l.targetTouches[0] || l.changedTouches[0]),
            u = "touchmove" === l.type ? d.pageX : l.pageX,
            p = "touchmove" === l.type ? d.pageY : l.pageY;
        if (l.preventedByNestedSwiper) return s.startX = u, void (s.startY = p);
        if (!i.allowTouchMove) return i.allowClick = !1, void (a.isTouched && (C(s, {
          startX: u,
          startY: p,
          currentX: u,
          currentY: p
        }), a.touchStartTime = T()));
        if (a.isTouchEvent && r.touchReleaseOnEdges && !r.loop) if (i.isVertical()) {
          if (p < s.startY && i.translate <= i.maxTranslate() || p > s.startY && i.translate >= i.minTranslate()) return a.isTouched = !1, void (a.isMoved = !1);
        } else if (u < s.startX && i.translate <= i.maxTranslate() || u > s.startX && i.translate >= i.minTranslate()) return;
        if (a.isTouchEvent && t.activeElement && l.target === t.activeElement && v(l.target).is(a.formElements)) return a.isMoved = !0, void (i.allowClick = !1);

        if (a.allowTouchCallbacks && i.emit("touchMove", l), !(l.targetTouches && l.targetTouches.length > 1)) {
          s.currentX = u, s.currentY = p;
          var c,
              h = s.currentX - s.startX,
              f = s.currentY - s.startY;
          if (!(i.params.threshold && Math.sqrt(Math.pow(h, 2) + Math.pow(f, 2)) < i.params.threshold)) if (void 0 === a.isScrolling && (i.isHorizontal() && s.currentY === s.startY || i.isVertical() && s.currentX === s.startX ? a.isScrolling = !1 : h * h + f * f >= 25 && (c = 180 * Math.atan2(Math.abs(f), Math.abs(h)) / Math.PI, a.isScrolling = i.isHorizontal() ? c > r.touchAngle : 90 - c > r.touchAngle)), a.isScrolling && i.emit("touchMoveOpposite", l), void 0 === a.startMoving && (s.currentX === s.startX && s.currentY === s.startY || (a.startMoving = !0)), a.isScrolling) a.isTouched = !1;else if (a.startMoving) {
            i.allowClick = !1, !r.cssMode && l.cancelable && l.preventDefault(), r.touchMoveStopPropagation && !r.nested && l.stopPropagation(), a.isMoved || (r.loop && i.loopFix(), a.startTranslate = i.getTranslate(), i.setTransition(0), i.animating && i.$wrapperEl.trigger("webkitTransitionEnd transitionend"), a.allowMomentumBounce = !1, !r.grabCursor || !0 !== i.allowSlideNext && !0 !== i.allowSlidePrev || i.setGrabCursor(!0), i.emit("sliderFirstMove", l)), i.emit("sliderMove", l), a.isMoved = !0;
            var m = i.isHorizontal() ? h : f;
            s.diff = m, m *= r.touchRatio, o && (m = -m), i.swipeDirection = m > 0 ? "prev" : "next", a.currentTranslate = m + a.startTranslate;
            var g = !0,
                y = r.resistanceRatio;

            if (r.touchReleaseOnEdges && (y = 0), m > 0 && a.currentTranslate > i.minTranslate() ? (g = !1, r.resistance && (a.currentTranslate = i.minTranslate() - 1 + Math.pow(-i.minTranslate() + a.startTranslate + m, y))) : m < 0 && a.currentTranslate < i.maxTranslate() && (g = !1, r.resistance && (a.currentTranslate = i.maxTranslate() + 1 - Math.pow(i.maxTranslate() - a.startTranslate - m, y))), g && (l.preventedByNestedSwiper = !0), !i.allowSlideNext && "next" === i.swipeDirection && a.currentTranslate < a.startTranslate && (a.currentTranslate = a.startTranslate), !i.allowSlidePrev && "prev" === i.swipeDirection && a.currentTranslate > a.startTranslate && (a.currentTranslate = a.startTranslate), i.allowSlidePrev || i.allowSlideNext || (a.currentTranslate = a.startTranslate), r.threshold > 0) {
              if (!(Math.abs(m) > r.threshold || a.allowThresholdMove)) return void (a.currentTranslate = a.startTranslate);
              if (!a.allowThresholdMove) return a.allowThresholdMove = !0, s.startX = s.currentX, s.startY = s.currentY, a.currentTranslate = a.startTranslate, void (s.diff = i.isHorizontal() ? s.currentX - s.startX : s.currentY - s.startY);
            }

            r.followFinger && !r.cssMode && ((r.freeMode || r.watchSlidesProgress || r.watchSlidesVisibility) && (i.updateActiveIndex(), i.updateSlidesClasses()), r.freeMode && (0 === a.velocities.length && a.velocities.push({
              position: s[i.isHorizontal() ? "startX" : "startY"],
              time: a.touchStartTime
            }), a.velocities.push({
              position: s[i.isHorizontal() ? "currentX" : "currentY"],
              time: T()
            })), i.updateProgress(a.currentTranslate), i.setTranslate(a.currentTranslate));
          }
        }
      }
    } else a.startMoving && a.isScrolling && i.emit("touchMoveOpposite", l);
  }

  function D(e) {
    var t = this,
        i = t.touchEventsData,
        a = t.params,
        r = t.touches,
        n = t.rtlTranslate,
        s = t.$wrapperEl,
        o = t.slidesGrid,
        l = t.snapGrid,
        d = e;
    if (d.originalEvent && (d = d.originalEvent), i.allowTouchCallbacks && t.emit("touchEnd", d), i.allowTouchCallbacks = !1, !i.isTouched) return i.isMoved && a.grabCursor && t.setGrabCursor(!1), i.isMoved = !1, void (i.startMoving = !1);
    a.grabCursor && i.isMoved && i.isTouched && (!0 === t.allowSlideNext || !0 === t.allowSlidePrev) && t.setGrabCursor(!1);
    var u,
        p = T(),
        c = p - i.touchStartTime;
    if (t.allowClick && (t.updateClickedSlide(d), t.emit("tap click", d), c < 300 && p - i.lastClickTime < 300 && t.emit("doubleTap doubleClick", d)), i.lastClickTime = T(), x(function () {
      t.destroyed || (t.allowClick = !0);
    }), !i.isTouched || !i.isMoved || !t.swipeDirection || 0 === r.diff || i.currentTranslate === i.startTranslate) return i.isTouched = !1, i.isMoved = !1, void (i.startMoving = !1);
    if (i.isTouched = !1, i.isMoved = !1, i.startMoving = !1, u = a.followFinger ? n ? t.translate : -t.translate : -i.currentTranslate, !a.cssMode) if (a.freeMode) {
      if (u < -t.minTranslate()) return void t.slideTo(t.activeIndex);
      if (u > -t.maxTranslate()) return void (t.slides.length < l.length ? t.slideTo(l.length - 1) : t.slideTo(t.slides.length - 1));

      if (a.freeModeMomentum) {
        if (i.velocities.length > 1) {
          var h = i.velocities.pop(),
              f = i.velocities.pop(),
              m = h.position - f.position,
              v = h.time - f.time;
          t.velocity = m / v, t.velocity /= 2, Math.abs(t.velocity) < a.freeModeMinimumVelocity && (t.velocity = 0), (v > 150 || T() - h.time > 300) && (t.velocity = 0);
        } else t.velocity = 0;

        t.velocity *= a.freeModeMomentumVelocityRatio, i.velocities.length = 0;
        var g = 1e3 * a.freeModeMomentumRatio,
            y = t.velocity * g,
            w = t.translate + y;
        n && (w = -w);

        var b,
            _,
            E = !1,
            C = 20 * Math.abs(t.velocity) * a.freeModeMomentumBounceRatio;

        if (w < t.maxTranslate()) a.freeModeMomentumBounce ? (w + t.maxTranslate() < -C && (w = t.maxTranslate() - C), b = t.maxTranslate(), E = !0, i.allowMomentumBounce = !0) : w = t.maxTranslate(), a.loop && a.centeredSlides && (_ = !0);else if (w > t.minTranslate()) a.freeModeMomentumBounce ? (w - t.minTranslate() > C && (w = t.minTranslate() + C), b = t.minTranslate(), E = !0, i.allowMomentumBounce = !0) : w = t.minTranslate(), a.loop && a.centeredSlides && (_ = !0);else if (a.freeModeSticky) {
          for (var S, M = 0; M < l.length; M += 1) {
            if (l[M] > -w) {
              S = M;
              break;
            }
          }

          w = -(w = Math.abs(l[S] - w) < Math.abs(l[S - 1] - w) || "next" === t.swipeDirection ? l[S] : l[S - 1]);
        }

        if (_ && t.once("transitionEnd", function () {
          t.loopFix();
        }), 0 !== t.velocity) {
          if (g = n ? Math.abs((-w - t.translate) / t.velocity) : Math.abs((w - t.translate) / t.velocity), a.freeModeSticky) {
            var z = Math.abs((n ? -w : w) - t.translate),
                k = t.slidesSizesGrid[t.activeIndex];
            g = z < k ? a.speed : z < 2 * k ? 1.5 * a.speed : 2.5 * a.speed;
          }
        } else if (a.freeModeSticky) return void t.slideToClosest();

        a.freeModeMomentumBounce && E ? (t.updateProgress(b), t.setTransition(g), t.setTranslate(w), t.transitionStart(!0, t.swipeDirection), t.animating = !0, s.transitionEnd(function () {
          t && !t.destroyed && i.allowMomentumBounce && (t.emit("momentumBounce"), t.setTransition(a.speed), setTimeout(function () {
            t.setTranslate(b), s.transitionEnd(function () {
              t && !t.destroyed && t.transitionEnd();
            });
          }, 0));
        })) : t.velocity ? (t.updateProgress(w), t.setTransition(g), t.setTranslate(w), t.transitionStart(!0, t.swipeDirection), t.animating || (t.animating = !0, s.transitionEnd(function () {
          t && !t.destroyed && t.transitionEnd();
        }))) : (t.emit("_freeModeNoMomentumRelease"), t.updateProgress(w)), t.updateActiveIndex(), t.updateSlidesClasses();
      } else {
        if (a.freeModeSticky) return void t.slideToClosest();
        a.freeMode && t.emit("_freeModeNoMomentumRelease");
      }

      (!a.freeModeMomentum || c >= a.longSwipesMs) && (t.updateProgress(), t.updateActiveIndex(), t.updateSlidesClasses());
    } else {
      for (var P = 0, O = t.slidesSizesGrid[0], L = 0; L < o.length; L += L < a.slidesPerGroupSkip ? 1 : a.slidesPerGroup) {
        var A = L < a.slidesPerGroupSkip - 1 ? 1 : a.slidesPerGroup;
        void 0 !== o[L + A] ? u >= o[L] && u < o[L + A] && (P = L, O = o[L + A] - o[L]) : u >= o[L] && (P = L, O = o[o.length - 1] - o[o.length - 2]);
      }

      var $ = (u - o[P]) / O,
          D = P < a.slidesPerGroupSkip - 1 ? 1 : a.slidesPerGroup;

      if (c > a.longSwipesMs) {
        if (!a.longSwipes) return void t.slideTo(t.activeIndex);
        "next" === t.swipeDirection && ($ >= a.longSwipesRatio ? t.slideTo(P + D) : t.slideTo(P)), "prev" === t.swipeDirection && ($ > 1 - a.longSwipesRatio ? t.slideTo(P + D) : t.slideTo(P));
      } else {
        if (!a.shortSwipes) return void t.slideTo(t.activeIndex);
        !t.navigation || d.target !== t.navigation.nextEl && d.target !== t.navigation.prevEl ? ("next" === t.swipeDirection && t.slideTo(P + D), "prev" === t.swipeDirection && t.slideTo(P)) : d.target === t.navigation.nextEl ? t.slideTo(P + D) : t.slideTo(P);
      }
    }
  }

  function I() {
    var e = this,
        t = e.params,
        i = e.el;

    if (!i || 0 !== i.offsetWidth) {
      t.breakpoints && e.setBreakpoint();
      var a = e.allowSlideNext,
          r = e.allowSlidePrev,
          n = e.snapGrid;
      e.allowSlideNext = !0, e.allowSlidePrev = !0, e.updateSize(), e.updateSlides(), e.updateSlidesClasses(), ("auto" === t.slidesPerView || t.slidesPerView > 1) && e.isEnd && !e.isBeginning && !e.params.centeredSlides ? e.slideTo(e.slides.length - 1, 0, !1, !0) : e.slideTo(e.activeIndex, 0, !1, !0), e.autoplay && e.autoplay.running && e.autoplay.paused && e.autoplay.run(), e.allowSlidePrev = r, e.allowSlideNext = a, e.params.watchOverflow && n !== e.snapGrid && e.checkOverflow();
    }
  }

  function B(e) {
    var t = this;
    t.allowClick || (t.params.preventClicks && e.preventDefault(), t.params.preventClicksPropagation && t.animating && (e.stopPropagation(), e.stopImmediatePropagation()));
  }

  function N() {
    var e = this,
        t = e.wrapperEl,
        i = e.rtlTranslate;
    e.previousTranslate = e.translate, e.isHorizontal() ? e.translate = i ? t.scrollWidth - t.offsetWidth - t.scrollLeft : -t.scrollLeft : e.translate = -t.scrollTop, -0 === e.translate && (e.translate = 0), e.updateActiveIndex(), e.updateSlidesClasses();
    var a = e.maxTranslate() - e.minTranslate();
    (0 === a ? 0 : (e.translate - e.minTranslate()) / a) !== e.progress && e.updateProgress(i ? -e.translate : e.translate), e.emit("setTranslate", e.translate, !1);
  }

  var R = !1;

  function G() {}

  var F = {
    init: !0,
    direction: "horizontal",
    touchEventsTarget: "container",
    initialSlide: 0,
    speed: 300,
    cssMode: !1,
    updateOnWindowResize: !0,
    resizeObserver: !1,
    nested: !1,
    width: null,
    height: null,
    preventInteractionOnTransition: !1,
    userAgent: null,
    url: null,
    edgeSwipeDetection: !1,
    edgeSwipeThreshold: 20,
    freeMode: !1,
    freeModeMomentum: !0,
    freeModeMomentumRatio: 1,
    freeModeMomentumBounce: !0,
    freeModeMomentumBounceRatio: 1,
    freeModeMomentumVelocityRatio: 1,
    freeModeSticky: !1,
    freeModeMinimumVelocity: .02,
    autoHeight: !1,
    setWrapperSize: !1,
    virtualTranslate: !1,
    effect: "slide",
    breakpoints: void 0,
    breakpointsBase: "window",
    spaceBetween: 0,
    slidesPerView: 1,
    slidesPerColumn: 1,
    slidesPerColumnFill: "column",
    slidesPerGroup: 1,
    slidesPerGroupSkip: 0,
    centeredSlides: !1,
    centeredSlidesBounds: !1,
    slidesOffsetBefore: 0,
    slidesOffsetAfter: 0,
    normalizeSlideIndex: !0,
    centerInsufficientSlides: !1,
    watchOverflow: !1,
    roundLengths: !1,
    touchRatio: 1,
    touchAngle: 45,
    simulateTouch: !0,
    shortSwipes: !0,
    longSwipes: !0,
    longSwipesRatio: .5,
    longSwipesMs: 300,
    followFinger: !0,
    allowTouchMove: !0,
    threshold: 0,
    touchMoveStopPropagation: !1,
    touchStartPreventDefault: !0,
    touchStartForcePreventDefault: !1,
    touchReleaseOnEdges: !1,
    uniqueNavElements: !0,
    resistance: !0,
    resistanceRatio: .85,
    watchSlidesProgress: !1,
    watchSlidesVisibility: !1,
    grabCursor: !1,
    preventClicks: !0,
    preventClicksPropagation: !0,
    slideToClickedSlide: !1,
    preloadImages: !0,
    updateOnImagesReady: !0,
    loop: !1,
    loopAdditionalSlides: 0,
    loopedSlides: null,
    loopFillGroupWithBlank: !1,
    loopPreventsSlide: !0,
    allowSlidePrev: !0,
    allowSlideNext: !0,
    swipeHandler: null,
    noSwiping: !0,
    noSwipingClass: "swiper-no-swiping",
    noSwipingSelector: null,
    passiveListeners: !0,
    containerModifierClass: "swiper-container-",
    slideClass: "swiper-slide",
    slideBlankClass: "swiper-slide-invisible-blank",
    slideActiveClass: "swiper-slide-active",
    slideDuplicateActiveClass: "swiper-slide-duplicate-active",
    slideVisibleClass: "swiper-slide-visible",
    slideDuplicateClass: "swiper-slide-duplicate",
    slideNextClass: "swiper-slide-next",
    slideDuplicateNextClass: "swiper-slide-duplicate-next",
    slidePrevClass: "swiper-slide-prev",
    slideDuplicatePrevClass: "swiper-slide-duplicate-prev",
    wrapperClass: "swiper-wrapper",
    runCallbacksOnInit: !0,
    _emitClasses: !1
  },
      X = {
    modular: {
      useParams: function useParams(e) {
        var t = this;
        t.modules && Object.keys(t.modules).forEach(function (i) {
          var a = t.modules[i];
          a.params && C(e, a.params);
        });
      },
      useModules: function useModules(e) {
        void 0 === e && (e = {});
        var t = this;
        t.modules && Object.keys(t.modules).forEach(function (i) {
          var a = t.modules[i],
              r = e[i] || {};
          a.on && t.on && Object.keys(a.on).forEach(function (e) {
            t.on(e, a.on[e]);
          }), a.create && a.create.bind(t)(r);
        });
      }
    },
    eventsEmitter: {
      on: function on(e, t, i) {
        var a = this;
        if ("function" != typeof t) return a;
        var r = i ? "unshift" : "push";
        return e.split(" ").forEach(function (e) {
          a.eventsListeners[e] || (a.eventsListeners[e] = []), a.eventsListeners[e][r](t);
        }), a;
      },
      once: function once(e, t, i) {
        var a = this;
        if ("function" != typeof t) return a;

        function r() {
          a.off(e, r), r.__emitterProxy && delete r.__emitterProxy;

          for (var i = arguments.length, n = new Array(i), s = 0; s < i; s++) {
            n[s] = arguments[s];
          }

          t.apply(a, n);
        }

        return r.__emitterProxy = t, a.on(e, r, i);
      },
      onAny: function onAny(e, t) {
        var i = this;
        if ("function" != typeof e) return i;
        var a = t ? "unshift" : "push";
        return i.eventsAnyListeners.indexOf(e) < 0 && i.eventsAnyListeners[a](e), i;
      },
      offAny: function offAny(e) {
        var t = this;
        if (!t.eventsAnyListeners) return t;
        var i = t.eventsAnyListeners.indexOf(e);
        return i >= 0 && t.eventsAnyListeners.splice(i, 1), t;
      },
      off: function off(e, t) {
        var i = this;
        return i.eventsListeners ? (e.split(" ").forEach(function (e) {
          void 0 === t ? i.eventsListeners[e] = [] : i.eventsListeners[e] && i.eventsListeners[e].forEach(function (a, r) {
            (a === t || a.__emitterProxy && a.__emitterProxy === t) && i.eventsListeners[e].splice(r, 1);
          });
        }), i) : i;
      },
      emit: function emit() {
        var e,
            t,
            i,
            a = this;
        if (!a.eventsListeners) return a;

        for (var r = arguments.length, n = new Array(r), s = 0; s < r; s++) {
          n[s] = arguments[s];
        }

        "string" == typeof n[0] || Array.isArray(n[0]) ? (e = n[0], t = n.slice(1, n.length), i = a) : (e = n[0].events, t = n[0].data, i = n[0].context || a), t.unshift(i);
        var o = Array.isArray(e) ? e : e.split(" ");
        return o.forEach(function (e) {
          a.eventsAnyListeners && a.eventsAnyListeners.length && a.eventsAnyListeners.forEach(function (a) {
            a.apply(i, [e].concat(t));
          }), a.eventsListeners && a.eventsListeners[e] && a.eventsListeners[e].forEach(function (e) {
            e.apply(i, t);
          });
        }), a;
      }
    },
    update: {
      updateSize: function updateSize() {
        var e,
            t,
            i = this,
            a = i.$el;
        e = void 0 !== i.params.width && null !== i.params.width ? i.params.width : a[0].clientWidth, t = void 0 !== i.params.height && null !== i.params.height ? i.params.height : a[0].clientHeight, 0 === e && i.isHorizontal() || 0 === t && i.isVertical() || (e = e - parseInt(a.css("padding-left") || 0, 10) - parseInt(a.css("padding-right") || 0, 10), t = t - parseInt(a.css("padding-top") || 0, 10) - parseInt(a.css("padding-bottom") || 0, 10), Number.isNaN(e) && (e = 0), Number.isNaN(t) && (t = 0), C(i, {
          width: e,
          height: t,
          size: i.isHorizontal() ? e : t
        }));
      },
      updateSlides: function updateSlides() {
        var e = this,
            t = function t(_t2) {
          return e.isHorizontal() ? _t2 : {
            width: "height",
            "margin-top": "margin-left",
            "margin-bottom ": "margin-right",
            "margin-left": "margin-top",
            "margin-right": "margin-bottom",
            "padding-left": "padding-top",
            "padding-right": "padding-bottom",
            marginRight: "marginBottom"
          }[_t2];
        },
            i = function i(e, _i2) {
          return parseFloat(e.getPropertyValue(t(_i2)) || 0);
        },
            a = o(),
            r = e.params,
            n = e.$wrapperEl,
            s = e.size,
            l = e.rtlTranslate,
            d = e.wrongRTL,
            u = e.virtual && r.virtual.enabled,
            p = u ? e.virtual.slides.length : e.slides.length,
            c = n.children("." + e.params.slideClass),
            h = u ? e.virtual.slides.length : c.length,
            f = [],
            m = [],
            v = [],
            g = r.slidesOffsetBefore;

        "function" == typeof g && (g = r.slidesOffsetBefore.call(e));
        var y = r.slidesOffsetAfter;
        "function" == typeof y && (y = r.slidesOffsetAfter.call(e));
        var w = e.snapGrid.length,
            b = e.slidesGrid.length,
            x = r.spaceBetween,
            T = -g,
            _ = 0,
            E = 0;

        if (void 0 !== s) {
          var S, M;
          "string" == typeof x && x.indexOf("%") >= 0 && (x = parseFloat(x.replace("%", "")) / 100 * s), e.virtualSize = -x, l ? c.css({
            marginLeft: "",
            marginTop: ""
          }) : c.css({
            marginRight: "",
            marginBottom: ""
          }), r.slidesPerColumn > 1 && (S = Math.floor(h / r.slidesPerColumn) === h / e.params.slidesPerColumn ? h : Math.ceil(h / r.slidesPerColumn) * r.slidesPerColumn, "auto" !== r.slidesPerView && "row" === r.slidesPerColumnFill && (S = Math.max(S, r.slidesPerView * r.slidesPerColumn)));

          for (var z, k, P, O = r.slidesPerColumn, L = S / O, A = Math.floor(h / r.slidesPerColumn), $ = 0; $ < h; $ += 1) {
            M = 0;
            var D = c.eq($);

            if (r.slidesPerColumn > 1) {
              var I = void 0,
                  B = void 0,
                  N = void 0;

              if ("row" === r.slidesPerColumnFill && r.slidesPerGroup > 1) {
                var R = Math.floor($ / (r.slidesPerGroup * r.slidesPerColumn)),
                    G = $ - r.slidesPerColumn * r.slidesPerGroup * R,
                    F = 0 === R ? r.slidesPerGroup : Math.min(Math.ceil((h - R * O * r.slidesPerGroup) / O), r.slidesPerGroup);
                I = (B = G - (N = Math.floor(G / F)) * F + R * r.slidesPerGroup) + N * S / O, D.css({
                  "-webkit-box-ordinal-group": I,
                  "-moz-box-ordinal-group": I,
                  "-ms-flex-order": I,
                  "-webkit-order": I,
                  order: I
                });
              } else "column" === r.slidesPerColumnFill ? (N = $ - (B = Math.floor($ / O)) * O, (B > A || B === A && N === O - 1) && (N += 1) >= O && (N = 0, B += 1)) : B = $ - (N = Math.floor($ / L)) * L;

              D.css(t("margin-top"), 0 !== N && r.spaceBetween && r.spaceBetween + "px");
            }

            if ("none" !== D.css("display")) {
              if ("auto" === r.slidesPerView) {
                var X = a.getComputedStyle(D[0], null),
                    Y = D[0].style.transform,
                    H = D[0].style.webkitTransform;
                if (Y && (D[0].style.transform = "none"), H && (D[0].style.webkitTransform = "none"), r.roundLengths) M = e.isHorizontal() ? D.outerWidth(!0) : D.outerHeight(!0);else {
                  var V = i(X, "width"),
                      W = i(X, "padding-left"),
                      q = i(X, "padding-right"),
                      j = i(X, "margin-left"),
                      U = i(X, "margin-right"),
                      K = X.getPropertyValue(X, "box-sizing");
                  if (K && "border-box" === K) M = V + j + U;else {
                    var Q = D[0],
                        Z = Q.clientWidth;
                    M = V + W + q + j + U + (Q.offsetWidth - Z);
                  }
                }
                Y && (D[0].style.transform = Y), H && (D[0].style.webkitTransform = H), r.roundLengths && (M = Math.floor(M));
              } else M = (s - (r.slidesPerView - 1) * x) / r.slidesPerView, r.roundLengths && (M = Math.floor(M)), c[$] && (c[$].style[t("width")] = M + "px");

              c[$] && (c[$].swiperSlideSize = M), v.push(M), r.centeredSlides ? (T = T + M / 2 + _ / 2 + x, 0 === _ && 0 !== $ && (T = T - s / 2 - x), 0 === $ && (T = T - s / 2 - x), Math.abs(T) < .001 && (T = 0), r.roundLengths && (T = Math.floor(T)), E % r.slidesPerGroup == 0 && f.push(T), m.push(T)) : (r.roundLengths && (T = Math.floor(T)), (E - Math.min(e.params.slidesPerGroupSkip, E)) % e.params.slidesPerGroup == 0 && f.push(T), m.push(T), T = T + M + x), e.virtualSize += M + x, _ = M, E += 1;
            }
          }

          if (e.virtualSize = Math.max(e.virtualSize, s) + y, l && d && ("slide" === r.effect || "coverflow" === r.effect) && n.css({
            width: e.virtualSize + r.spaceBetween + "px"
          }), r.setWrapperSize && n.css(((k = {})[t("width")] = e.virtualSize + r.spaceBetween + "px", k)), r.slidesPerColumn > 1 && (e.virtualSize = (M + r.spaceBetween) * S, e.virtualSize = Math.ceil(e.virtualSize / r.slidesPerColumn) - r.spaceBetween, n.css(((P = {})[t("width")] = e.virtualSize + r.spaceBetween + "px", P)), r.centeredSlides)) {
            z = [];

            for (var J = 0; J < f.length; J += 1) {
              var ee = f[J];
              r.roundLengths && (ee = Math.floor(ee)), f[J] < e.virtualSize + f[0] && z.push(ee);
            }

            f = z;
          }

          if (!r.centeredSlides) {
            z = [];

            for (var te = 0; te < f.length; te += 1) {
              var ie = f[te];
              r.roundLengths && (ie = Math.floor(ie)), f[te] <= e.virtualSize - s && z.push(ie);
            }

            f = z, Math.floor(e.virtualSize - s) - Math.floor(f[f.length - 1]) > 1 && f.push(e.virtualSize - s);
          }

          if (0 === f.length && (f = [0]), 0 !== r.spaceBetween) {
            var ae,
                re = e.isHorizontal() && l ? "marginLeft" : t("marginRight");
            c.filter(function (e, t) {
              return !r.cssMode || t !== c.length - 1;
            }).css(((ae = {})[re] = x + "px", ae));
          }

          if (r.centeredSlides && r.centeredSlidesBounds) {
            var ne = 0;
            v.forEach(function (e) {
              ne += e + (r.spaceBetween ? r.spaceBetween : 0);
            });
            var se = (ne -= r.spaceBetween) - s;
            f = f.map(function (e) {
              return e < 0 ? -g : e > se ? se + y : e;
            });
          }

          if (r.centerInsufficientSlides) {
            var oe = 0;

            if (v.forEach(function (e) {
              oe += e + (r.spaceBetween ? r.spaceBetween : 0);
            }), (oe -= r.spaceBetween) < s) {
              var le = (s - oe) / 2;
              f.forEach(function (e, t) {
                f[t] = e - le;
              }), m.forEach(function (e, t) {
                m[t] = e + le;
              });
            }
          }

          C(e, {
            slides: c,
            snapGrid: f,
            slidesGrid: m,
            slidesSizesGrid: v
          }), h !== p && e.emit("slidesLengthChange"), f.length !== w && (e.params.watchOverflow && e.checkOverflow(), e.emit("snapGridLengthChange")), m.length !== b && e.emit("slidesGridLengthChange"), (r.watchSlidesProgress || r.watchSlidesVisibility) && e.updateSlidesOffset();
        }
      },
      updateAutoHeight: function updateAutoHeight(e) {
        var t,
            i = this,
            a = [],
            r = 0;
        if ("number" == typeof e ? i.setTransition(e) : !0 === e && i.setTransition(i.params.speed), "auto" !== i.params.slidesPerView && i.params.slidesPerView > 1) {
          if (i.params.centeredSlides) i.visibleSlides.each(function (e) {
            a.push(e);
          });else for (t = 0; t < Math.ceil(i.params.slidesPerView); t += 1) {
            var n = i.activeIndex + t;
            if (n > i.slides.length) break;
            a.push(i.slides.eq(n)[0]);
          }
        } else a.push(i.slides.eq(i.activeIndex)[0]);

        for (t = 0; t < a.length; t += 1) {
          if (void 0 !== a[t]) {
            var s = a[t].offsetHeight;
            r = s > r ? s : r;
          }
        }

        r && i.$wrapperEl.css("height", r + "px");
      },
      updateSlidesOffset: function updateSlidesOffset() {
        for (var e = this.slides, t = 0; t < e.length; t += 1) {
          e[t].swiperSlideOffset = this.isHorizontal() ? e[t].offsetLeft : e[t].offsetTop;
        }
      },
      updateSlidesProgress: function updateSlidesProgress(e) {
        void 0 === e && (e = this && this.translate || 0);
        var t = this,
            i = t.params,
            a = t.slides,
            r = t.rtlTranslate;

        if (0 !== a.length) {
          void 0 === a[0].swiperSlideOffset && t.updateSlidesOffset();
          var n = -e;
          r && (n = e), a.removeClass(i.slideVisibleClass), t.visibleSlidesIndexes = [], t.visibleSlides = [];

          for (var s = 0; s < a.length; s += 1) {
            var o = a[s],
                l = (n + (i.centeredSlides ? t.minTranslate() : 0) - o.swiperSlideOffset) / (o.swiperSlideSize + i.spaceBetween);

            if (i.watchSlidesVisibility || i.centeredSlides && i.autoHeight) {
              var d = -(n - o.swiperSlideOffset),
                  u = d + t.slidesSizesGrid[s];
              (d >= 0 && d < t.size - 1 || u > 1 && u <= t.size || d <= 0 && u >= t.size) && (t.visibleSlides.push(o), t.visibleSlidesIndexes.push(s), a.eq(s).addClass(i.slideVisibleClass));
            }

            o.progress = r ? -l : l;
          }

          t.visibleSlides = v(t.visibleSlides);
        }
      },
      updateProgress: function updateProgress(e) {
        var t = this;

        if (void 0 === e) {
          var i = t.rtlTranslate ? -1 : 1;
          e = t && t.translate && t.translate * i || 0;
        }

        var a = t.params,
            r = t.maxTranslate() - t.minTranslate(),
            n = t.progress,
            s = t.isBeginning,
            o = t.isEnd,
            l = s,
            d = o;
        0 === r ? (n = 0, s = !0, o = !0) : (s = (n = (e - t.minTranslate()) / r) <= 0, o = n >= 1), C(t, {
          progress: n,
          isBeginning: s,
          isEnd: o
        }), (a.watchSlidesProgress || a.watchSlidesVisibility || a.centeredSlides && a.autoHeight) && t.updateSlidesProgress(e), s && !l && t.emit("reachBeginning toEdge"), o && !d && t.emit("reachEnd toEdge"), (l && !s || d && !o) && t.emit("fromEdge"), t.emit("progress", n);
      },
      updateSlidesClasses: function updateSlidesClasses() {
        var e,
            t = this,
            i = t.slides,
            a = t.params,
            r = t.$wrapperEl,
            n = t.activeIndex,
            s = t.realIndex,
            o = t.virtual && a.virtual.enabled;
        i.removeClass(a.slideActiveClass + " " + a.slideNextClass + " " + a.slidePrevClass + " " + a.slideDuplicateActiveClass + " " + a.slideDuplicateNextClass + " " + a.slideDuplicatePrevClass), (e = o ? t.$wrapperEl.find("." + a.slideClass + '[data-swiper-slide-index="' + n + '"]') : i.eq(n)).addClass(a.slideActiveClass), a.loop && (e.hasClass(a.slideDuplicateClass) ? r.children("." + a.slideClass + ":not(." + a.slideDuplicateClass + ')[data-swiper-slide-index="' + s + '"]').addClass(a.slideDuplicateActiveClass) : r.children("." + a.slideClass + "." + a.slideDuplicateClass + '[data-swiper-slide-index="' + s + '"]').addClass(a.slideDuplicateActiveClass));
        var l = e.nextAll("." + a.slideClass).eq(0).addClass(a.slideNextClass);
        a.loop && 0 === l.length && (l = i.eq(0)).addClass(a.slideNextClass);
        var d = e.prevAll("." + a.slideClass).eq(0).addClass(a.slidePrevClass);
        a.loop && 0 === d.length && (d = i.eq(-1)).addClass(a.slidePrevClass), a.loop && (l.hasClass(a.slideDuplicateClass) ? r.children("." + a.slideClass + ":not(." + a.slideDuplicateClass + ')[data-swiper-slide-index="' + l.attr("data-swiper-slide-index") + '"]').addClass(a.slideDuplicateNextClass) : r.children("." + a.slideClass + "." + a.slideDuplicateClass + '[data-swiper-slide-index="' + l.attr("data-swiper-slide-index") + '"]').addClass(a.slideDuplicateNextClass), d.hasClass(a.slideDuplicateClass) ? r.children("." + a.slideClass + ":not(." + a.slideDuplicateClass + ')[data-swiper-slide-index="' + d.attr("data-swiper-slide-index") + '"]').addClass(a.slideDuplicatePrevClass) : r.children("." + a.slideClass + "." + a.slideDuplicateClass + '[data-swiper-slide-index="' + d.attr("data-swiper-slide-index") + '"]').addClass(a.slideDuplicatePrevClass)), t.emitSlidesClasses();
      },
      updateActiveIndex: function updateActiveIndex(e) {
        var t,
            i = this,
            a = i.rtlTranslate ? i.translate : -i.translate,
            r = i.slidesGrid,
            n = i.snapGrid,
            s = i.params,
            o = i.activeIndex,
            l = i.realIndex,
            d = i.snapIndex,
            u = e;

        if (void 0 === u) {
          for (var p = 0; p < r.length; p += 1) {
            void 0 !== r[p + 1] ? a >= r[p] && a < r[p + 1] - (r[p + 1] - r[p]) / 2 ? u = p : a >= r[p] && a < r[p + 1] && (u = p + 1) : a >= r[p] && (u = p);
          }

          s.normalizeSlideIndex && (u < 0 || void 0 === u) && (u = 0);
        }

        if (n.indexOf(a) >= 0) t = n.indexOf(a);else {
          var c = Math.min(s.slidesPerGroupSkip, u);
          t = c + Math.floor((u - c) / s.slidesPerGroup);
        }

        if (t >= n.length && (t = n.length - 1), u !== o) {
          var h = parseInt(i.slides.eq(u).attr("data-swiper-slide-index") || u, 10);
          C(i, {
            snapIndex: t,
            realIndex: h,
            previousIndex: o,
            activeIndex: u
          }), i.emit("activeIndexChange"), i.emit("snapIndexChange"), l !== h && i.emit("realIndexChange"), (i.initialized || i.params.runCallbacksOnInit) && i.emit("slideChange");
        } else t !== d && (i.snapIndex = t, i.emit("snapIndexChange"));
      },
      updateClickedSlide: function updateClickedSlide(e) {
        var t,
            i = this,
            a = i.params,
            r = v(e.target).closest("." + a.slideClass)[0],
            n = !1;
        if (r) for (var s = 0; s < i.slides.length; s += 1) {
          if (i.slides[s] === r) {
            n = !0, t = s;
            break;
          }
        }
        if (!r || !n) return i.clickedSlide = void 0, void (i.clickedIndex = void 0);
        i.clickedSlide = r, i.virtual && i.params.virtual.enabled ? i.clickedIndex = parseInt(v(r).attr("data-swiper-slide-index"), 10) : i.clickedIndex = t, a.slideToClickedSlide && void 0 !== i.clickedIndex && i.clickedIndex !== i.activeIndex && i.slideToClickedSlide();
      }
    },
    translate: {
      getTranslate: function getTranslate(e) {
        void 0 === e && (e = this.isHorizontal() ? "x" : "y");
        var t = this,
            i = t.params,
            a = t.rtlTranslate,
            r = t.translate,
            n = t.$wrapperEl;
        if (i.virtualTranslate) return a ? -r : r;
        if (i.cssMode) return r;

        var s = _(n[0], e);

        return a && (s = -s), s || 0;
      },
      setTranslate: function setTranslate(e, t) {
        var i = this,
            a = i.rtlTranslate,
            r = i.params,
            n = i.$wrapperEl,
            s = i.wrapperEl,
            o = i.progress,
            l = 0,
            d = 0;
        i.isHorizontal() ? l = a ? -e : e : d = e, r.roundLengths && (l = Math.floor(l), d = Math.floor(d)), r.cssMode ? s[i.isHorizontal() ? "scrollLeft" : "scrollTop"] = i.isHorizontal() ? -l : -d : r.virtualTranslate || n.transform("translate3d(" + l + "px, " + d + "px, 0px)"), i.previousTranslate = i.translate, i.translate = i.isHorizontal() ? l : d;
        var u = i.maxTranslate() - i.minTranslate();
        (0 === u ? 0 : (e - i.minTranslate()) / u) !== o && i.updateProgress(e), i.emit("setTranslate", i.translate, t);
      },
      minTranslate: function minTranslate() {
        return -this.snapGrid[0];
      },
      maxTranslate: function maxTranslate() {
        return -this.snapGrid[this.snapGrid.length - 1];
      },
      translateTo: function translateTo(e, t, i, a, r) {
        void 0 === e && (e = 0), void 0 === t && (t = this.params.speed), void 0 === i && (i = !0), void 0 === a && (a = !0);
        var n = this,
            s = n.params,
            o = n.wrapperEl;
        if (n.animating && s.preventInteractionOnTransition) return !1;
        var l,
            d = n.minTranslate(),
            u = n.maxTranslate();

        if (l = a && e > d ? d : a && e < u ? u : e, n.updateProgress(l), s.cssMode) {
          var p,
              c = n.isHorizontal();
          return 0 === t ? o[c ? "scrollLeft" : "scrollTop"] = -l : o.scrollTo ? o.scrollTo(((p = {})[c ? "left" : "top"] = -l, p.behavior = "smooth", p)) : o[c ? "scrollLeft" : "scrollTop"] = -l, !0;
        }

        return 0 === t ? (n.setTransition(0), n.setTranslate(l), i && (n.emit("beforeTransitionStart", t, r), n.emit("transitionEnd"))) : (n.setTransition(t), n.setTranslate(l), i && (n.emit("beforeTransitionStart", t, r), n.emit("transitionStart")), n.animating || (n.animating = !0, n.onTranslateToWrapperTransitionEnd || (n.onTranslateToWrapperTransitionEnd = function (e) {
          n && !n.destroyed && e.target === this && (n.$wrapperEl[0].removeEventListener("transitionend", n.onTranslateToWrapperTransitionEnd), n.$wrapperEl[0].removeEventListener("webkitTransitionEnd", n.onTranslateToWrapperTransitionEnd), n.onTranslateToWrapperTransitionEnd = null, delete n.onTranslateToWrapperTransitionEnd, i && n.emit("transitionEnd"));
        }), n.$wrapperEl[0].addEventListener("transitionend", n.onTranslateToWrapperTransitionEnd), n.$wrapperEl[0].addEventListener("webkitTransitionEnd", n.onTranslateToWrapperTransitionEnd))), !0;
      }
    },
    transition: {
      setTransition: function setTransition(e, t) {
        var i = this;
        i.params.cssMode || i.$wrapperEl.transition(e), i.emit("setTransition", e, t);
      },
      transitionStart: function transitionStart(e, t) {
        void 0 === e && (e = !0);
        var i = this,
            a = i.activeIndex,
            r = i.params,
            n = i.previousIndex;

        if (!r.cssMode) {
          r.autoHeight && i.updateAutoHeight();
          var s = t;

          if (s || (s = a > n ? "next" : a < n ? "prev" : "reset"), i.emit("transitionStart"), e && a !== n) {
            if ("reset" === s) return void i.emit("slideResetTransitionStart");
            i.emit("slideChangeTransitionStart"), "next" === s ? i.emit("slideNextTransitionStart") : i.emit("slidePrevTransitionStart");
          }
        }
      },
      transitionEnd: function transitionEnd(e, t) {
        void 0 === e && (e = !0);
        var i = this,
            a = i.activeIndex,
            r = i.previousIndex,
            n = i.params;

        if (i.animating = !1, !n.cssMode) {
          i.setTransition(0);
          var s = t;

          if (s || (s = a > r ? "next" : a < r ? "prev" : "reset"), i.emit("transitionEnd"), e && a !== r) {
            if ("reset" === s) return void i.emit("slideResetTransitionEnd");
            i.emit("slideChangeTransitionEnd"), "next" === s ? i.emit("slideNextTransitionEnd") : i.emit("slidePrevTransitionEnd");
          }
        }
      }
    },
    slide: {
      slideTo: function slideTo(e, t, i, a) {
        if (void 0 === e && (e = 0), void 0 === t && (t = this.params.speed), void 0 === i && (i = !0), "number" != typeof e && "string" != typeof e) throw new Error("The 'index' argument cannot have type other than 'number' or 'string'. [" + _typeof(e) + "] given.");

        if ("string" == typeof e) {
          var r = parseInt(e, 10);
          if (!isFinite(r)) throw new Error("The passed-in 'index' (string) couldn't be converted to 'number'. [" + e + "] given.");
          e = r;
        }

        var n = this,
            s = e;
        s < 0 && (s = 0);
        var o = n.params,
            l = n.snapGrid,
            d = n.slidesGrid,
            u = n.previousIndex,
            p = n.activeIndex,
            c = n.rtlTranslate,
            h = n.wrapperEl;
        if (n.animating && o.preventInteractionOnTransition) return !1;
        var f = Math.min(n.params.slidesPerGroupSkip, s),
            m = f + Math.floor((s - f) / n.params.slidesPerGroup);
        m >= l.length && (m = l.length - 1), (p || o.initialSlide || 0) === (u || 0) && i && n.emit("beforeSlideChangeStart");
        var v,
            g = -l[m];
        if (n.updateProgress(g), o.normalizeSlideIndex) for (var y = 0; y < d.length; y += 1) {
          var w = -Math.floor(100 * g),
              b = Math.floor(100 * d[y]),
              x = Math.floor(100 * d[y + 1]);
          void 0 !== d[y + 1] ? w >= b && w < x - (x - b) / 2 ? s = y : w >= b && w < x && (s = y + 1) : w >= b && (s = y);
        }

        if (n.initialized && s !== p) {
          if (!n.allowSlideNext && g < n.translate && g < n.minTranslate()) return !1;
          if (!n.allowSlidePrev && g > n.translate && g > n.maxTranslate() && (p || 0) !== s) return !1;
        }

        if (v = s > p ? "next" : s < p ? "prev" : "reset", c && -g === n.translate || !c && g === n.translate) return n.updateActiveIndex(s), o.autoHeight && n.updateAutoHeight(), n.updateSlidesClasses(), "slide" !== o.effect && n.setTranslate(g), "reset" !== v && (n.transitionStart(i, v), n.transitionEnd(i, v)), !1;

        if (o.cssMode) {
          var T,
              _ = n.isHorizontal(),
              E = -g;

          return c && (E = h.scrollWidth - h.offsetWidth - E), 0 === t ? h[_ ? "scrollLeft" : "scrollTop"] = E : h.scrollTo ? h.scrollTo(((T = {})[_ ? "left" : "top"] = E, T.behavior = "smooth", T)) : h[_ ? "scrollLeft" : "scrollTop"] = E, !0;
        }

        return 0 === t ? (n.setTransition(0), n.setTranslate(g), n.updateActiveIndex(s), n.updateSlidesClasses(), n.emit("beforeTransitionStart", t, a), n.transitionStart(i, v), n.transitionEnd(i, v)) : (n.setTransition(t), n.setTranslate(g), n.updateActiveIndex(s), n.updateSlidesClasses(), n.emit("beforeTransitionStart", t, a), n.transitionStart(i, v), n.animating || (n.animating = !0, n.onSlideToWrapperTransitionEnd || (n.onSlideToWrapperTransitionEnd = function (e) {
          n && !n.destroyed && e.target === this && (n.$wrapperEl[0].removeEventListener("transitionend", n.onSlideToWrapperTransitionEnd), n.$wrapperEl[0].removeEventListener("webkitTransitionEnd", n.onSlideToWrapperTransitionEnd), n.onSlideToWrapperTransitionEnd = null, delete n.onSlideToWrapperTransitionEnd, n.transitionEnd(i, v));
        }), n.$wrapperEl[0].addEventListener("transitionend", n.onSlideToWrapperTransitionEnd), n.$wrapperEl[0].addEventListener("webkitTransitionEnd", n.onSlideToWrapperTransitionEnd))), !0;
      },
      slideToLoop: function slideToLoop(e, t, i, a) {
        void 0 === e && (e = 0), void 0 === t && (t = this.params.speed), void 0 === i && (i = !0);
        var r = this,
            n = e;
        return r.params.loop && (n += r.loopedSlides), r.slideTo(n, t, i, a);
      },
      slideNext: function slideNext(e, t, i) {
        void 0 === e && (e = this.params.speed), void 0 === t && (t = !0);
        var a = this,
            r = a.params,
            n = a.animating,
            s = a.activeIndex < r.slidesPerGroupSkip ? 1 : r.slidesPerGroup;

        if (r.loop) {
          if (n && r.loopPreventsSlide) return !1;
          a.loopFix(), a._clientLeft = a.$wrapperEl[0].clientLeft;
        }

        return a.slideTo(a.activeIndex + s, e, t, i);
      },
      slidePrev: function slidePrev(e, t, i) {
        void 0 === e && (e = this.params.speed), void 0 === t && (t = !0);
        var a = this,
            r = a.params,
            n = a.animating,
            s = a.snapGrid,
            o = a.slidesGrid,
            l = a.rtlTranslate;

        if (r.loop) {
          if (n && r.loopPreventsSlide) return !1;
          a.loopFix(), a._clientLeft = a.$wrapperEl[0].clientLeft;
        }

        function d(e) {
          return e < 0 ? -Math.floor(Math.abs(e)) : Math.floor(e);
        }

        var u = d(l ? a.translate : -a.translate),
            p = s.map(function (e) {
          return d(e);
        });
        s[p.indexOf(u)];
        var c,
            h = s[p.indexOf(u) - 1];
        return void 0 === h && r.cssMode && s.forEach(function (e) {
          !h && u >= e && (h = e);
        }), void 0 !== h && (c = o.indexOf(h)) < 0 && (c = a.activeIndex - 1), a.slideTo(c, e, t, i);
      },
      slideReset: function slideReset(e, t, i) {
        return void 0 === e && (e = this.params.speed), void 0 === t && (t = !0), this.slideTo(this.activeIndex, e, t, i);
      },
      slideToClosest: function slideToClosest(e, t, i, a) {
        void 0 === e && (e = this.params.speed), void 0 === t && (t = !0), void 0 === a && (a = .5);
        var r = this,
            n = r.activeIndex,
            s = Math.min(r.params.slidesPerGroupSkip, n),
            o = s + Math.floor((n - s) / r.params.slidesPerGroup),
            l = r.rtlTranslate ? r.translate : -r.translate;

        if (l >= r.snapGrid[o]) {
          var d = r.snapGrid[o];
          l - d > (r.snapGrid[o + 1] - d) * a && (n += r.params.slidesPerGroup);
        } else {
          var u = r.snapGrid[o - 1];
          l - u <= (r.snapGrid[o] - u) * a && (n -= r.params.slidesPerGroup);
        }

        return n = Math.max(n, 0), n = Math.min(n, r.slidesGrid.length - 1), r.slideTo(n, e, t, i);
      },
      slideToClickedSlide: function slideToClickedSlide() {
        var e,
            t = this,
            i = t.params,
            a = t.$wrapperEl,
            r = "auto" === i.slidesPerView ? t.slidesPerViewDynamic() : i.slidesPerView,
            n = t.clickedIndex;

        if (i.loop) {
          if (t.animating) return;
          e = parseInt(v(t.clickedSlide).attr("data-swiper-slide-index"), 10), i.centeredSlides ? n < t.loopedSlides - r / 2 || n > t.slides.length - t.loopedSlides + r / 2 ? (t.loopFix(), n = a.children("." + i.slideClass + '[data-swiper-slide-index="' + e + '"]:not(.' + i.slideDuplicateClass + ")").eq(0).index(), x(function () {
            t.slideTo(n);
          })) : t.slideTo(n) : n > t.slides.length - r ? (t.loopFix(), n = a.children("." + i.slideClass + '[data-swiper-slide-index="' + e + '"]:not(.' + i.slideDuplicateClass + ")").eq(0).index(), x(function () {
            t.slideTo(n);
          })) : t.slideTo(n);
        } else t.slideTo(n);
      }
    },
    loop: {
      loopCreate: function loopCreate() {
        var e = this,
            t = n(),
            i = e.params,
            a = e.$wrapperEl;
        a.children("." + i.slideClass + "." + i.slideDuplicateClass).remove();
        var r = a.children("." + i.slideClass);

        if (i.loopFillGroupWithBlank) {
          var s = i.slidesPerGroup - r.length % i.slidesPerGroup;

          if (s !== i.slidesPerGroup) {
            for (var o = 0; o < s; o += 1) {
              var l = v(t.createElement("div")).addClass(i.slideClass + " " + i.slideBlankClass);
              a.append(l);
            }

            r = a.children("." + i.slideClass);
          }
        }

        "auto" !== i.slidesPerView || i.loopedSlides || (i.loopedSlides = r.length), e.loopedSlides = Math.ceil(parseFloat(i.loopedSlides || i.slidesPerView, 10)), e.loopedSlides += i.loopAdditionalSlides, e.loopedSlides > r.length && (e.loopedSlides = r.length);
        var d = [],
            u = [];
        r.each(function (t, i) {
          var a = v(t);
          i < e.loopedSlides && u.push(t), i < r.length && i >= r.length - e.loopedSlides && d.push(t), a.attr("data-swiper-slide-index", i);
        });

        for (var p = 0; p < u.length; p += 1) {
          a.append(v(u[p].cloneNode(!0)).addClass(i.slideDuplicateClass));
        }

        for (var c = d.length - 1; c >= 0; c -= 1) {
          a.prepend(v(d[c].cloneNode(!0)).addClass(i.slideDuplicateClass));
        }
      },
      loopFix: function loopFix() {
        var e = this;
        e.emit("beforeLoopFix");
        var t,
            i = e.activeIndex,
            a = e.slides,
            r = e.loopedSlides,
            n = e.allowSlidePrev,
            s = e.allowSlideNext,
            o = e.snapGrid,
            l = e.rtlTranslate;
        e.allowSlidePrev = !0, e.allowSlideNext = !0;
        var d = -o[i] - e.getTranslate();
        i < r ? (t = a.length - 3 * r + i, t += r, e.slideTo(t, 0, !1, !0) && 0 !== d && e.setTranslate((l ? -e.translate : e.translate) - d)) : i >= a.length - r && (t = -a.length + i + r, t += r, e.slideTo(t, 0, !1, !0) && 0 !== d && e.setTranslate((l ? -e.translate : e.translate) - d)), e.allowSlidePrev = n, e.allowSlideNext = s, e.emit("loopFix");
      },
      loopDestroy: function loopDestroy() {
        var e = this,
            t = e.$wrapperEl,
            i = e.params,
            a = e.slides;
        t.children("." + i.slideClass + "." + i.slideDuplicateClass + ",." + i.slideClass + "." + i.slideBlankClass).remove(), a.removeAttr("data-swiper-slide-index");
      }
    },
    grabCursor: {
      setGrabCursor: function setGrabCursor(e) {
        var t = this;

        if (!(t.support.touch || !t.params.simulateTouch || t.params.watchOverflow && t.isLocked || t.params.cssMode)) {
          var i = t.el;
          i.style.cursor = "move", i.style.cursor = e ? "-webkit-grabbing" : "-webkit-grab", i.style.cursor = e ? "-moz-grabbin" : "-moz-grab", i.style.cursor = e ? "grabbing" : "grab";
        }
      },
      unsetGrabCursor: function unsetGrabCursor() {
        var e = this;
        e.support.touch || e.params.watchOverflow && e.isLocked || e.params.cssMode || (e.el.style.cursor = "");
      }
    },
    manipulation: {
      appendSlide: function appendSlide(e) {
        var t = this,
            i = t.$wrapperEl,
            a = t.params;
        if (a.loop && t.loopDestroy(), "object" == _typeof(e) && "length" in e) for (var r = 0; r < e.length; r += 1) {
          e[r] && i.append(e[r]);
        } else i.append(e);
        a.loop && t.loopCreate(), a.observer && t.support.observer || t.update();
      },
      prependSlide: function prependSlide(e) {
        var t = this,
            i = t.params,
            a = t.$wrapperEl,
            r = t.activeIndex;
        i.loop && t.loopDestroy();
        var n = r + 1;

        if ("object" == _typeof(e) && "length" in e) {
          for (var s = 0; s < e.length; s += 1) {
            e[s] && a.prepend(e[s]);
          }

          n = r + e.length;
        } else a.prepend(e);

        i.loop && t.loopCreate(), i.observer && t.support.observer || t.update(), t.slideTo(n, 0, !1);
      },
      addSlide: function addSlide(e, t) {
        var i = this,
            a = i.$wrapperEl,
            r = i.params,
            n = i.activeIndex;
        r.loop && (n -= i.loopedSlides, i.loopDestroy(), i.slides = a.children("." + r.slideClass));
        var s = i.slides.length;
        if (e <= 0) i.prependSlide(t);else if (e >= s) i.appendSlide(t);else {
          for (var o = n > e ? n + 1 : n, l = [], d = s - 1; d >= e; d -= 1) {
            var u = i.slides.eq(d);
            u.remove(), l.unshift(u);
          }

          if ("object" == _typeof(t) && "length" in t) {
            for (var p = 0; p < t.length; p += 1) {
              t[p] && a.append(t[p]);
            }

            o = n > e ? n + t.length : n;
          } else a.append(t);

          for (var c = 0; c < l.length; c += 1) {
            a.append(l[c]);
          }

          r.loop && i.loopCreate(), r.observer && i.support.observer || i.update(), r.loop ? i.slideTo(o + i.loopedSlides, 0, !1) : i.slideTo(o, 0, !1);
        }
      },
      removeSlide: function removeSlide(e) {
        var t = this,
            i = t.params,
            a = t.$wrapperEl,
            r = t.activeIndex;
        i.loop && (r -= t.loopedSlides, t.loopDestroy(), t.slides = a.children("." + i.slideClass));
        var n,
            s = r;

        if ("object" == _typeof(e) && "length" in e) {
          for (var o = 0; o < e.length; o += 1) {
            n = e[o], t.slides[n] && t.slides.eq(n).remove(), n < s && (s -= 1);
          }

          s = Math.max(s, 0);
        } else n = e, t.slides[n] && t.slides.eq(n).remove(), n < s && (s -= 1), s = Math.max(s, 0);

        i.loop && t.loopCreate(), i.observer && t.support.observer || t.update(), i.loop ? t.slideTo(s + t.loopedSlides, 0, !1) : t.slideTo(s, 0, !1);
      },
      removeAllSlides: function removeAllSlides() {
        for (var e = [], t = 0; t < this.slides.length; t += 1) {
          e.push(t);
        }

        this.removeSlide(e);
      }
    },
    events: {
      attachEvents: function attachEvents() {
        var e = this,
            t = n(),
            i = e.params,
            a = e.touchEvents,
            r = e.el,
            s = e.wrapperEl,
            o = e.device,
            l = e.support;
        e.onTouchStart = A.bind(e), e.onTouchMove = $.bind(e), e.onTouchEnd = D.bind(e), i.cssMode && (e.onScroll = N.bind(e)), e.onClick = B.bind(e);
        var d = !!i.nested;
        if (!l.touch && l.pointerEvents) r.addEventListener(a.start, e.onTouchStart, !1), t.addEventListener(a.move, e.onTouchMove, d), t.addEventListener(a.end, e.onTouchEnd, !1);else {
          if (l.touch) {
            var u = !("touchstart" !== a.start || !l.passiveListener || !i.passiveListeners) && {
              passive: !0,
              capture: !1
            };
            r.addEventListener(a.start, e.onTouchStart, u), r.addEventListener(a.move, e.onTouchMove, l.passiveListener ? {
              passive: !1,
              capture: d
            } : d), r.addEventListener(a.end, e.onTouchEnd, u), a.cancel && r.addEventListener(a.cancel, e.onTouchEnd, u), R || (t.addEventListener("touchstart", G), R = !0);
          }

          (i.simulateTouch && !o.ios && !o.android || i.simulateTouch && !l.touch && o.ios) && (r.addEventListener("mousedown", e.onTouchStart, !1), t.addEventListener("mousemove", e.onTouchMove, d), t.addEventListener("mouseup", e.onTouchEnd, !1));
        }
        (i.preventClicks || i.preventClicksPropagation) && r.addEventListener("click", e.onClick, !0), i.cssMode && s.addEventListener("scroll", e.onScroll), i.updateOnWindowResize ? e.on(o.ios || o.android ? "resize orientationchange observerUpdate" : "resize observerUpdate", I, !0) : e.on("observerUpdate", I, !0);
      },
      detachEvents: function detachEvents() {
        var e = this,
            t = n(),
            i = e.params,
            a = e.touchEvents,
            r = e.el,
            s = e.wrapperEl,
            o = e.device,
            l = e.support,
            d = !!i.nested;
        if (!l.touch && l.pointerEvents) r.removeEventListener(a.start, e.onTouchStart, !1), t.removeEventListener(a.move, e.onTouchMove, d), t.removeEventListener(a.end, e.onTouchEnd, !1);else {
          if (l.touch) {
            var u = !("onTouchStart" !== a.start || !l.passiveListener || !i.passiveListeners) && {
              passive: !0,
              capture: !1
            };
            r.removeEventListener(a.start, e.onTouchStart, u), r.removeEventListener(a.move, e.onTouchMove, d), r.removeEventListener(a.end, e.onTouchEnd, u), a.cancel && r.removeEventListener(a.cancel, e.onTouchEnd, u);
          }

          (i.simulateTouch && !o.ios && !o.android || i.simulateTouch && !l.touch && o.ios) && (r.removeEventListener("mousedown", e.onTouchStart, !1), t.removeEventListener("mousemove", e.onTouchMove, d), t.removeEventListener("mouseup", e.onTouchEnd, !1));
        }
        (i.preventClicks || i.preventClicksPropagation) && r.removeEventListener("click", e.onClick, !0), i.cssMode && s.removeEventListener("scroll", e.onScroll), e.off(o.ios || o.android ? "resize orientationchange observerUpdate" : "resize observerUpdate", I);
      }
    },
    breakpoints: {
      setBreakpoint: function setBreakpoint() {
        var e = this,
            t = e.activeIndex,
            i = e.initialized,
            a = e.loopedSlides,
            r = void 0 === a ? 0 : a,
            n = e.params,
            s = e.$el,
            o = n.breakpoints;

        if (o && (!o || 0 !== Object.keys(o).length)) {
          var l = e.getBreakpoint(o, e.params.breakpointsBase, e.el);

          if (l && e.currentBreakpoint !== l) {
            var d = l in o ? o[l] : void 0;
            d && ["slidesPerView", "spaceBetween", "slidesPerGroup", "slidesPerGroupSkip", "slidesPerColumn"].forEach(function (e) {
              var t = d[e];
              void 0 !== t && (d[e] = "slidesPerView" !== e || "AUTO" !== t && "auto" !== t ? "slidesPerView" === e ? parseFloat(t) : parseInt(t, 10) : "auto");
            });
            var u = d || e.originalParams,
                p = n.slidesPerColumn > 1,
                c = u.slidesPerColumn > 1;
            p && !c ? (s.removeClass(n.containerModifierClass + "multirow " + n.containerModifierClass + "multirow-column"), e.emitContainerClasses()) : !p && c && (s.addClass(n.containerModifierClass + "multirow"), "column" === u.slidesPerColumnFill && s.addClass(n.containerModifierClass + "multirow-column"), e.emitContainerClasses());
            var h = u.direction && u.direction !== n.direction,
                f = n.loop && (u.slidesPerView !== n.slidesPerView || h);
            h && i && e.changeDirection(), C(e.params, u), C(e, {
              allowTouchMove: e.params.allowTouchMove,
              allowSlideNext: e.params.allowSlideNext,
              allowSlidePrev: e.params.allowSlidePrev
            }), e.currentBreakpoint = l, e.emit("_beforeBreakpoint", u), f && i && (e.loopDestroy(), e.loopCreate(), e.updateSlides(), e.slideTo(t - r + e.loopedSlides, 0, !1)), e.emit("breakpoint", u);
          }
        }
      },
      getBreakpoint: function getBreakpoint(e, t, i) {
        if (void 0 === t && (t = "window"), e && ("container" !== t || i)) {
          var a = !1,
              r = o(),
              n = "window" === t ? r.innerWidth : i.clientWidth,
              s = "window" === t ? r.innerHeight : i.clientHeight,
              l = Object.keys(e).map(function (e) {
            if ("string" == typeof e && 0 === e.indexOf("@")) {
              var t = parseFloat(e.substr(1));
              return {
                value: s * t,
                point: e
              };
            }

            return {
              value: e,
              point: e
            };
          });
          l.sort(function (e, t) {
            return parseInt(e.value, 10) - parseInt(t.value, 10);
          });

          for (var d = 0; d < l.length; d += 1) {
            var u = l[d],
                p = u.point;
            u.value <= n && (a = p);
          }

          return a || "max";
        }
      }
    },
    checkOverflow: {
      checkOverflow: function checkOverflow() {
        var e = this,
            t = e.params,
            i = e.isLocked,
            a = e.slides.length > 0 && t.slidesOffsetBefore + t.spaceBetween * (e.slides.length - 1) + e.slides[0].offsetWidth * e.slides.length;
        t.slidesOffsetBefore && t.slidesOffsetAfter && a ? e.isLocked = a <= e.size : e.isLocked = 1 === e.snapGrid.length, e.allowSlideNext = !e.isLocked, e.allowSlidePrev = !e.isLocked, i !== e.isLocked && e.emit(e.isLocked ? "lock" : "unlock"), i && i !== e.isLocked && (e.isEnd = !1, e.navigation && e.navigation.update());
      }
    },
    classes: {
      addClasses: function addClasses() {
        var e,
            t,
            i,
            a = this,
            r = a.classNames,
            n = a.params,
            s = a.rtl,
            o = a.$el,
            l = a.device,
            d = a.support,
            u = (e = ["initialized", n.direction, {
          "pointer-events": d.pointerEvents && !d.touch
        }, {
          "free-mode": n.freeMode
        }, {
          autoheight: n.autoHeight
        }, {
          rtl: s
        }, {
          multirow: n.slidesPerColumn > 1
        }, {
          "multirow-column": n.slidesPerColumn > 1 && "column" === n.slidesPerColumnFill
        }, {
          android: l.android
        }, {
          ios: l.ios
        }, {
          "css-mode": n.cssMode
        }], t = n.containerModifierClass, i = [], e.forEach(function (e) {
          "object" == _typeof(e) ? Object.entries(e).forEach(function (e) {
            var a = e[0];
            e[1] && i.push(t + a);
          }) : "string" == typeof e && i.push(t + e);
        }), i);
        r.push.apply(r, u), o.addClass([].concat(r).join(" ")), a.emitContainerClasses();
      },
      removeClasses: function removeClasses() {
        var e = this,
            t = e.$el,
            i = e.classNames;
        t.removeClass(i.join(" ")), e.emitContainerClasses();
      }
    },
    images: {
      loadImage: function loadImage(e, t, i, a, r, n) {
        var s,
            l = o();

        function d() {
          n && n();
        }

        v(e).parent("picture")[0] || e.complete && r ? d() : t ? ((s = new l.Image()).onload = d, s.onerror = d, a && (s.sizes = a), i && (s.srcset = i), t && (s.src = t)) : d();
      },
      preloadImages: function preloadImages() {
        var e = this;

        function t() {
          null != e && e && !e.destroyed && (void 0 !== e.imagesLoaded && (e.imagesLoaded += 1), e.imagesLoaded === e.imagesToLoad.length && (e.params.updateOnImagesReady && e.update(), e.emit("imagesReady")));
        }

        e.imagesToLoad = e.$el.find("img");

        for (var i = 0; i < e.imagesToLoad.length; i += 1) {
          var a = e.imagesToLoad[i];
          e.loadImage(a, a.currentSrc || a.getAttribute("src"), a.srcset || a.getAttribute("srcset"), a.sizes || a.getAttribute("sizes"), !0, t);
        }
      }
    }
  },
      Y = {},
      H = function () {
    function t() {
      for (var e, i, a = arguments.length, r = new Array(a), n = 0; n < a; n++) {
        r[n] = arguments[n];
      }

      if (1 === r.length && r[0].constructor && r[0].constructor === Object ? i = r[0] : (e = r[0], i = r[1]), i || (i = {}), i = C({}, i), e && !i.el && (i.el = e), i.el && v(i.el).length > 1) {
        var s = [];
        return v(i.el).each(function (e) {
          var a = C({}, i, {
            el: e
          });
          s.push(new t(a));
        }), s;
      }

      var o = this;
      o.support = M(), o.device = z({
        userAgent: i.userAgent
      }), o.browser = k(), o.eventsListeners = {}, o.eventsAnyListeners = [], void 0 === o.modules && (o.modules = {}), Object.keys(o.modules).forEach(function (e) {
        var t = o.modules[e];

        if (t.params) {
          var a = Object.keys(t.params)[0],
              r = t.params[a];
          if ("object" != _typeof(r) || null === r) return;
          if (!(a in i) || !("enabled" in r)) return;
          !0 === i[a] && (i[a] = {
            enabled: !0
          }), "object" != _typeof(i[a]) || "enabled" in i[a] || (i[a].enabled = !0), i[a] || (i[a] = {
            enabled: !1
          });
        }
      });
      var l,
          d,
          u = C({}, F);
      return o.useParams(u), o.params = C({}, u, Y, i), o.originalParams = C({}, o.params), o.passedParams = C({}, i), o.params && o.params.on && Object.keys(o.params.on).forEach(function (e) {
        o.on(e, o.params.on[e]);
      }), o.params && o.params.onAny && o.onAny(o.params.onAny), o.$ = v, C(o, {
        el: e,
        classNames: [],
        slides: v(),
        slidesGrid: [],
        snapGrid: [],
        slidesSizesGrid: [],
        isHorizontal: function isHorizontal() {
          return "horizontal" === o.params.direction;
        },
        isVertical: function isVertical() {
          return "vertical" === o.params.direction;
        },
        activeIndex: 0,
        realIndex: 0,
        isBeginning: !0,
        isEnd: !1,
        translate: 0,
        previousTranslate: 0,
        progress: 0,
        velocity: 0,
        animating: !1,
        allowSlideNext: o.params.allowSlideNext,
        allowSlidePrev: o.params.allowSlidePrev,
        touchEvents: (l = ["touchstart", "touchmove", "touchend", "touchcancel"], d = ["mousedown", "mousemove", "mouseup"], o.support.pointerEvents && (d = ["pointerdown", "pointermove", "pointerup"]), o.touchEventsTouch = {
          start: l[0],
          move: l[1],
          end: l[2],
          cancel: l[3]
        }, o.touchEventsDesktop = {
          start: d[0],
          move: d[1],
          end: d[2]
        }, o.support.touch || !o.params.simulateTouch ? o.touchEventsTouch : o.touchEventsDesktop),
        touchEventsData: {
          isTouched: void 0,
          isMoved: void 0,
          allowTouchCallbacks: void 0,
          touchStartTime: void 0,
          isScrolling: void 0,
          currentTranslate: void 0,
          startTranslate: void 0,
          allowThresholdMove: void 0,
          formElements: "input, select, option, textarea, button, video, label",
          lastClickTime: T(),
          clickTimeout: void 0,
          velocities: [],
          allowMomentumBounce: void 0,
          isTouchEvent: void 0,
          startMoving: void 0
        },
        allowClick: !0,
        allowTouchMove: o.params.allowTouchMove,
        touches: {
          startX: 0,
          startY: 0,
          currentX: 0,
          currentY: 0,
          diff: 0
        },
        imagesToLoad: [],
        imagesLoaded: 0
      }), o.useModules(), o.emit("_swiper"), o.params.init && o.init(), o;
    }

    var i,
        a,
        r = t.prototype;
    return r.emitContainerClasses = function () {
      var e = this;

      if (e.params._emitClasses && e.el) {
        var t = e.el.className.split(" ").filter(function (t) {
          return 0 === t.indexOf("swiper-container") || 0 === t.indexOf(e.params.containerModifierClass);
        });
        e.emit("_containerClasses", t.join(" "));
      }
    }, r.getSlideClasses = function (e) {
      var t = this;
      return e.className.split(" ").filter(function (e) {
        return 0 === e.indexOf("swiper-slide") || 0 === e.indexOf(t.params.slideClass);
      }).join(" ");
    }, r.emitSlidesClasses = function () {
      var e = this;

      if (e.params._emitClasses && e.el) {
        var t = [];
        e.slides.each(function (i) {
          var a = e.getSlideClasses(i);
          t.push({
            slideEl: i,
            classNames: a
          }), e.emit("_slideClass", i, a);
        }), e.emit("_slideClasses", t);
      }
    }, r.slidesPerViewDynamic = function () {
      var e = this,
          t = e.params,
          i = e.slides,
          a = e.slidesGrid,
          r = e.size,
          n = e.activeIndex,
          s = 1;

      if (t.centeredSlides) {
        for (var o, l = i[n].swiperSlideSize, d = n + 1; d < i.length; d += 1) {
          i[d] && !o && (s += 1, (l += i[d].swiperSlideSize) > r && (o = !0));
        }

        for (var u = n - 1; u >= 0; u -= 1) {
          i[u] && !o && (s += 1, (l += i[u].swiperSlideSize) > r && (o = !0));
        }
      } else for (var p = n + 1; p < i.length; p += 1) {
        a[p] - a[n] < r && (s += 1);
      }

      return s;
    }, r.update = function () {
      var e = this;

      if (e && !e.destroyed) {
        var t = e.snapGrid,
            i = e.params;
        i.breakpoints && e.setBreakpoint(), e.updateSize(), e.updateSlides(), e.updateProgress(), e.updateSlidesClasses(), e.params.freeMode ? (a(), e.params.autoHeight && e.updateAutoHeight()) : (("auto" === e.params.slidesPerView || e.params.slidesPerView > 1) && e.isEnd && !e.params.centeredSlides ? e.slideTo(e.slides.length - 1, 0, !1, !0) : e.slideTo(e.activeIndex, 0, !1, !0)) || a(), i.watchOverflow && t !== e.snapGrid && e.checkOverflow(), e.emit("update");
      }

      function a() {
        var t = e.rtlTranslate ? -1 * e.translate : e.translate,
            i = Math.min(Math.max(t, e.maxTranslate()), e.minTranslate());
        e.setTranslate(i), e.updateActiveIndex(), e.updateSlidesClasses();
      }
    }, r.changeDirection = function (e, t) {
      void 0 === t && (t = !0);
      var i = this,
          a = i.params.direction;
      return e || (e = "horizontal" === a ? "vertical" : "horizontal"), e === a || "horizontal" !== e && "vertical" !== e || (i.$el.removeClass("" + i.params.containerModifierClass + a).addClass("" + i.params.containerModifierClass + e), i.emitContainerClasses(), i.params.direction = e, i.slides.each(function (t) {
        "vertical" === e ? t.style.width = "" : t.style.height = "";
      }), i.emit("changeDirection"), t && i.update()), i;
    }, r.mount = function (e) {
      var t = this;
      if (t.mounted) return !0;
      var i,
          a = v(e || t.params.el);
      return !!(e = a[0]) && (e.swiper = t, e && e.shadowRoot && e.shadowRoot.querySelector ? (i = v(e.shadowRoot.querySelector("." + t.params.wrapperClass))).children = function (e) {
        return a.children(e);
      } : i = a.children("." + t.params.wrapperClass), C(t, {
        $el: a,
        el: e,
        $wrapperEl: i,
        wrapperEl: i[0],
        mounted: !0,
        rtl: "rtl" === e.dir.toLowerCase() || "rtl" === a.css("direction"),
        rtlTranslate: "horizontal" === t.params.direction && ("rtl" === e.dir.toLowerCase() || "rtl" === a.css("direction")),
        wrongRTL: "-webkit-box" === i.css("display")
      }), !0);
    }, r.init = function (e) {
      var t = this;
      return t.initialized || !1 === t.mount(e) || (t.emit("beforeInit"), t.params.breakpoints && t.setBreakpoint(), t.addClasses(), t.params.loop && t.loopCreate(), t.updateSize(), t.updateSlides(), t.params.watchOverflow && t.checkOverflow(), t.params.grabCursor && t.setGrabCursor(), t.params.preloadImages && t.preloadImages(), t.params.loop ? t.slideTo(t.params.initialSlide + t.loopedSlides, 0, t.params.runCallbacksOnInit) : t.slideTo(t.params.initialSlide, 0, t.params.runCallbacksOnInit), t.attachEvents(), t.initialized = !0, t.emit("init"), t.emit("afterInit")), t;
    }, r.destroy = function (e, t) {
      void 0 === e && (e = !0), void 0 === t && (t = !0);
      var i,
          a = this,
          r = a.params,
          n = a.$el,
          s = a.$wrapperEl,
          o = a.slides;
      return void 0 === a.params || a.destroyed || (a.emit("beforeDestroy"), a.initialized = !1, a.detachEvents(), r.loop && a.loopDestroy(), t && (a.removeClasses(), n.removeAttr("style"), s.removeAttr("style"), o && o.length && o.removeClass([r.slideVisibleClass, r.slideActiveClass, r.slideNextClass, r.slidePrevClass].join(" ")).removeAttr("style").removeAttr("data-swiper-slide-index")), a.emit("destroy"), Object.keys(a.eventsListeners).forEach(function (e) {
        a.off(e);
      }), !1 !== e && (a.$el[0].swiper = null, i = a, Object.keys(i).forEach(function (e) {
        try {
          i[e] = null;
        } catch (e) {}

        try {
          delete i[e];
        } catch (e) {}
      })), a.destroyed = !0), null;
    }, t.extendDefaults = function (e) {
      C(Y, e);
    }, t.installModule = function (e) {
      t.prototype.modules || (t.prototype.modules = {});
      var i = e.name || Object.keys(t.prototype.modules).length + "_" + T();
      t.prototype.modules[i] = e;
    }, t.use = function (e) {
      return Array.isArray(e) ? (e.forEach(function (e) {
        return t.installModule(e);
      }), t) : (t.installModule(e), t);
    }, i = t, a = [{
      key: "extendedDefaults",
      get: function get() {
        return Y;
      }
    }, {
      key: "defaults",
      get: function get() {
        return F;
      }
    }], null && e(i.prototype, null), a && e(i, a), t;
  }();

  Object.keys(X).forEach(function (e) {
    Object.keys(X[e]).forEach(function (t) {
      H.prototype[t] = X[e][t];
    });
  }), H.use([P, L]);
  var V = {
    update: function update(e) {
      var t = this,
          i = t.params,
          a = i.slidesPerView,
          r = i.slidesPerGroup,
          n = i.centeredSlides,
          s = t.params.virtual,
          o = s.addSlidesBefore,
          l = s.addSlidesAfter,
          d = t.virtual,
          u = d.from,
          p = d.to,
          c = d.slides,
          h = d.slidesGrid,
          f = d.renderSlide,
          m = d.offset;
      t.updateActiveIndex();
      var v,
          g,
          y,
          w = t.activeIndex || 0;
      v = t.rtlTranslate ? "right" : t.isHorizontal() ? "left" : "top", n ? (g = Math.floor(a / 2) + r + l, y = Math.floor(a / 2) + r + o) : (g = a + (r - 1) + l, y = r + o);
      var b = Math.max((w || 0) - y, 0),
          x = Math.min((w || 0) + g, c.length - 1),
          T = (t.slidesGrid[b] || 0) - (t.slidesGrid[0] || 0);

      function _() {
        t.updateSlides(), t.updateProgress(), t.updateSlidesClasses(), t.lazy && t.params.lazy.enabled && t.lazy.load();
      }

      if (C(t.virtual, {
        from: b,
        to: x,
        offset: T,
        slidesGrid: t.slidesGrid
      }), u === b && p === x && !e) return t.slidesGrid !== h && T !== m && t.slides.css(v, T + "px"), void t.updateProgress();
      if (t.params.virtual.renderExternal) return t.params.virtual.renderExternal.call(t, {
        offset: T,
        from: b,
        to: x,
        slides: function () {
          for (var e = [], t = b; t <= x; t += 1) {
            e.push(c[t]);
          }

          return e;
        }()
      }), void (t.params.virtual.renderExternalUpdate && _());
      var E = [],
          S = [];
      if (e) t.$wrapperEl.find("." + t.params.slideClass).remove();else for (var M = u; M <= p; M += 1) {
        (M < b || M > x) && t.$wrapperEl.find("." + t.params.slideClass + '[data-swiper-slide-index="' + M + '"]').remove();
      }

      for (var z = 0; z < c.length; z += 1) {
        z >= b && z <= x && (void 0 === p || e ? S.push(z) : (z > p && S.push(z), z < u && E.push(z)));
      }

      S.forEach(function (e) {
        t.$wrapperEl.append(f(c[e], e));
      }), E.sort(function (e, t) {
        return t - e;
      }).forEach(function (e) {
        t.$wrapperEl.prepend(f(c[e], e));
      }), t.$wrapperEl.children(".swiper-slide").css(v, T + "px"), _();
    },
    renderSlide: function renderSlide(e, t) {
      var i = this,
          a = i.params.virtual;
      if (a.cache && i.virtual.cache[t]) return i.virtual.cache[t];
      var r = a.renderSlide ? v(a.renderSlide.call(i, e, t)) : v('<div class="' + i.params.slideClass + '" data-swiper-slide-index="' + t + '">' + e + "</div>");
      return r.attr("data-swiper-slide-index") || r.attr("data-swiper-slide-index", t), a.cache && (i.virtual.cache[t] = r), r;
    },
    appendSlide: function appendSlide(e) {
      var t = this;
      if ("object" == _typeof(e) && "length" in e) for (var i = 0; i < e.length; i += 1) {
        e[i] && t.virtual.slides.push(e[i]);
      } else t.virtual.slides.push(e);
      t.virtual.update(!0);
    },
    prependSlide: function prependSlide(e) {
      var t = this,
          i = t.activeIndex,
          a = i + 1,
          r = 1;

      if (Array.isArray(e)) {
        for (var n = 0; n < e.length; n += 1) {
          e[n] && t.virtual.slides.unshift(e[n]);
        }

        a = i + e.length, r = e.length;
      } else t.virtual.slides.unshift(e);

      if (t.params.virtual.cache) {
        var s = t.virtual.cache,
            o = {};
        Object.keys(s).forEach(function (e) {
          var t = s[e],
              i = t.attr("data-swiper-slide-index");
          i && t.attr("data-swiper-slide-index", parseInt(i, 10) + 1), o[parseInt(e, 10) + r] = t;
        }), t.virtual.cache = o;
      }

      t.virtual.update(!0), t.slideTo(a, 0);
    },
    removeSlide: function removeSlide(e) {
      var t = this;

      if (null != e) {
        var i = t.activeIndex;
        if (Array.isArray(e)) for (var a = e.length - 1; a >= 0; a -= 1) {
          t.virtual.slides.splice(e[a], 1), t.params.virtual.cache && delete t.virtual.cache[e[a]], e[a] < i && (i -= 1), i = Math.max(i, 0);
        } else t.virtual.slides.splice(e, 1), t.params.virtual.cache && delete t.virtual.cache[e], e < i && (i -= 1), i = Math.max(i, 0);
        t.virtual.update(!0), t.slideTo(i, 0);
      }
    },
    removeAllSlides: function removeAllSlides() {
      var e = this;
      e.virtual.slides = [], e.params.virtual.cache && (e.virtual.cache = {}), e.virtual.update(!0), e.slideTo(0, 0);
    }
  },
      W = {
    name: "virtual",
    params: {
      virtual: {
        enabled: !1,
        slides: [],
        cache: !0,
        renderSlide: null,
        renderExternal: null,
        renderExternalUpdate: !0,
        addSlidesBefore: 0,
        addSlidesAfter: 0
      }
    },
    create: function create() {
      S(this, {
        virtual: t({}, V, {
          slides: this.params.virtual.slides,
          cache: {}
        })
      });
    },
    on: {
      beforeInit: function beforeInit(e) {
        if (e.params.virtual.enabled) {
          e.classNames.push(e.params.containerModifierClass + "virtual");
          var t = {
            watchSlidesProgress: !0
          };
          C(e.params, t), C(e.originalParams, t), e.params.initialSlide || e.virtual.update();
        }
      },
      setTranslate: function setTranslate(e) {
        e.params.virtual.enabled && e.virtual.update();
      }
    }
  },
      q = {
    handle: function handle(e) {
      var t = this,
          i = o(),
          a = n(),
          r = t.rtlTranslate,
          s = e;
      s.originalEvent && (s = s.originalEvent);
      var l = s.keyCode || s.charCode,
          d = t.params.keyboard.pageUpDown,
          u = d && 33 === l,
          p = d && 34 === l,
          c = 37 === l,
          h = 39 === l,
          f = 38 === l,
          m = 40 === l;
      if (!t.allowSlideNext && (t.isHorizontal() && h || t.isVertical() && m || p)) return !1;
      if (!t.allowSlidePrev && (t.isHorizontal() && c || t.isVertical() && f || u)) return !1;

      if (!(s.shiftKey || s.altKey || s.ctrlKey || s.metaKey || a.activeElement && a.activeElement.nodeName && ("input" === a.activeElement.nodeName.toLowerCase() || "textarea" === a.activeElement.nodeName.toLowerCase()))) {
        if (t.params.keyboard.onlyInViewport && (u || p || c || h || f || m)) {
          var v = !1;
          if (t.$el.parents("." + t.params.slideClass).length > 0 && 0 === t.$el.parents("." + t.params.slideActiveClass).length) return;
          var g = i.innerWidth,
              y = i.innerHeight,
              w = t.$el.offset();
          r && (w.left -= t.$el[0].scrollLeft);

          for (var b = [[w.left, w.top], [w.left + t.width, w.top], [w.left, w.top + t.height], [w.left + t.width, w.top + t.height]], x = 0; x < b.length; x += 1) {
            var T = b[x];

            if (T[0] >= 0 && T[0] <= g && T[1] >= 0 && T[1] <= y) {
              if (0 === T[0] && 0 === T[1]) continue;
              v = !0;
            }
          }

          if (!v) return;
        }

        t.isHorizontal() ? ((u || p || c || h) && (s.preventDefault ? s.preventDefault() : s.returnValue = !1), ((p || h) && !r || (u || c) && r) && t.slideNext(), ((u || c) && !r || (p || h) && r) && t.slidePrev()) : ((u || p || f || m) && (s.preventDefault ? s.preventDefault() : s.returnValue = !1), (p || m) && t.slideNext(), (u || f) && t.slidePrev()), t.emit("keyPress", l);
      }
    },
    enable: function enable() {
      var e = this,
          t = n();
      e.keyboard.enabled || (v(t).on("keydown", e.keyboard.handle), e.keyboard.enabled = !0);
    },
    disable: function disable() {
      var e = this,
          t = n();
      e.keyboard.enabled && (v(t).off("keydown", e.keyboard.handle), e.keyboard.enabled = !1);
    }
  },
      j = {
    name: "keyboard",
    params: {
      keyboard: {
        enabled: !1,
        onlyInViewport: !0,
        pageUpDown: !0
      }
    },
    create: function create() {
      S(this, {
        keyboard: t({
          enabled: !1
        }, q)
      });
    },
    on: {
      init: function init(e) {
        e.params.keyboard.enabled && e.keyboard.enable();
      },
      destroy: function destroy(e) {
        e.keyboard.enabled && e.keyboard.disable();
      }
    }
  },
      U = {
    lastScrollTime: T(),
    lastEventBeforeSnap: void 0,
    recentWheelEvents: [],
    event: function event() {
      return o().navigator.userAgent.indexOf("firefox") > -1 ? "DOMMouseScroll" : function () {
        var e = n(),
            t = "onwheel",
            i = (t in e);

        if (!i) {
          var a = e.createElement("div");
          a.setAttribute(t, "return;"), i = "function" == typeof a.onwheel;
        }

        return !i && e.implementation && e.implementation.hasFeature && !0 !== e.implementation.hasFeature("", "") && (i = e.implementation.hasFeature("Events.wheel", "3.0")), i;
      }() ? "wheel" : "mousewheel";
    },
    normalize: function normalize(e) {
      var t = 0,
          i = 0,
          a = 0,
          r = 0;
      return "detail" in e && (i = e.detail), "wheelDelta" in e && (i = -e.wheelDelta / 120), "wheelDeltaY" in e && (i = -e.wheelDeltaY / 120), "wheelDeltaX" in e && (t = -e.wheelDeltaX / 120), "axis" in e && e.axis === e.HORIZONTAL_AXIS && (t = i, i = 0), a = 10 * t, r = 10 * i, "deltaY" in e && (r = e.deltaY), "deltaX" in e && (a = e.deltaX), e.shiftKey && !a && (a = r, r = 0), (a || r) && e.deltaMode && (1 === e.deltaMode ? (a *= 40, r *= 40) : (a *= 800, r *= 800)), a && !t && (t = a < 1 ? -1 : 1), r && !i && (i = r < 1 ? -1 : 1), {
        spinX: t,
        spinY: i,
        pixelX: a,
        pixelY: r
      };
    },
    handleMouseEnter: function handleMouseEnter() {
      this.mouseEntered = !0;
    },
    handleMouseLeave: function handleMouseLeave() {
      this.mouseEntered = !1;
    },
    handle: function handle(e) {
      var t = e,
          i = this,
          a = i.params.mousewheel;
      i.params.cssMode && t.preventDefault();
      var r = i.$el;
      if ("container" !== i.params.mousewheel.eventsTarget && (r = v(i.params.mousewheel.eventsTarget)), !i.mouseEntered && !r[0].contains(t.target) && !a.releaseOnEdges) return !0;
      t.originalEvent && (t = t.originalEvent);
      var n = 0,
          s = i.rtlTranslate ? -1 : 1,
          o = U.normalize(t);
      if (a.forceToAxis) {
        if (i.isHorizontal()) {
          if (!(Math.abs(o.pixelX) > Math.abs(o.pixelY))) return !0;
          n = -o.pixelX * s;
        } else {
          if (!(Math.abs(o.pixelY) > Math.abs(o.pixelX))) return !0;
          n = -o.pixelY;
        }
      } else n = Math.abs(o.pixelX) > Math.abs(o.pixelY) ? -o.pixelX * s : -o.pixelY;
      if (0 === n) return !0;
      a.invert && (n = -n);
      var l = i.getTranslate() + n * a.sensitivity;

      if (l >= i.minTranslate() && (l = i.minTranslate()), l <= i.maxTranslate() && (l = i.maxTranslate()), (!!i.params.loop || !(l === i.minTranslate() || l === i.maxTranslate())) && i.params.nested && t.stopPropagation(), i.params.freeMode) {
        var d = {
          time: T(),
          delta: Math.abs(n),
          direction: Math.sign(n)
        },
            u = i.mousewheel.lastEventBeforeSnap,
            p = u && d.time < u.time + 500 && d.delta <= u.delta && d.direction === u.direction;

        if (!p) {
          i.mousewheel.lastEventBeforeSnap = void 0, i.params.loop && i.loopFix();
          var c = i.getTranslate() + n * a.sensitivity,
              h = i.isBeginning,
              f = i.isEnd;

          if (c >= i.minTranslate() && (c = i.minTranslate()), c <= i.maxTranslate() && (c = i.maxTranslate()), i.setTransition(0), i.setTranslate(c), i.updateProgress(), i.updateActiveIndex(), i.updateSlidesClasses(), (!h && i.isBeginning || !f && i.isEnd) && i.updateSlidesClasses(), i.params.freeModeSticky) {
            clearTimeout(i.mousewheel.timeout), i.mousewheel.timeout = void 0;
            var m = i.mousewheel.recentWheelEvents;
            m.length >= 15 && m.shift();
            var g = m.length ? m[m.length - 1] : void 0,
                y = m[0];
            if (m.push(d), g && (d.delta > g.delta || d.direction !== g.direction)) m.splice(0);else if (m.length >= 15 && d.time - y.time < 500 && y.delta - d.delta >= 1 && d.delta <= 6) {
              var w = n > 0 ? .8 : .2;
              i.mousewheel.lastEventBeforeSnap = d, m.splice(0), i.mousewheel.timeout = x(function () {
                i.slideToClosest(i.params.speed, !0, void 0, w);
              }, 0);
            }
            i.mousewheel.timeout || (i.mousewheel.timeout = x(function () {
              i.mousewheel.lastEventBeforeSnap = d, m.splice(0), i.slideToClosest(i.params.speed, !0, void 0, .5);
            }, 500));
          }

          if (p || i.emit("scroll", t), i.params.autoplay && i.params.autoplayDisableOnInteraction && i.autoplay.stop(), c === i.minTranslate() || c === i.maxTranslate()) return !0;
        }
      } else {
        var b = {
          time: T(),
          delta: Math.abs(n),
          direction: Math.sign(n),
          raw: e
        },
            _ = i.mousewheel.recentWheelEvents;
        _.length >= 2 && _.shift();
        var E = _.length ? _[_.length - 1] : void 0;
        if (_.push(b), E ? (b.direction !== E.direction || b.delta > E.delta || b.time > E.time + 150) && i.mousewheel.animateSlider(b) : i.mousewheel.animateSlider(b), i.mousewheel.releaseScroll(b)) return !0;
      }

      return t.preventDefault ? t.preventDefault() : t.returnValue = !1, !1;
    },
    animateSlider: function animateSlider(e) {
      var t = this,
          i = o();
      return !(this.params.mousewheel.thresholdDelta && e.delta < this.params.mousewheel.thresholdDelta || this.params.mousewheel.thresholdTime && T() - t.mousewheel.lastScrollTime < this.params.mousewheel.thresholdTime || !(e.delta >= 6 && T() - t.mousewheel.lastScrollTime < 60) && (e.direction < 0 ? t.isEnd && !t.params.loop || t.animating || (t.slideNext(), t.emit("scroll", e.raw)) : t.isBeginning && !t.params.loop || t.animating || (t.slidePrev(), t.emit("scroll", e.raw)), t.mousewheel.lastScrollTime = new i.Date().getTime(), 1));
    },
    releaseScroll: function releaseScroll(e) {
      var t = this,
          i = t.params.mousewheel;

      if (e.direction < 0) {
        if (t.isEnd && !t.params.loop && i.releaseOnEdges) return !0;
      } else if (t.isBeginning && !t.params.loop && i.releaseOnEdges) return !0;

      return !1;
    },
    enable: function enable() {
      var e = this,
          t = U.event();
      if (e.params.cssMode) return e.wrapperEl.removeEventListener(t, e.mousewheel.handle), !0;
      if (!t) return !1;
      if (e.mousewheel.enabled) return !1;
      var i = e.$el;
      return "container" !== e.params.mousewheel.eventsTarget && (i = v(e.params.mousewheel.eventsTarget)), i.on("mouseenter", e.mousewheel.handleMouseEnter), i.on("mouseleave", e.mousewheel.handleMouseLeave), i.on(t, e.mousewheel.handle), e.mousewheel.enabled = !0, !0;
    },
    disable: function disable() {
      var e = this,
          t = U.event();
      if (e.params.cssMode) return e.wrapperEl.addEventListener(t, e.mousewheel.handle), !0;
      if (!t) return !1;
      if (!e.mousewheel.enabled) return !1;
      var i = e.$el;
      return "container" !== e.params.mousewheel.eventsTarget && (i = v(e.params.mousewheel.eventsTarget)), i.off(t, e.mousewheel.handle), e.mousewheel.enabled = !1, !0;
    }
  },
      K = {
    update: function update() {
      var e = this,
          t = e.params.navigation;

      if (!e.params.loop) {
        var i = e.navigation,
            a = i.$nextEl,
            r = i.$prevEl;
        r && r.length > 0 && (e.isBeginning ? r.addClass(t.disabledClass) : r.removeClass(t.disabledClass), r[e.params.watchOverflow && e.isLocked ? "addClass" : "removeClass"](t.lockClass)), a && a.length > 0 && (e.isEnd ? a.addClass(t.disabledClass) : a.removeClass(t.disabledClass), a[e.params.watchOverflow && e.isLocked ? "addClass" : "removeClass"](t.lockClass));
      }
    },
    onPrevClick: function onPrevClick(e) {
      var t = this;
      e.preventDefault(), t.isBeginning && !t.params.loop || t.slidePrev();
    },
    onNextClick: function onNextClick(e) {
      var t = this;
      e.preventDefault(), t.isEnd && !t.params.loop || t.slideNext();
    },
    init: function init() {
      var e,
          t,
          i = this,
          a = i.params.navigation;
      (a.nextEl || a.prevEl) && (a.nextEl && (e = v(a.nextEl), i.params.uniqueNavElements && "string" == typeof a.nextEl && e.length > 1 && 1 === i.$el.find(a.nextEl).length && (e = i.$el.find(a.nextEl))), a.prevEl && (t = v(a.prevEl), i.params.uniqueNavElements && "string" == typeof a.prevEl && t.length > 1 && 1 === i.$el.find(a.prevEl).length && (t = i.$el.find(a.prevEl))), e && e.length > 0 && e.on("click", i.navigation.onNextClick), t && t.length > 0 && t.on("click", i.navigation.onPrevClick), C(i.navigation, {
        $nextEl: e,
        nextEl: e && e[0],
        $prevEl: t,
        prevEl: t && t[0]
      }));
    },
    destroy: function destroy() {
      var e = this,
          t = e.navigation,
          i = t.$nextEl,
          a = t.$prevEl;
      i && i.length && (i.off("click", e.navigation.onNextClick), i.removeClass(e.params.navigation.disabledClass)), a && a.length && (a.off("click", e.navigation.onPrevClick), a.removeClass(e.params.navigation.disabledClass));
    }
  },
      Q = {
    update: function update() {
      var e = this,
          t = e.rtl,
          i = e.params.pagination;

      if (i.el && e.pagination.el && e.pagination.$el && 0 !== e.pagination.$el.length) {
        var a,
            r = e.virtual && e.params.virtual.enabled ? e.virtual.slides.length : e.slides.length,
            n = e.pagination.$el,
            s = e.params.loop ? Math.ceil((r - 2 * e.loopedSlides) / e.params.slidesPerGroup) : e.snapGrid.length;

        if (e.params.loop ? ((a = Math.ceil((e.activeIndex - e.loopedSlides) / e.params.slidesPerGroup)) > r - 1 - 2 * e.loopedSlides && (a -= r - 2 * e.loopedSlides), a > s - 1 && (a -= s), a < 0 && "bullets" !== e.params.paginationType && (a = s + a)) : a = void 0 !== e.snapIndex ? e.snapIndex : e.activeIndex || 0, "bullets" === i.type && e.pagination.bullets && e.pagination.bullets.length > 0) {
          var o,
              l,
              d,
              u = e.pagination.bullets;
          if (i.dynamicBullets && (e.pagination.bulletSize = u.eq(0)[e.isHorizontal() ? "outerWidth" : "outerHeight"](!0), n.css(e.isHorizontal() ? "width" : "height", e.pagination.bulletSize * (i.dynamicMainBullets + 4) + "px"), i.dynamicMainBullets > 1 && void 0 !== e.previousIndex && (e.pagination.dynamicBulletIndex += a - e.previousIndex, e.pagination.dynamicBulletIndex > i.dynamicMainBullets - 1 ? e.pagination.dynamicBulletIndex = i.dynamicMainBullets - 1 : e.pagination.dynamicBulletIndex < 0 && (e.pagination.dynamicBulletIndex = 0)), o = a - e.pagination.dynamicBulletIndex, d = ((l = o + (Math.min(u.length, i.dynamicMainBullets) - 1)) + o) / 2), u.removeClass(i.bulletActiveClass + " " + i.bulletActiveClass + "-next " + i.bulletActiveClass + "-next-next " + i.bulletActiveClass + "-prev " + i.bulletActiveClass + "-prev-prev " + i.bulletActiveClass + "-main"), n.length > 1) u.each(function (e) {
            var t = v(e),
                r = t.index();
            r === a && t.addClass(i.bulletActiveClass), i.dynamicBullets && (r >= o && r <= l && t.addClass(i.bulletActiveClass + "-main"), r === o && t.prev().addClass(i.bulletActiveClass + "-prev").prev().addClass(i.bulletActiveClass + "-prev-prev"), r === l && t.next().addClass(i.bulletActiveClass + "-next").next().addClass(i.bulletActiveClass + "-next-next"));
          });else {
            var p = u.eq(a),
                c = p.index();

            if (p.addClass(i.bulletActiveClass), i.dynamicBullets) {
              for (var h = u.eq(o), f = u.eq(l), m = o; m <= l; m += 1) {
                u.eq(m).addClass(i.bulletActiveClass + "-main");
              }

              if (e.params.loop) {
                if (c >= u.length - i.dynamicMainBullets) {
                  for (var g = i.dynamicMainBullets; g >= 0; g -= 1) {
                    u.eq(u.length - g).addClass(i.bulletActiveClass + "-main");
                  }

                  u.eq(u.length - i.dynamicMainBullets - 1).addClass(i.bulletActiveClass + "-prev");
                } else h.prev().addClass(i.bulletActiveClass + "-prev").prev().addClass(i.bulletActiveClass + "-prev-prev"), f.next().addClass(i.bulletActiveClass + "-next").next().addClass(i.bulletActiveClass + "-next-next");
              } else h.prev().addClass(i.bulletActiveClass + "-prev").prev().addClass(i.bulletActiveClass + "-prev-prev"), f.next().addClass(i.bulletActiveClass + "-next").next().addClass(i.bulletActiveClass + "-next-next");
            }
          }

          if (i.dynamicBullets) {
            var y = Math.min(u.length, i.dynamicMainBullets + 4),
                w = (e.pagination.bulletSize * y - e.pagination.bulletSize) / 2 - d * e.pagination.bulletSize,
                b = t ? "right" : "left";
            u.css(e.isHorizontal() ? b : "top", w + "px");
          }
        }

        if ("fraction" === i.type && (n.find("." + i.currentClass).text(i.formatFractionCurrent(a + 1)), n.find("." + i.totalClass).text(i.formatFractionTotal(s))), "progressbar" === i.type) {
          var x;
          x = i.progressbarOpposite ? e.isHorizontal() ? "vertical" : "horizontal" : e.isHorizontal() ? "horizontal" : "vertical";
          var T = (a + 1) / s,
              _ = 1,
              E = 1;
          "horizontal" === x ? _ = T : E = T, n.find("." + i.progressbarFillClass).transform("translate3d(0,0,0) scaleX(" + _ + ") scaleY(" + E + ")").transition(e.params.speed);
        }

        "custom" === i.type && i.renderCustom ? (n.html(i.renderCustom(e, a + 1, s)), e.emit("paginationRender", n[0])) : e.emit("paginationUpdate", n[0]), n[e.params.watchOverflow && e.isLocked ? "addClass" : "removeClass"](i.lockClass);
      }
    },
    render: function render() {
      var e = this,
          t = e.params.pagination;

      if (t.el && e.pagination.el && e.pagination.$el && 0 !== e.pagination.$el.length) {
        var i = e.virtual && e.params.virtual.enabled ? e.virtual.slides.length : e.slides.length,
            a = e.pagination.$el,
            r = "";

        if ("bullets" === t.type) {
          var n = e.params.loop ? Math.ceil((i - 2 * e.loopedSlides) / e.params.slidesPerGroup) : e.snapGrid.length;
          e.params.freeMode && !e.params.loop && n > i && (n = i);

          for (var s = 0; s < n; s += 1) {
            t.renderBullet ? r += t.renderBullet.call(e, s, t.bulletClass) : r += "<" + t.bulletElement + ' class="' + t.bulletClass + '"></' + t.bulletElement + ">";
          }

          a.html(r), e.pagination.bullets = a.find("." + t.bulletClass.replace(/ /g, "."));
        }

        "fraction" === t.type && (r = t.renderFraction ? t.renderFraction.call(e, t.currentClass, t.totalClass) : '<span class="' + t.currentClass + '"></span> / <span class="' + t.totalClass + '"></span>', a.html(r)), "progressbar" === t.type && (r = t.renderProgressbar ? t.renderProgressbar.call(e, t.progressbarFillClass) : '<span class="' + t.progressbarFillClass + '"></span>', a.html(r)), "custom" !== t.type && e.emit("paginationRender", e.pagination.$el[0]);
      }
    },
    init: function init() {
      var e = this,
          t = e.params.pagination;

      if (t.el) {
        var i = v(t.el);
        0 !== i.length && (e.params.uniqueNavElements && "string" == typeof t.el && i.length > 1 && (i = e.$el.find(t.el)), "bullets" === t.type && t.clickable && i.addClass(t.clickableClass), i.addClass(t.modifierClass + t.type), "bullets" === t.type && t.dynamicBullets && (i.addClass("" + t.modifierClass + t.type + "-dynamic"), e.pagination.dynamicBulletIndex = 0, t.dynamicMainBullets < 1 && (t.dynamicMainBullets = 1)), "progressbar" === t.type && t.progressbarOpposite && i.addClass(t.progressbarOppositeClass), t.clickable && i.on("click", "." + t.bulletClass.replace(/ /g, "."), function (t) {
          t.preventDefault();
          var i = v(this).index() * e.params.slidesPerGroup;
          e.params.loop && (i += e.loopedSlides), e.slideTo(i);
        }), C(e.pagination, {
          $el: i,
          el: i[0]
        }));
      }
    },
    destroy: function destroy() {
      var e = this,
          t = e.params.pagination;

      if (t.el && e.pagination.el && e.pagination.$el && 0 !== e.pagination.$el.length) {
        var i = e.pagination.$el;
        i.removeClass(t.hiddenClass), i.removeClass(t.modifierClass + t.type), e.pagination.bullets && e.pagination.bullets.removeClass(t.bulletActiveClass), t.clickable && i.off("click", "." + t.bulletClass.replace(/ /g, "."));
      }
    }
  },
      Z = {
    setTranslate: function setTranslate() {
      var e = this;

      if (e.params.scrollbar.el && e.scrollbar.el) {
        var t = e.scrollbar,
            i = e.rtlTranslate,
            a = e.progress,
            r = t.dragSize,
            n = t.trackSize,
            s = t.$dragEl,
            o = t.$el,
            l = e.params.scrollbar,
            d = r,
            u = (n - r) * a;
        i ? (u = -u) > 0 ? (d = r - u, u = 0) : -u + r > n && (d = n + u) : u < 0 ? (d = r + u, u = 0) : u + r > n && (d = n - u), e.isHorizontal() ? (s.transform("translate3d(" + u + "px, 0, 0)"), s[0].style.width = d + "px") : (s.transform("translate3d(0px, " + u + "px, 0)"), s[0].style.height = d + "px"), l.hide && (clearTimeout(e.scrollbar.timeout), o[0].style.opacity = 1, e.scrollbar.timeout = setTimeout(function () {
          o[0].style.opacity = 0, o.transition(400);
        }, 1e3));
      }
    },
    setTransition: function setTransition(e) {
      var t = this;
      t.params.scrollbar.el && t.scrollbar.el && t.scrollbar.$dragEl.transition(e);
    },
    updateSize: function updateSize() {
      var e = this;

      if (e.params.scrollbar.el && e.scrollbar.el) {
        var t = e.scrollbar,
            i = t.$dragEl,
            a = t.$el;
        i[0].style.width = "", i[0].style.height = "";
        var r,
            n = e.isHorizontal() ? a[0].offsetWidth : a[0].offsetHeight,
            s = e.size / e.virtualSize,
            o = s * (n / e.size);
        r = "auto" === e.params.scrollbar.dragSize ? n * s : parseInt(e.params.scrollbar.dragSize, 10), e.isHorizontal() ? i[0].style.width = r + "px" : i[0].style.height = r + "px", a[0].style.display = s >= 1 ? "none" : "", e.params.scrollbar.hide && (a[0].style.opacity = 0), C(t, {
          trackSize: n,
          divider: s,
          moveDivider: o,
          dragSize: r
        }), t.$el[e.params.watchOverflow && e.isLocked ? "addClass" : "removeClass"](e.params.scrollbar.lockClass);
      }
    },
    getPointerPosition: function getPointerPosition(e) {
      return this.isHorizontal() ? "touchstart" === e.type || "touchmove" === e.type ? e.targetTouches[0].clientX : e.clientX : "touchstart" === e.type || "touchmove" === e.type ? e.targetTouches[0].clientY : e.clientY;
    },
    setDragPosition: function setDragPosition(e) {
      var t,
          i = this,
          a = i.scrollbar,
          r = i.rtlTranslate,
          n = a.$el,
          s = a.dragSize,
          o = a.trackSize,
          l = a.dragStartPos;
      t = (a.getPointerPosition(e) - n.offset()[i.isHorizontal() ? "left" : "top"] - (null !== l ? l : s / 2)) / (o - s), t = Math.max(Math.min(t, 1), 0), r && (t = 1 - t);
      var d = i.minTranslate() + (i.maxTranslate() - i.minTranslate()) * t;
      i.updateProgress(d), i.setTranslate(d), i.updateActiveIndex(), i.updateSlidesClasses();
    },
    onDragStart: function onDragStart(e) {
      var t = this,
          i = t.params.scrollbar,
          a = t.scrollbar,
          r = t.$wrapperEl,
          n = a.$el,
          s = a.$dragEl;
      t.scrollbar.isTouched = !0, t.scrollbar.dragStartPos = e.target === s[0] || e.target === s ? a.getPointerPosition(e) - e.target.getBoundingClientRect()[t.isHorizontal() ? "left" : "top"] : null, e.preventDefault(), e.stopPropagation(), r.transition(100), s.transition(100), a.setDragPosition(e), clearTimeout(t.scrollbar.dragTimeout), n.transition(0), i.hide && n.css("opacity", 1), t.params.cssMode && t.$wrapperEl.css("scroll-snap-type", "none"), t.emit("scrollbarDragStart", e);
    },
    onDragMove: function onDragMove(e) {
      var t = this,
          i = t.scrollbar,
          a = t.$wrapperEl,
          r = i.$el,
          n = i.$dragEl;
      t.scrollbar.isTouched && (e.preventDefault ? e.preventDefault() : e.returnValue = !1, i.setDragPosition(e), a.transition(0), r.transition(0), n.transition(0), t.emit("scrollbarDragMove", e));
    },
    onDragEnd: function onDragEnd(e) {
      var t = this,
          i = t.params.scrollbar,
          a = t.scrollbar,
          r = t.$wrapperEl,
          n = a.$el;
      t.scrollbar.isTouched && (t.scrollbar.isTouched = !1, t.params.cssMode && (t.$wrapperEl.css("scroll-snap-type", ""), r.transition("")), i.hide && (clearTimeout(t.scrollbar.dragTimeout), t.scrollbar.dragTimeout = x(function () {
        n.css("opacity", 0), n.transition(400);
      }, 1e3)), t.emit("scrollbarDragEnd", e), i.snapOnRelease && t.slideToClosest());
    },
    enableDraggable: function enableDraggable() {
      var e = this;

      if (e.params.scrollbar.el) {
        var t = n(),
            i = e.scrollbar,
            a = e.touchEventsTouch,
            r = e.touchEventsDesktop,
            s = e.params,
            o = e.support,
            l = i.$el[0],
            d = !(!o.passiveListener || !s.passiveListeners) && {
          passive: !1,
          capture: !1
        },
            u = !(!o.passiveListener || !s.passiveListeners) && {
          passive: !0,
          capture: !1
        };
        l && (o.touch ? (l.addEventListener(a.start, e.scrollbar.onDragStart, d), l.addEventListener(a.move, e.scrollbar.onDragMove, d), l.addEventListener(a.end, e.scrollbar.onDragEnd, u)) : (l.addEventListener(r.start, e.scrollbar.onDragStart, d), t.addEventListener(r.move, e.scrollbar.onDragMove, d), t.addEventListener(r.end, e.scrollbar.onDragEnd, u)));
      }
    },
    disableDraggable: function disableDraggable() {
      var e = this;

      if (e.params.scrollbar.el) {
        var t = n(),
            i = e.scrollbar,
            a = e.touchEventsTouch,
            r = e.touchEventsDesktop,
            s = e.params,
            o = e.support,
            l = i.$el[0],
            d = !(!o.passiveListener || !s.passiveListeners) && {
          passive: !1,
          capture: !1
        },
            u = !(!o.passiveListener || !s.passiveListeners) && {
          passive: !0,
          capture: !1
        };
        l && (o.touch ? (l.removeEventListener(a.start, e.scrollbar.onDragStart, d), l.removeEventListener(a.move, e.scrollbar.onDragMove, d), l.removeEventListener(a.end, e.scrollbar.onDragEnd, u)) : (l.removeEventListener(r.start, e.scrollbar.onDragStart, d), t.removeEventListener(r.move, e.scrollbar.onDragMove, d), t.removeEventListener(r.end, e.scrollbar.onDragEnd, u)));
      }
    },
    init: function init() {
      var e = this;

      if (e.params.scrollbar.el) {
        var t = e.scrollbar,
            i = e.$el,
            a = e.params.scrollbar,
            r = v(a.el);
        e.params.uniqueNavElements && "string" == typeof a.el && r.length > 1 && 1 === i.find(a.el).length && (r = i.find(a.el));
        var n = r.find("." + e.params.scrollbar.dragClass);
        0 === n.length && (n = v('<div class="' + e.params.scrollbar.dragClass + '"></div>'), r.append(n)), C(t, {
          $el: r,
          el: r[0],
          $dragEl: n,
          dragEl: n[0]
        }), a.draggable && t.enableDraggable();
      }
    },
    destroy: function destroy() {
      this.scrollbar.disableDraggable();
    }
  },
      J = {
    setTransform: function setTransform(e, t) {
      var i = this.rtl,
          a = v(e),
          r = i ? -1 : 1,
          n = a.attr("data-swiper-parallax") || "0",
          s = a.attr("data-swiper-parallax-x"),
          o = a.attr("data-swiper-parallax-y"),
          l = a.attr("data-swiper-parallax-scale"),
          d = a.attr("data-swiper-parallax-opacity");

      if (s || o ? (s = s || "0", o = o || "0") : this.isHorizontal() ? (s = n, o = "0") : (o = n, s = "0"), s = s.indexOf("%") >= 0 ? parseInt(s, 10) * t * r + "%" : s * t * r + "px", o = o.indexOf("%") >= 0 ? parseInt(o, 10) * t + "%" : o * t + "px", null != d) {
        var u = d - (d - 1) * (1 - Math.abs(t));
        a[0].style.opacity = u;
      }

      if (null == l) a.transform("translate3d(" + s + ", " + o + ", 0px)");else {
        var p = l - (l - 1) * (1 - Math.abs(t));
        a.transform("translate3d(" + s + ", " + o + ", 0px) scale(" + p + ")");
      }
    },
    setTranslate: function setTranslate() {
      var e = this,
          t = e.$el,
          i = e.slides,
          a = e.progress,
          r = e.snapGrid;
      t.children("[data-swiper-parallax], [data-swiper-parallax-x], [data-swiper-parallax-y], [data-swiper-parallax-opacity], [data-swiper-parallax-scale]").each(function (t) {
        e.parallax.setTransform(t, a);
      }), i.each(function (t, i) {
        var n = t.progress;
        e.params.slidesPerGroup > 1 && "auto" !== e.params.slidesPerView && (n += Math.ceil(i / 2) - a * (r.length - 1)), n = Math.min(Math.max(n, -1), 1), v(t).find("[data-swiper-parallax], [data-swiper-parallax-x], [data-swiper-parallax-y], [data-swiper-parallax-opacity], [data-swiper-parallax-scale]").each(function (t) {
          e.parallax.setTransform(t, n);
        });
      });
    },
    setTransition: function setTransition(e) {
      void 0 === e && (e = this.params.speed), this.$el.find("[data-swiper-parallax], [data-swiper-parallax-x], [data-swiper-parallax-y], [data-swiper-parallax-opacity], [data-swiper-parallax-scale]").each(function (t) {
        var i = v(t),
            a = parseInt(i.attr("data-swiper-parallax-duration"), 10) || e;
        0 === e && (a = 0), i.transition(a);
      });
    }
  },
      ee = {
    getDistanceBetweenTouches: function getDistanceBetweenTouches(e) {
      if (e.targetTouches.length < 2) return 1;
      var t = e.targetTouches[0].pageX,
          i = e.targetTouches[0].pageY,
          a = e.targetTouches[1].pageX,
          r = e.targetTouches[1].pageY;
      return Math.sqrt(Math.pow(a - t, 2) + Math.pow(r - i, 2));
    },
    onGestureStart: function onGestureStart(e) {
      var t = this,
          i = t.support,
          a = t.params.zoom,
          r = t.zoom,
          n = r.gesture;

      if (r.fakeGestureTouched = !1, r.fakeGestureMoved = !1, !i.gestures) {
        if ("touchstart" !== e.type || "touchstart" === e.type && e.targetTouches.length < 2) return;
        r.fakeGestureTouched = !0, n.scaleStart = ee.getDistanceBetweenTouches(e);
      }

      n.$slideEl && n.$slideEl.length || (n.$slideEl = v(e.target).closest("." + t.params.slideClass), 0 === n.$slideEl.length && (n.$slideEl = t.slides.eq(t.activeIndex)), n.$imageEl = n.$slideEl.find("img, svg, canvas, picture, .swiper-zoom-target"), n.$imageWrapEl = n.$imageEl.parent("." + a.containerClass), n.maxRatio = n.$imageWrapEl.attr("data-swiper-zoom") || a.maxRatio, 0 !== n.$imageWrapEl.length) ? (n.$imageEl && n.$imageEl.transition(0), t.zoom.isScaling = !0) : n.$imageEl = void 0;
    },
    onGestureChange: function onGestureChange(e) {
      var t = this,
          i = t.support,
          a = t.params.zoom,
          r = t.zoom,
          n = r.gesture;

      if (!i.gestures) {
        if ("touchmove" !== e.type || "touchmove" === e.type && e.targetTouches.length < 2) return;
        r.fakeGestureMoved = !0, n.scaleMove = ee.getDistanceBetweenTouches(e);
      }

      n.$imageEl && 0 !== n.$imageEl.length ? (i.gestures ? r.scale = e.scale * r.currentScale : r.scale = n.scaleMove / n.scaleStart * r.currentScale, r.scale > n.maxRatio && (r.scale = n.maxRatio - 1 + Math.pow(r.scale - n.maxRatio + 1, .5)), r.scale < a.minRatio && (r.scale = a.minRatio + 1 - Math.pow(a.minRatio - r.scale + 1, .5)), n.$imageEl.transform("translate3d(0,0,0) scale(" + r.scale + ")")) : "gesturechange" === e.type && r.onGestureStart(e);
    },
    onGestureEnd: function onGestureEnd(e) {
      var t = this,
          i = t.device,
          a = t.support,
          r = t.params.zoom,
          n = t.zoom,
          s = n.gesture;

      if (!a.gestures) {
        if (!n.fakeGestureTouched || !n.fakeGestureMoved) return;
        if ("touchend" !== e.type || "touchend" === e.type && e.changedTouches.length < 2 && !i.android) return;
        n.fakeGestureTouched = !1, n.fakeGestureMoved = !1;
      }

      s.$imageEl && 0 !== s.$imageEl.length && (n.scale = Math.max(Math.min(n.scale, s.maxRatio), r.minRatio), s.$imageEl.transition(t.params.speed).transform("translate3d(0,0,0) scale(" + n.scale + ")"), n.currentScale = n.scale, n.isScaling = !1, 1 === n.scale && (s.$slideEl = void 0));
    },
    onTouchStart: function onTouchStart(e) {
      var t = this.device,
          i = this.zoom,
          a = i.gesture,
          r = i.image;
      a.$imageEl && 0 !== a.$imageEl.length && (r.isTouched || (t.android && e.cancelable && e.preventDefault(), r.isTouched = !0, r.touchesStart.x = "touchstart" === e.type ? e.targetTouches[0].pageX : e.pageX, r.touchesStart.y = "touchstart" === e.type ? e.targetTouches[0].pageY : e.pageY));
    },
    onTouchMove: function onTouchMove(e) {
      var t = this,
          i = t.zoom,
          a = i.gesture,
          r = i.image,
          n = i.velocity;

      if (a.$imageEl && 0 !== a.$imageEl.length && (t.allowClick = !1, r.isTouched && a.$slideEl)) {
        r.isMoved || (r.width = a.$imageEl[0].offsetWidth, r.height = a.$imageEl[0].offsetHeight, r.startX = _(a.$imageWrapEl[0], "x") || 0, r.startY = _(a.$imageWrapEl[0], "y") || 0, a.slideWidth = a.$slideEl[0].offsetWidth, a.slideHeight = a.$slideEl[0].offsetHeight, a.$imageWrapEl.transition(0), t.rtl && (r.startX = -r.startX, r.startY = -r.startY));
        var s = r.width * i.scale,
            o = r.height * i.scale;

        if (!(s < a.slideWidth && o < a.slideHeight)) {
          if (r.minX = Math.min(a.slideWidth / 2 - s / 2, 0), r.maxX = -r.minX, r.minY = Math.min(a.slideHeight / 2 - o / 2, 0), r.maxY = -r.minY, r.touchesCurrent.x = "touchmove" === e.type ? e.targetTouches[0].pageX : e.pageX, r.touchesCurrent.y = "touchmove" === e.type ? e.targetTouches[0].pageY : e.pageY, !r.isMoved && !i.isScaling) {
            if (t.isHorizontal() && (Math.floor(r.minX) === Math.floor(r.startX) && r.touchesCurrent.x < r.touchesStart.x || Math.floor(r.maxX) === Math.floor(r.startX) && r.touchesCurrent.x > r.touchesStart.x)) return void (r.isTouched = !1);
            if (!t.isHorizontal() && (Math.floor(r.minY) === Math.floor(r.startY) && r.touchesCurrent.y < r.touchesStart.y || Math.floor(r.maxY) === Math.floor(r.startY) && r.touchesCurrent.y > r.touchesStart.y)) return void (r.isTouched = !1);
          }

          e.cancelable && e.preventDefault(), e.stopPropagation(), r.isMoved = !0, r.currentX = r.touchesCurrent.x - r.touchesStart.x + r.startX, r.currentY = r.touchesCurrent.y - r.touchesStart.y + r.startY, r.currentX < r.minX && (r.currentX = r.minX + 1 - Math.pow(r.minX - r.currentX + 1, .8)), r.currentX > r.maxX && (r.currentX = r.maxX - 1 + Math.pow(r.currentX - r.maxX + 1, .8)), r.currentY < r.minY && (r.currentY = r.minY + 1 - Math.pow(r.minY - r.currentY + 1, .8)), r.currentY > r.maxY && (r.currentY = r.maxY - 1 + Math.pow(r.currentY - r.maxY + 1, .8)), n.prevPositionX || (n.prevPositionX = r.touchesCurrent.x), n.prevPositionY || (n.prevPositionY = r.touchesCurrent.y), n.prevTime || (n.prevTime = Date.now()), n.x = (r.touchesCurrent.x - n.prevPositionX) / (Date.now() - n.prevTime) / 2, n.y = (r.touchesCurrent.y - n.prevPositionY) / (Date.now() - n.prevTime) / 2, Math.abs(r.touchesCurrent.x - n.prevPositionX) < 2 && (n.x = 0), Math.abs(r.touchesCurrent.y - n.prevPositionY) < 2 && (n.y = 0), n.prevPositionX = r.touchesCurrent.x, n.prevPositionY = r.touchesCurrent.y, n.prevTime = Date.now(), a.$imageWrapEl.transform("translate3d(" + r.currentX + "px, " + r.currentY + "px,0)");
        }
      }
    },
    onTouchEnd: function onTouchEnd() {
      var e = this.zoom,
          t = e.gesture,
          i = e.image,
          a = e.velocity;

      if (t.$imageEl && 0 !== t.$imageEl.length) {
        if (!i.isTouched || !i.isMoved) return i.isTouched = !1, void (i.isMoved = !1);
        i.isTouched = !1, i.isMoved = !1;
        var r = 300,
            n = 300,
            s = a.x * r,
            o = i.currentX + s,
            l = a.y * n,
            d = i.currentY + l;
        0 !== a.x && (r = Math.abs((o - i.currentX) / a.x)), 0 !== a.y && (n = Math.abs((d - i.currentY) / a.y));
        var u = Math.max(r, n);
        i.currentX = o, i.currentY = d;
        var p = i.width * e.scale,
            c = i.height * e.scale;
        i.minX = Math.min(t.slideWidth / 2 - p / 2, 0), i.maxX = -i.minX, i.minY = Math.min(t.slideHeight / 2 - c / 2, 0), i.maxY = -i.minY, i.currentX = Math.max(Math.min(i.currentX, i.maxX), i.minX), i.currentY = Math.max(Math.min(i.currentY, i.maxY), i.minY), t.$imageWrapEl.transition(u).transform("translate3d(" + i.currentX + "px, " + i.currentY + "px,0)");
      }
    },
    onTransitionEnd: function onTransitionEnd() {
      var e = this,
          t = e.zoom,
          i = t.gesture;
      i.$slideEl && e.previousIndex !== e.activeIndex && (i.$imageEl && i.$imageEl.transform("translate3d(0,0,0) scale(1)"), i.$imageWrapEl && i.$imageWrapEl.transform("translate3d(0,0,0)"), t.scale = 1, t.currentScale = 1, i.$slideEl = void 0, i.$imageEl = void 0, i.$imageWrapEl = void 0);
    },
    toggle: function toggle(e) {
      var t = this.zoom;
      t.scale && 1 !== t.scale ? t.out() : t["in"](e);
    },
    "in": function _in(e) {
      var t,
          i,
          a,
          r,
          n,
          s,
          l,
          d,
          u,
          p,
          c,
          h,
          f,
          m,
          v,
          g,
          y = this,
          w = o(),
          b = y.zoom,
          x = y.params.zoom,
          T = b.gesture,
          _ = b.image;
      T.$slideEl || (y.params.virtual && y.params.virtual.enabled && y.virtual ? T.$slideEl = y.$wrapperEl.children("." + y.params.slideActiveClass) : T.$slideEl = y.slides.eq(y.activeIndex), T.$imageEl = T.$slideEl.find("img, svg, canvas, picture, .swiper-zoom-target"), T.$imageWrapEl = T.$imageEl.parent("." + x.containerClass)), T.$imageEl && 0 !== T.$imageEl.length && (T.$slideEl.addClass("" + x.zoomedSlideClass), void 0 === _.touchesStart.x && e ? (t = "touchend" === e.type ? e.changedTouches[0].pageX : e.pageX, i = "touchend" === e.type ? e.changedTouches[0].pageY : e.pageY) : (t = _.touchesStart.x, i = _.touchesStart.y), b.scale = T.$imageWrapEl.attr("data-swiper-zoom") || x.maxRatio, b.currentScale = T.$imageWrapEl.attr("data-swiper-zoom") || x.maxRatio, e ? (v = T.$slideEl[0].offsetWidth, g = T.$slideEl[0].offsetHeight, a = T.$slideEl.offset().left + w.scrollX + v / 2 - t, r = T.$slideEl.offset().top + w.scrollY + g / 2 - i, l = T.$imageEl[0].offsetWidth, d = T.$imageEl[0].offsetHeight, u = l * b.scale, p = d * b.scale, f = -(c = Math.min(v / 2 - u / 2, 0)), m = -(h = Math.min(g / 2 - p / 2, 0)), (n = a * b.scale) < c && (n = c), n > f && (n = f), (s = r * b.scale) < h && (s = h), s > m && (s = m)) : (n = 0, s = 0), T.$imageWrapEl.transition(300).transform("translate3d(" + n + "px, " + s + "px,0)"), T.$imageEl.transition(300).transform("translate3d(0,0,0) scale(" + b.scale + ")"));
    },
    out: function out() {
      var e = this,
          t = e.zoom,
          i = e.params.zoom,
          a = t.gesture;
      a.$slideEl || (e.params.virtual && e.params.virtual.enabled && e.virtual ? a.$slideEl = e.$wrapperEl.children("." + e.params.slideActiveClass) : a.$slideEl = e.slides.eq(e.activeIndex), a.$imageEl = a.$slideEl.find("img, svg, canvas, picture, .swiper-zoom-target"), a.$imageWrapEl = a.$imageEl.parent("." + i.containerClass)), a.$imageEl && 0 !== a.$imageEl.length && (t.scale = 1, t.currentScale = 1, a.$imageWrapEl.transition(300).transform("translate3d(0,0,0)"), a.$imageEl.transition(300).transform("translate3d(0,0,0) scale(1)"), a.$slideEl.removeClass("" + i.zoomedSlideClass), a.$slideEl = void 0);
    },
    toggleGestures: function toggleGestures(e) {
      var t = this,
          i = t.zoom,
          a = i.slideSelector,
          r = i.passiveListener;
      t.$wrapperEl[e]("gesturestart", a, i.onGestureStart, r), t.$wrapperEl[e]("gesturechange", a, i.onGestureChange, r), t.$wrapperEl[e]("gestureend", a, i.onGestureEnd, r);
    },
    enableGestures: function enableGestures() {
      this.zoom.gesturesEnabled || (this.zoom.gesturesEnabled = !0, this.zoom.toggleGestures("on"));
    },
    disableGestures: function disableGestures() {
      this.zoom.gesturesEnabled && (this.zoom.gesturesEnabled = !1, this.zoom.toggleGestures("off"));
    },
    enable: function enable() {
      var e = this,
          t = e.support,
          i = e.zoom;

      if (!i.enabled) {
        i.enabled = !0;
        var a = !("touchstart" !== e.touchEvents.start || !t.passiveListener || !e.params.passiveListeners) && {
          passive: !0,
          capture: !1
        },
            r = !t.passiveListener || {
          passive: !1,
          capture: !0
        },
            n = "." + e.params.slideClass;
        e.zoom.passiveListener = a, e.zoom.slideSelector = n, t.gestures ? (e.$wrapperEl.on(e.touchEvents.start, e.zoom.enableGestures, a), e.$wrapperEl.on(e.touchEvents.end, e.zoom.disableGestures, a)) : "touchstart" === e.touchEvents.start && (e.$wrapperEl.on(e.touchEvents.start, n, i.onGestureStart, a), e.$wrapperEl.on(e.touchEvents.move, n, i.onGestureChange, r), e.$wrapperEl.on(e.touchEvents.end, n, i.onGestureEnd, a), e.touchEvents.cancel && e.$wrapperEl.on(e.touchEvents.cancel, n, i.onGestureEnd, a)), e.$wrapperEl.on(e.touchEvents.move, "." + e.params.zoom.containerClass, i.onTouchMove, r);
      }
    },
    disable: function disable() {
      var e = this,
          t = e.zoom;

      if (t.enabled) {
        var i = e.support;
        e.zoom.enabled = !1;
        var a = !("touchstart" !== e.touchEvents.start || !i.passiveListener || !e.params.passiveListeners) && {
          passive: !0,
          capture: !1
        },
            r = !i.passiveListener || {
          passive: !1,
          capture: !0
        },
            n = "." + e.params.slideClass;
        i.gestures ? (e.$wrapperEl.off(e.touchEvents.start, e.zoom.enableGestures, a), e.$wrapperEl.off(e.touchEvents.end, e.zoom.disableGestures, a)) : "touchstart" === e.touchEvents.start && (e.$wrapperEl.off(e.touchEvents.start, n, t.onGestureStart, a), e.$wrapperEl.off(e.touchEvents.move, n, t.onGestureChange, r), e.$wrapperEl.off(e.touchEvents.end, n, t.onGestureEnd, a), e.touchEvents.cancel && e.$wrapperEl.off(e.touchEvents.cancel, n, t.onGestureEnd, a)), e.$wrapperEl.off(e.touchEvents.move, "." + e.params.zoom.containerClass, t.onTouchMove, r);
      }
    }
  },
      te = {
    loadInSlide: function loadInSlide(e, t) {
      void 0 === t && (t = !0);
      var i = this,
          a = i.params.lazy;

      if (void 0 !== e && 0 !== i.slides.length) {
        var r = i.virtual && i.params.virtual.enabled ? i.$wrapperEl.children("." + i.params.slideClass + '[data-swiper-slide-index="' + e + '"]') : i.slides.eq(e),
            n = r.find("." + a.elementClass + ":not(." + a.loadedClass + "):not(." + a.loadingClass + ")");
        !r.hasClass(a.elementClass) || r.hasClass(a.loadedClass) || r.hasClass(a.loadingClass) || n.push(r[0]), 0 !== n.length && n.each(function (e) {
          var n = v(e);
          n.addClass(a.loadingClass);
          var s = n.attr("data-background"),
              o = n.attr("data-src"),
              l = n.attr("data-srcset"),
              d = n.attr("data-sizes"),
              u = n.parent("picture");
          i.loadImage(n[0], o || s, l, d, !1, function () {
            if (null != i && i && (!i || i.params) && !i.destroyed) {
              if (s ? (n.css("background-image", 'url("' + s + '")'), n.removeAttr("data-background")) : (l && (n.attr("srcset", l), n.removeAttr("data-srcset")), d && (n.attr("sizes", d), n.removeAttr("data-sizes")), u.length && u.children("source").each(function (e) {
                var t = v(e);
                t.attr("data-srcset") && (t.attr("srcset", t.attr("data-srcset")), t.removeAttr("data-srcset"));
              }), o && (n.attr("src", o), n.removeAttr("data-src"))), n.addClass(a.loadedClass).removeClass(a.loadingClass), r.find("." + a.preloaderClass).remove(), i.params.loop && t) {
                var e = r.attr("data-swiper-slide-index");

                if (r.hasClass(i.params.slideDuplicateClass)) {
                  var p = i.$wrapperEl.children('[data-swiper-slide-index="' + e + '"]:not(.' + i.params.slideDuplicateClass + ")");
                  i.lazy.loadInSlide(p.index(), !1);
                } else {
                  var c = i.$wrapperEl.children("." + i.params.slideDuplicateClass + '[data-swiper-slide-index="' + e + '"]');
                  i.lazy.loadInSlide(c.index(), !1);
                }
              }

              i.emit("lazyImageReady", r[0], n[0]), i.params.autoHeight && i.updateAutoHeight();
            }
          }), i.emit("lazyImageLoad", r[0], n[0]);
        });
      }
    },
    load: function load() {
      var e = this,
          t = e.$wrapperEl,
          i = e.params,
          a = e.slides,
          r = e.activeIndex,
          n = e.virtual && i.virtual.enabled,
          s = i.lazy,
          o = i.slidesPerView;

      function l(e) {
        if (n) {
          if (t.children("." + i.slideClass + '[data-swiper-slide-index="' + e + '"]').length) return !0;
        } else if (a[e]) return !0;

        return !1;
      }

      function d(e) {
        return n ? v(e).attr("data-swiper-slide-index") : v(e).index();
      }

      if ("auto" === o && (o = 0), e.lazy.initialImageLoaded || (e.lazy.initialImageLoaded = !0), e.params.watchSlidesVisibility) t.children("." + i.slideVisibleClass).each(function (t) {
        var i = n ? v(t).attr("data-swiper-slide-index") : v(t).index();
        e.lazy.loadInSlide(i);
      });else if (o > 1) for (var u = r; u < r + o; u += 1) {
        l(u) && e.lazy.loadInSlide(u);
      } else e.lazy.loadInSlide(r);
      if (s.loadPrevNext) if (o > 1 || s.loadPrevNextAmount && s.loadPrevNextAmount > 1) {
        for (var p = s.loadPrevNextAmount, c = o, h = Math.min(r + c + Math.max(p, c), a.length), f = Math.max(r - Math.max(c, p), 0), m = r + o; m < h; m += 1) {
          l(m) && e.lazy.loadInSlide(m);
        }

        for (var g = f; g < r; g += 1) {
          l(g) && e.lazy.loadInSlide(g);
        }
      } else {
        var y = t.children("." + i.slideNextClass);
        y.length > 0 && e.lazy.loadInSlide(d(y));
        var w = t.children("." + i.slidePrevClass);
        w.length > 0 && e.lazy.loadInSlide(d(w));
      }
    },
    checkInViewOnLoad: function checkInViewOnLoad() {
      var e = o(),
          t = this;

      if (t && !t.destroyed) {
        var i = t.params.lazy.scrollingElement ? v(t.params.lazy.scrollingElement) : v(e),
            a = i[0] === e,
            r = a ? e.innerWidth : i[0].offsetWidth,
            n = a ? e.innerHeight : i[0].offsetHeight,
            s = t.$el.offset(),
            l = !1;
        t.rtlTranslate && (s.left -= t.$el[0].scrollLeft);

        for (var d = [[s.left, s.top], [s.left + t.width, s.top], [s.left, s.top + t.height], [s.left + t.width, s.top + t.height]], u = 0; u < d.length; u += 1) {
          var p = d[u];

          if (p[0] >= 0 && p[0] <= r && p[1] >= 0 && p[1] <= n) {
            if (0 === p[0] && 0 === p[1]) continue;
            l = !0;
          }
        }

        l ? (t.lazy.load(), i.off("scroll", t.lazy.checkInViewOnLoad)) : t.lazy.scrollHandlerAttached || (t.lazy.scrollHandlerAttached = !0, i.on("scroll", t.lazy.checkInViewOnLoad));
      }
    }
  },
      ie = {
    LinearSpline: function LinearSpline(e, t) {
      var i, a, r, n, s;
      return this.x = e, this.y = t, this.lastIndex = e.length - 1, this.interpolate = function (e) {
        return e ? (s = function (e, t) {
          for (a = -1, i = e.length; i - a > 1;) {
            e[r = i + a >> 1] <= t ? a = r : i = r;
          }

          return i;
        }(this.x, e), n = s - 1, (e - this.x[n]) * (this.y[s] - this.y[n]) / (this.x[s] - this.x[n]) + this.y[n]) : 0;
      }, this;
    },
    getInterpolateFunction: function getInterpolateFunction(e) {
      var t = this;
      t.controller.spline || (t.controller.spline = t.params.loop ? new ie.LinearSpline(t.slidesGrid, e.slidesGrid) : new ie.LinearSpline(t.snapGrid, e.snapGrid));
    },
    setTranslate: function setTranslate(e, t) {
      var i,
          a,
          r = this,
          n = r.controller.control,
          s = r.constructor;

      function o(e) {
        var t = r.rtlTranslate ? -r.translate : r.translate;
        "slide" === r.params.controller.by && (r.controller.getInterpolateFunction(e), a = -r.controller.spline.interpolate(-t)), a && "container" !== r.params.controller.by || (i = (e.maxTranslate() - e.minTranslate()) / (r.maxTranslate() - r.minTranslate()), a = (t - r.minTranslate()) * i + e.minTranslate()), r.params.controller.inverse && (a = e.maxTranslate() - a), e.updateProgress(a), e.setTranslate(a, r), e.updateActiveIndex(), e.updateSlidesClasses();
      }

      if (Array.isArray(n)) for (var l = 0; l < n.length; l += 1) {
        n[l] !== t && n[l] instanceof s && o(n[l]);
      } else n instanceof s && t !== n && o(n);
    },
    setTransition: function setTransition(e, t) {
      var i,
          a = this,
          r = a.constructor,
          n = a.controller.control;

      function s(t) {
        t.setTransition(e, a), 0 !== e && (t.transitionStart(), t.params.autoHeight && x(function () {
          t.updateAutoHeight();
        }), t.$wrapperEl.transitionEnd(function () {
          n && (t.params.loop && "slide" === a.params.controller.by && t.loopFix(), t.transitionEnd());
        }));
      }

      if (Array.isArray(n)) for (i = 0; i < n.length; i += 1) {
        n[i] !== t && n[i] instanceof r && s(n[i]);
      } else n instanceof r && t !== n && s(n);
    }
  },
      ae = {
    getRandomNumber: function getRandomNumber(e) {
      return void 0 === e && (e = 16), "x".repeat(e).replace(/x/g, function () {
        return Math.round(16 * Math.random()).toString(16);
      });
    },
    makeElFocusable: function makeElFocusable(e) {
      return e.attr("tabIndex", "0"), e;
    },
    makeElNotFocusable: function makeElNotFocusable(e) {
      return e.attr("tabIndex", "-1"), e;
    },
    addElRole: function addElRole(e, t) {
      return e.attr("role", t), e;
    },
    addElRoleDescription: function addElRoleDescription(e, t) {
      return e.attr("aria-role-description", t), e;
    },
    addElControls: function addElControls(e, t) {
      return e.attr("aria-controls", t), e;
    },
    addElLabel: function addElLabel(e, t) {
      return e.attr("aria-label", t), e;
    },
    addElId: function addElId(e, t) {
      return e.attr("id", t), e;
    },
    addElLive: function addElLive(e, t) {
      return e.attr("aria-live", t), e;
    },
    disableEl: function disableEl(e) {
      return e.attr("aria-disabled", !0), e;
    },
    enableEl: function enableEl(e) {
      return e.attr("aria-disabled", !1), e;
    },
    onEnterOrSpaceKey: function onEnterOrSpaceKey(e) {
      if (13 === e.keyCode || 32 === e.keyCode) {
        var t = this,
            i = t.params.a11y,
            a = v(e.target);
        t.navigation && t.navigation.$nextEl && a.is(t.navigation.$nextEl) && (t.isEnd && !t.params.loop || t.slideNext(), t.isEnd ? t.a11y.notify(i.lastSlideMessage) : t.a11y.notify(i.nextSlideMessage)), t.navigation && t.navigation.$prevEl && a.is(t.navigation.$prevEl) && (t.isBeginning && !t.params.loop || t.slidePrev(), t.isBeginning ? t.a11y.notify(i.firstSlideMessage) : t.a11y.notify(i.prevSlideMessage)), t.pagination && a.is("." + t.params.pagination.bulletClass.replace(/ /g, ".")) && a[0].click();
      }
    },
    notify: function notify(e) {
      var t = this.a11y.liveRegion;
      0 !== t.length && (t.html(""), t.html(e));
    },
    updateNavigation: function updateNavigation() {
      var e = this;

      if (!e.params.loop && e.navigation) {
        var t = e.navigation,
            i = t.$nextEl,
            a = t.$prevEl;
        a && a.length > 0 && (e.isBeginning ? (e.a11y.disableEl(a), e.a11y.makeElNotFocusable(a)) : (e.a11y.enableEl(a), e.a11y.makeElFocusable(a))), i && i.length > 0 && (e.isEnd ? (e.a11y.disableEl(i), e.a11y.makeElNotFocusable(i)) : (e.a11y.enableEl(i), e.a11y.makeElFocusable(i)));
      }
    },
    updatePagination: function updatePagination() {
      var e = this,
          t = e.params.a11y;
      e.pagination && e.params.pagination.clickable && e.pagination.bullets && e.pagination.bullets.length && e.pagination.bullets.each(function (i) {
        var a = v(i);
        e.a11y.makeElFocusable(a), e.params.pagination.renderBullet || (e.a11y.addElRole(a, "button"), e.a11y.addElLabel(a, t.paginationBulletMessage.replace(/\{\{index\}\}/, a.index() + 1)));
      });
    },
    init: function init() {
      var e = this,
          t = e.params.a11y;
      e.$el.append(e.a11y.liveRegion);
      var i = e.$el;
      t.containerRoleDescriptionMessage && e.a11y.addElRoleDescription(i, t.containerRoleDescriptionMessage), t.containerMessage && e.a11y.addElLabel(i, t.containerMessage);
      var a,
          r,
          n,
          s = e.$wrapperEl,
          o = s.attr("id") || "swiper-wrapper-" + e.a11y.getRandomNumber(16);
      e.a11y.addElId(s, o), a = e.params.autoplay && e.params.autoplay.enabled ? "off" : "polite", e.a11y.addElLive(s, a), t.itemRoleDescriptionMessage && e.a11y.addElRoleDescription(v(e.slides), t.itemRoleDescriptionMessage), e.a11y.addElRole(v(e.slides), "group"), e.slides.each(function (t) {
        var i = v(t);
        e.a11y.addElLabel(i, i.index() + 1 + " / " + e.slides.length);
      }), e.navigation && e.navigation.$nextEl && (r = e.navigation.$nextEl), e.navigation && e.navigation.$prevEl && (n = e.navigation.$prevEl), r && r.length && (e.a11y.makeElFocusable(r), "BUTTON" !== r[0].tagName && (e.a11y.addElRole(r, "button"), r.on("keydown", e.a11y.onEnterOrSpaceKey)), e.a11y.addElLabel(r, t.nextSlideMessage), e.a11y.addElControls(r, o)), n && n.length && (e.a11y.makeElFocusable(n), "BUTTON" !== n[0].tagName && (e.a11y.addElRole(n, "button"), n.on("keydown", e.a11y.onEnterOrSpaceKey)), e.a11y.addElLabel(n, t.prevSlideMessage), e.a11y.addElControls(n, o)), e.pagination && e.params.pagination.clickable && e.pagination.bullets && e.pagination.bullets.length && e.pagination.$el.on("keydown", "." + e.params.pagination.bulletClass.replace(/ /g, "."), e.a11y.onEnterOrSpaceKey);
    },
    destroy: function destroy() {
      var e,
          t,
          i = this;
      i.a11y.liveRegion && i.a11y.liveRegion.length > 0 && i.a11y.liveRegion.remove(), i.navigation && i.navigation.$nextEl && (e = i.navigation.$nextEl), i.navigation && i.navigation.$prevEl && (t = i.navigation.$prevEl), e && e.off("keydown", i.a11y.onEnterOrSpaceKey), t && t.off("keydown", i.a11y.onEnterOrSpaceKey), i.pagination && i.params.pagination.clickable && i.pagination.bullets && i.pagination.bullets.length && i.pagination.$el.off("keydown", "." + i.params.pagination.bulletClass.replace(/ /g, "."), i.a11y.onEnterOrSpaceKey);
    }
  },
      re = {
    init: function init() {
      var e = this,
          t = o();

      if (e.params.history) {
        if (!t.history || !t.history.pushState) return e.params.history.enabled = !1, void (e.params.hashNavigation.enabled = !0);
        var i = e.history;
        i.initialized = !0, i.paths = re.getPathValues(e.params.url), (i.paths.key || i.paths.value) && (i.scrollToSlide(0, i.paths.value, e.params.runCallbacksOnInit), e.params.history.replaceState || t.addEventListener("popstate", e.history.setHistoryPopState));
      }
    },
    destroy: function destroy() {
      var e = o();
      this.params.history.replaceState || e.removeEventListener("popstate", this.history.setHistoryPopState);
    },
    setHistoryPopState: function setHistoryPopState() {
      var e = this;
      e.history.paths = re.getPathValues(e.params.url), e.history.scrollToSlide(e.params.speed, e.history.paths.value, !1);
    },
    getPathValues: function getPathValues(e) {
      var t = o(),
          i = (e ? new URL(e) : t.location).pathname.slice(1).split("/").filter(function (e) {
        return "" !== e;
      }),
          a = i.length;
      return {
        key: i[a - 2],
        value: i[a - 1]
      };
    },
    setHistory: function setHistory(e, t) {
      var i = this,
          a = o();

      if (i.history.initialized && i.params.history.enabled) {
        var r;
        r = i.params.url ? new URL(i.params.url) : a.location;
        var n = i.slides.eq(t),
            s = re.slugify(n.attr("data-history"));
        r.pathname.includes(e) || (s = e + "/" + s);
        var l = a.history.state;
        l && l.value === s || (i.params.history.replaceState ? a.history.replaceState({
          value: s
        }, null, s) : a.history.pushState({
          value: s
        }, null, s));
      }
    },
    slugify: function slugify(e) {
      return e.toString().replace(/\s+/g, "-").replace(/[^\w-]+/g, "").replace(/--+/g, "-").replace(/^-+/, "").replace(/-+$/, "");
    },
    scrollToSlide: function scrollToSlide(e, t, i) {
      var a = this;
      if (t) for (var r = 0, n = a.slides.length; r < n; r += 1) {
        var s = a.slides.eq(r);

        if (re.slugify(s.attr("data-history")) === t && !s.hasClass(a.params.slideDuplicateClass)) {
          var o = s.index();
          a.slideTo(o, e, i);
        }
      } else a.slideTo(0, e, i);
    }
  },
      ne = {
    onHashCange: function onHashCange() {
      var e = this,
          t = n();
      e.emit("hashChange");
      var i = t.location.hash.replace("#", "");

      if (i !== e.slides.eq(e.activeIndex).attr("data-hash")) {
        var a = e.$wrapperEl.children("." + e.params.slideClass + '[data-hash="' + i + '"]').index();
        if (void 0 === a) return;
        e.slideTo(a);
      }
    },
    setHash: function setHash() {
      var e = this,
          t = o(),
          i = n();
      if (e.hashNavigation.initialized && e.params.hashNavigation.enabled) if (e.params.hashNavigation.replaceState && t.history && t.history.replaceState) t.history.replaceState(null, null, "#" + e.slides.eq(e.activeIndex).attr("data-hash") || ""), e.emit("hashSet");else {
        var a = e.slides.eq(e.activeIndex),
            r = a.attr("data-hash") || a.attr("data-history");
        i.location.hash = r || "", e.emit("hashSet");
      }
    },
    init: function init() {
      var e = this,
          t = n(),
          i = o();

      if (!(!e.params.hashNavigation.enabled || e.params.history && e.params.history.enabled)) {
        e.hashNavigation.initialized = !0;
        var a = t.location.hash.replace("#", "");
        if (a) for (var r = 0, s = e.slides.length; r < s; r += 1) {
          var l = e.slides.eq(r);

          if ((l.attr("data-hash") || l.attr("data-history")) === a && !l.hasClass(e.params.slideDuplicateClass)) {
            var d = l.index();
            e.slideTo(d, 0, e.params.runCallbacksOnInit, !0);
          }
        }
        e.params.hashNavigation.watchState && v(i).on("hashchange", e.hashNavigation.onHashCange);
      }
    },
    destroy: function destroy() {
      var e = o();
      this.params.hashNavigation.watchState && v(e).off("hashchange", this.hashNavigation.onHashCange);
    }
  },
      se = {
    run: function run() {
      var e = this,
          t = e.slides.eq(e.activeIndex),
          i = e.params.autoplay.delay;
      t.attr("data-swiper-autoplay") && (i = t.attr("data-swiper-autoplay") || e.params.autoplay.delay), clearTimeout(e.autoplay.timeout), e.autoplay.timeout = x(function () {
        var t;
        e.params.autoplay.reverseDirection ? e.params.loop ? (e.loopFix(), t = e.slidePrev(e.params.speed, !0, !0), e.emit("autoplay")) : e.isBeginning ? e.params.autoplay.stopOnLastSlide ? e.autoplay.stop() : (t = e.slideTo(e.slides.length - 1, e.params.speed, !0, !0), e.emit("autoplay")) : (t = e.slidePrev(e.params.speed, !0, !0), e.emit("autoplay")) : e.params.loop ? (e.loopFix(), t = e.slideNext(e.params.speed, !0, !0), e.emit("autoplay")) : e.isEnd ? e.params.autoplay.stopOnLastSlide ? e.autoplay.stop() : (t = e.slideTo(0, e.params.speed, !0, !0), e.emit("autoplay")) : (t = e.slideNext(e.params.speed, !0, !0), e.emit("autoplay")), (e.params.cssMode && e.autoplay.running || !1 === t) && e.autoplay.run();
      }, i);
    },
    start: function start() {
      var e = this;
      return void 0 === e.autoplay.timeout && !e.autoplay.running && (e.autoplay.running = !0, e.emit("autoplayStart"), e.autoplay.run(), !0);
    },
    stop: function stop() {
      var e = this;
      return !!e.autoplay.running && void 0 !== e.autoplay.timeout && (e.autoplay.timeout && (clearTimeout(e.autoplay.timeout), e.autoplay.timeout = void 0), e.autoplay.running = !1, e.emit("autoplayStop"), !0);
    },
    pause: function pause(e) {
      var t = this;
      t.autoplay.running && (t.autoplay.paused || (t.autoplay.timeout && clearTimeout(t.autoplay.timeout), t.autoplay.paused = !0, 0 !== e && t.params.autoplay.waitForTransition ? (t.$wrapperEl[0].addEventListener("transitionend", t.autoplay.onTransitionEnd), t.$wrapperEl[0].addEventListener("webkitTransitionEnd", t.autoplay.onTransitionEnd)) : (t.autoplay.paused = !1, t.autoplay.run())));
    },
    onVisibilityChange: function onVisibilityChange() {
      var e = this,
          t = n();
      "hidden" === t.visibilityState && e.autoplay.running && e.autoplay.pause(), "visible" === t.visibilityState && e.autoplay.paused && (e.autoplay.run(), e.autoplay.paused = !1);
    },
    onTransitionEnd: function onTransitionEnd(e) {
      var t = this;
      t && !t.destroyed && t.$wrapperEl && e.target === t.$wrapperEl[0] && (t.$wrapperEl[0].removeEventListener("transitionend", t.autoplay.onTransitionEnd), t.$wrapperEl[0].removeEventListener("webkitTransitionEnd", t.autoplay.onTransitionEnd), t.autoplay.paused = !1, t.autoplay.running ? t.autoplay.run() : t.autoplay.stop());
    }
  },
      oe = {
    setTranslate: function setTranslate() {
      for (var e = this, t = e.slides, i = 0; i < t.length; i += 1) {
        var a = e.slides.eq(i),
            r = -a[0].swiperSlideOffset;
        e.params.virtualTranslate || (r -= e.translate);
        var n = 0;
        e.isHorizontal() || (n = r, r = 0);
        var s = e.params.fadeEffect.crossFade ? Math.max(1 - Math.abs(a[0].progress), 0) : 1 + Math.min(Math.max(a[0].progress, -1), 0);
        a.css({
          opacity: s
        }).transform("translate3d(" + r + "px, " + n + "px, 0px)");
      }
    },
    setTransition: function setTransition(e) {
      var t = this,
          i = t.slides,
          a = t.$wrapperEl;

      if (i.transition(e), t.params.virtualTranslate && 0 !== e) {
        var r = !1;
        i.transitionEnd(function () {
          if (!r && t && !t.destroyed) {
            r = !0, t.animating = !1;

            for (var e = ["webkitTransitionEnd", "transitionend"], i = 0; i < e.length; i += 1) {
              a.trigger(e[i]);
            }
          }
        });
      }
    }
  },
      le = {
    setTranslate: function setTranslate() {
      var e,
          t = this,
          i = t.$el,
          a = t.$wrapperEl,
          r = t.slides,
          n = t.width,
          s = t.height,
          o = t.rtlTranslate,
          l = t.size,
          d = t.browser,
          u = t.params.cubeEffect,
          p = t.isHorizontal(),
          c = t.virtual && t.params.virtual.enabled,
          h = 0;
      u.shadow && (p ? (0 === (e = a.find(".swiper-cube-shadow")).length && (e = v('<div class="swiper-cube-shadow"></div>'), a.append(e)), e.css({
        height: n + "px"
      })) : 0 === (e = i.find(".swiper-cube-shadow")).length && (e = v('<div class="swiper-cube-shadow"></div>'), i.append(e)));

      for (var f = 0; f < r.length; f += 1) {
        var m = r.eq(f),
            g = f;
        c && (g = parseInt(m.attr("data-swiper-slide-index"), 10));
        var y = 90 * g,
            w = Math.floor(y / 360);
        o && (y = -y, w = Math.floor(-y / 360));
        var b = Math.max(Math.min(m[0].progress, 1), -1),
            x = 0,
            T = 0,
            _ = 0;
        g % 4 == 0 ? (x = 4 * -w * l, _ = 0) : (g - 1) % 4 == 0 ? (x = 0, _ = 4 * -w * l) : (g - 2) % 4 == 0 ? (x = l + 4 * w * l, _ = l) : (g - 3) % 4 == 0 && (x = -l, _ = 3 * l + 4 * l * w), o && (x = -x), p || (T = x, x = 0);
        var E = "rotateX(" + (p ? 0 : -y) + "deg) rotateY(" + (p ? y : 0) + "deg) translate3d(" + x + "px, " + T + "px, " + _ + "px)";

        if (b <= 1 && b > -1 && (h = 90 * g + 90 * b, o && (h = 90 * -g - 90 * b)), m.transform(E), u.slideShadows) {
          var C = p ? m.find(".swiper-slide-shadow-left") : m.find(".swiper-slide-shadow-top"),
              S = p ? m.find(".swiper-slide-shadow-right") : m.find(".swiper-slide-shadow-bottom");
          0 === C.length && (C = v('<div class="swiper-slide-shadow-' + (p ? "left" : "top") + '"></div>'), m.append(C)), 0 === S.length && (S = v('<div class="swiper-slide-shadow-' + (p ? "right" : "bottom") + '"></div>'), m.append(S)), C.length && (C[0].style.opacity = Math.max(-b, 0)), S.length && (S[0].style.opacity = Math.max(b, 0));
        }
      }

      if (a.css({
        "-webkit-transform-origin": "50% 50% -" + l / 2 + "px",
        "-moz-transform-origin": "50% 50% -" + l / 2 + "px",
        "-ms-transform-origin": "50% 50% -" + l / 2 + "px",
        "transform-origin": "50% 50% -" + l / 2 + "px"
      }), u.shadow) if (p) e.transform("translate3d(0px, " + (n / 2 + u.shadowOffset) + "px, " + -n / 2 + "px) rotateX(90deg) rotateZ(0deg) scale(" + u.shadowScale + ")");else {
        var M = Math.abs(h) - 90 * Math.floor(Math.abs(h) / 90),
            z = 1.5 - (Math.sin(2 * M * Math.PI / 360) / 2 + Math.cos(2 * M * Math.PI / 360) / 2),
            k = u.shadowScale,
            P = u.shadowScale / z,
            O = u.shadowOffset;
        e.transform("scale3d(" + k + ", 1, " + P + ") translate3d(0px, " + (s / 2 + O) + "px, " + -s / 2 / P + "px) rotateX(-90deg)");
      }
      var L = d.isSafari || d.isWebView ? -l / 2 : 0;
      a.transform("translate3d(0px,0," + L + "px) rotateX(" + (t.isHorizontal() ? 0 : h) + "deg) rotateY(" + (t.isHorizontal() ? -h : 0) + "deg)");
    },
    setTransition: function setTransition(e) {
      var t = this,
          i = t.$el;
      t.slides.transition(e).find(".swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left").transition(e), t.params.cubeEffect.shadow && !t.isHorizontal() && i.find(".swiper-cube-shadow").transition(e);
    }
  },
      de = {
    setTranslate: function setTranslate() {
      for (var e = this, t = e.slides, i = e.rtlTranslate, a = 0; a < t.length; a += 1) {
        var r = t.eq(a),
            n = r[0].progress;
        e.params.flipEffect.limitRotation && (n = Math.max(Math.min(r[0].progress, 1), -1));
        var s = -180 * n,
            o = 0,
            l = -r[0].swiperSlideOffset,
            d = 0;

        if (e.isHorizontal() ? i && (s = -s) : (d = l, l = 0, o = -s, s = 0), r[0].style.zIndex = -Math.abs(Math.round(n)) + t.length, e.params.flipEffect.slideShadows) {
          var u = e.isHorizontal() ? r.find(".swiper-slide-shadow-left") : r.find(".swiper-slide-shadow-top"),
              p = e.isHorizontal() ? r.find(".swiper-slide-shadow-right") : r.find(".swiper-slide-shadow-bottom");
          0 === u.length && (u = v('<div class="swiper-slide-shadow-' + (e.isHorizontal() ? "left" : "top") + '"></div>'), r.append(u)), 0 === p.length && (p = v('<div class="swiper-slide-shadow-' + (e.isHorizontal() ? "right" : "bottom") + '"></div>'), r.append(p)), u.length && (u[0].style.opacity = Math.max(-n, 0)), p.length && (p[0].style.opacity = Math.max(n, 0));
        }

        r.transform("translate3d(" + l + "px, " + d + "px, 0px) rotateX(" + o + "deg) rotateY(" + s + "deg)");
      }
    },
    setTransition: function setTransition(e) {
      var t = this,
          i = t.slides,
          a = t.activeIndex,
          r = t.$wrapperEl;

      if (i.transition(e).find(".swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left").transition(e), t.params.virtualTranslate && 0 !== e) {
        var n = !1;
        i.eq(a).transitionEnd(function () {
          if (!n && t && !t.destroyed) {
            n = !0, t.animating = !1;

            for (var e = ["webkitTransitionEnd", "transitionend"], i = 0; i < e.length; i += 1) {
              r.trigger(e[i]);
            }
          }
        });
      }
    }
  },
      ue = {
    setTranslate: function setTranslate() {
      for (var e = this, t = e.width, i = e.height, a = e.slides, r = e.slidesSizesGrid, n = e.params.coverflowEffect, s = e.isHorizontal(), o = e.translate, l = s ? t / 2 - o : i / 2 - o, d = s ? n.rotate : -n.rotate, u = n.depth, p = 0, c = a.length; p < c; p += 1) {
        var h = a.eq(p),
            f = r[p],
            m = (l - h[0].swiperSlideOffset - f / 2) / f * n.modifier,
            g = s ? d * m : 0,
            y = s ? 0 : d * m,
            w = -u * Math.abs(m),
            b = n.stretch;
        "string" == typeof b && -1 !== b.indexOf("%") && (b = parseFloat(n.stretch) / 100 * f);

        var x = s ? 0 : b * m,
            T = s ? b * m : 0,
            _ = 1 - (1 - n.scale) * Math.abs(m);

        Math.abs(T) < .001 && (T = 0), Math.abs(x) < .001 && (x = 0), Math.abs(w) < .001 && (w = 0), Math.abs(g) < .001 && (g = 0), Math.abs(y) < .001 && (y = 0), Math.abs(_) < .001 && (_ = 0);
        var E = "translate3d(" + T + "px," + x + "px," + w + "px)  rotateX(" + y + "deg) rotateY(" + g + "deg) scale(" + _ + ")";

        if (h.transform(E), h[0].style.zIndex = 1 - Math.abs(Math.round(m)), n.slideShadows) {
          var C = s ? h.find(".swiper-slide-shadow-left") : h.find(".swiper-slide-shadow-top"),
              S = s ? h.find(".swiper-slide-shadow-right") : h.find(".swiper-slide-shadow-bottom");
          0 === C.length && (C = v('<div class="swiper-slide-shadow-' + (s ? "left" : "top") + '"></div>'), h.append(C)), 0 === S.length && (S = v('<div class="swiper-slide-shadow-' + (s ? "right" : "bottom") + '"></div>'), h.append(S)), C.length && (C[0].style.opacity = m > 0 ? m : 0), S.length && (S[0].style.opacity = -m > 0 ? -m : 0);
        }
      }
    },
    setTransition: function setTransition(e) {
      this.slides.transition(e).find(".swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left").transition(e);
    }
  },
      pe = {
    init: function init() {
      var e = this,
          t = e.params.thumbs;
      if (e.thumbs.initialized) return !1;
      e.thumbs.initialized = !0;
      var i = e.constructor;
      return t.swiper instanceof i ? (e.thumbs.swiper = t.swiper, C(e.thumbs.swiper.originalParams, {
        watchSlidesProgress: !0,
        slideToClickedSlide: !1
      }), C(e.thumbs.swiper.params, {
        watchSlidesProgress: !0,
        slideToClickedSlide: !1
      })) : E(t.swiper) && (e.thumbs.swiper = new i(C({}, t.swiper, {
        watchSlidesVisibility: !0,
        watchSlidesProgress: !0,
        slideToClickedSlide: !1
      })), e.thumbs.swiperCreated = !0), e.thumbs.swiper.$el.addClass(e.params.thumbs.thumbsContainerClass), e.thumbs.swiper.on("tap", e.thumbs.onThumbClick), !0;
    },
    onThumbClick: function onThumbClick() {
      var e = this,
          t = e.thumbs.swiper;

      if (t) {
        var i = t.clickedIndex,
            a = t.clickedSlide;

        if (!(a && v(a).hasClass(e.params.thumbs.slideThumbActiveClass) || null == i)) {
          var r;

          if (r = t.params.loop ? parseInt(v(t.clickedSlide).attr("data-swiper-slide-index"), 10) : i, e.params.loop) {
            var n = e.activeIndex;
            e.slides.eq(n).hasClass(e.params.slideDuplicateClass) && (e.loopFix(), e._clientLeft = e.$wrapperEl[0].clientLeft, n = e.activeIndex);
            var s = e.slides.eq(n).prevAll('[data-swiper-slide-index="' + r + '"]').eq(0).index(),
                o = e.slides.eq(n).nextAll('[data-swiper-slide-index="' + r + '"]').eq(0).index();
            r = void 0 === s ? o : void 0 === o ? s : o - n < n - s ? o : s;
          }

          e.slideTo(r);
        }
      }
    },
    update: function update(e) {
      var t = this,
          i = t.thumbs.swiper;

      if (i) {
        var a = "auto" === i.params.slidesPerView ? i.slidesPerViewDynamic() : i.params.slidesPerView,
            r = t.params.thumbs.autoScrollOffset,
            n = r && !i.params.loop;

        if (t.realIndex !== i.realIndex || n) {
          var s,
              o,
              l = i.activeIndex;

          if (i.params.loop) {
            i.slides.eq(l).hasClass(i.params.slideDuplicateClass) && (i.loopFix(), i._clientLeft = i.$wrapperEl[0].clientLeft, l = i.activeIndex);
            var d = i.slides.eq(l).prevAll('[data-swiper-slide-index="' + t.realIndex + '"]').eq(0).index(),
                u = i.slides.eq(l).nextAll('[data-swiper-slide-index="' + t.realIndex + '"]').eq(0).index();
            s = void 0 === d ? u : void 0 === u ? d : u - l == l - d ? l : u - l < l - d ? u : d, o = t.activeIndex > t.previousIndex ? "next" : "prev";
          } else o = (s = t.realIndex) > t.previousIndex ? "next" : "prev";

          n && (s += "next" === o ? r : -1 * r), i.visibleSlidesIndexes && i.visibleSlidesIndexes.indexOf(s) < 0 && (i.params.centeredSlides ? s = s > l ? s - Math.floor(a / 2) + 1 : s + Math.floor(a / 2) - 1 : s > l && (s = s - a + 1), i.slideTo(s, e ? 0 : void 0));
        }

        var p = 1,
            c = t.params.thumbs.slideThumbActiveClass;
        if (t.params.slidesPerView > 1 && !t.params.centeredSlides && (p = t.params.slidesPerView), t.params.thumbs.multipleActiveThumbs || (p = 1), p = Math.floor(p), i.slides.removeClass(c), i.params.loop || i.params.virtual && i.params.virtual.enabled) for (var h = 0; h < p; h += 1) {
          i.$wrapperEl.children('[data-swiper-slide-index="' + (t.realIndex + h) + '"]').addClass(c);
        } else for (var f = 0; f < p; f += 1) {
          i.slides.eq(t.realIndex + f).addClass(c);
        }
      }
    }
  },
      ce = [W, j, {
    name: "mousewheel",
    params: {
      mousewheel: {
        enabled: !1,
        releaseOnEdges: !1,
        invert: !1,
        forceToAxis: !1,
        sensitivity: 1,
        eventsTarget: "container",
        thresholdDelta: null,
        thresholdTime: null
      }
    },
    create: function create() {
      S(this, {
        mousewheel: {
          enabled: !1,
          lastScrollTime: T(),
          lastEventBeforeSnap: void 0,
          recentWheelEvents: [],
          enable: U.enable,
          disable: U.disable,
          handle: U.handle,
          handleMouseEnter: U.handleMouseEnter,
          handleMouseLeave: U.handleMouseLeave,
          animateSlider: U.animateSlider,
          releaseScroll: U.releaseScroll
        }
      });
    },
    on: {
      init: function init(e) {
        !e.params.mousewheel.enabled && e.params.cssMode && e.mousewheel.disable(), e.params.mousewheel.enabled && e.mousewheel.enable();
      },
      destroy: function destroy(e) {
        e.params.cssMode && e.mousewheel.enable(), e.mousewheel.enabled && e.mousewheel.disable();
      }
    }
  }, {
    name: "navigation",
    params: {
      navigation: {
        nextEl: null,
        prevEl: null,
        hideOnClick: !1,
        disabledClass: "swiper-button-disabled",
        hiddenClass: "swiper-button-hidden",
        lockClass: "swiper-button-lock"
      }
    },
    create: function create() {
      S(this, {
        navigation: t({}, K)
      });
    },
    on: {
      init: function init(e) {
        e.navigation.init(), e.navigation.update();
      },
      toEdge: function toEdge(e) {
        e.navigation.update();
      },
      fromEdge: function fromEdge(e) {
        e.navigation.update();
      },
      destroy: function destroy(e) {
        e.navigation.destroy();
      },
      click: function click(e, t) {
        var i = e.navigation,
            a = i.$nextEl,
            r = i.$prevEl,
            n = t.target;

        if (e.params.navigation.hideOnClick && !v(n).is(r) && !v(n).is(a)) {
          if (e.pagination && e.params.pagination && e.params.pagination.clickable && (e.pagination.el === n || e.pagination.el.contains(n))) return;
          var s;
          a ? s = a.hasClass(e.params.navigation.hiddenClass) : r && (s = r.hasClass(e.params.navigation.hiddenClass)), !0 === s ? e.emit("navigationShow") : e.emit("navigationHide"), a && a.toggleClass(e.params.navigation.hiddenClass), r && r.toggleClass(e.params.navigation.hiddenClass);
        }
      }
    }
  }, {
    name: "pagination",
    params: {
      pagination: {
        el: null,
        bulletElement: "span",
        clickable: !1,
        hideOnClick: !1,
        renderBullet: null,
        renderProgressbar: null,
        renderFraction: null,
        renderCustom: null,
        progressbarOpposite: !1,
        type: "bullets",
        dynamicBullets: !1,
        dynamicMainBullets: 1,
        formatFractionCurrent: function formatFractionCurrent(e) {
          return e;
        },
        formatFractionTotal: function formatFractionTotal(e) {
          return e;
        },
        bulletClass: "swiper-pagination-bullet",
        bulletActiveClass: "swiper-pagination-bullet-active",
        modifierClass: "swiper-pagination-",
        currentClass: "swiper-pagination-current",
        totalClass: "swiper-pagination-total",
        hiddenClass: "swiper-pagination-hidden",
        progressbarFillClass: "swiper-pagination-progressbar-fill",
        progressbarOppositeClass: "swiper-pagination-progressbar-opposite",
        clickableClass: "swiper-pagination-clickable",
        lockClass: "swiper-pagination-lock"
      }
    },
    create: function create() {
      S(this, {
        pagination: t({
          dynamicBulletIndex: 0
        }, Q)
      });
    },
    on: {
      init: function init(e) {
        e.pagination.init(), e.pagination.render(), e.pagination.update();
      },
      activeIndexChange: function activeIndexChange(e) {
        (e.params.loop || void 0 === e.snapIndex) && e.pagination.update();
      },
      snapIndexChange: function snapIndexChange(e) {
        e.params.loop || e.pagination.update();
      },
      slidesLengthChange: function slidesLengthChange(e) {
        e.params.loop && (e.pagination.render(), e.pagination.update());
      },
      snapGridLengthChange: function snapGridLengthChange(e) {
        e.params.loop || (e.pagination.render(), e.pagination.update());
      },
      destroy: function destroy(e) {
        e.pagination.destroy();
      },
      click: function click(e, t) {
        var i = t.target;

        if (e.params.pagination.el && e.params.pagination.hideOnClick && e.pagination.$el.length > 0 && !v(i).hasClass(e.params.pagination.bulletClass)) {
          if (e.navigation && (e.navigation.nextEl && i === e.navigation.nextEl || e.navigation.prevEl && i === e.navigation.prevEl)) return;
          !0 === e.pagination.$el.hasClass(e.params.pagination.hiddenClass) ? e.emit("paginationShow") : e.emit("paginationHide"), e.pagination.$el.toggleClass(e.params.pagination.hiddenClass);
        }
      }
    }
  }, {
    name: "scrollbar",
    params: {
      scrollbar: {
        el: null,
        dragSize: "auto",
        hide: !1,
        draggable: !1,
        snapOnRelease: !0,
        lockClass: "swiper-scrollbar-lock",
        dragClass: "swiper-scrollbar-drag"
      }
    },
    create: function create() {
      S(this, {
        scrollbar: t({
          isTouched: !1,
          timeout: null,
          dragTimeout: null
        }, Z)
      });
    },
    on: {
      init: function init(e) {
        e.scrollbar.init(), e.scrollbar.updateSize(), e.scrollbar.setTranslate();
      },
      update: function update(e) {
        e.scrollbar.updateSize();
      },
      resize: function resize(e) {
        e.scrollbar.updateSize();
      },
      observerUpdate: function observerUpdate(e) {
        e.scrollbar.updateSize();
      },
      setTranslate: function setTranslate(e) {
        e.scrollbar.setTranslate();
      },
      setTransition: function setTransition(e, t) {
        e.scrollbar.setTransition(t);
      },
      destroy: function destroy(e) {
        e.scrollbar.destroy();
      }
    }
  }, {
    name: "parallax",
    params: {
      parallax: {
        enabled: !1
      }
    },
    create: function create() {
      S(this, {
        parallax: t({}, J)
      });
    },
    on: {
      beforeInit: function beforeInit(e) {
        e.params.parallax.enabled && (e.params.watchSlidesProgress = !0, e.originalParams.watchSlidesProgress = !0);
      },
      init: function init(e) {
        e.params.parallax.enabled && e.parallax.setTranslate();
      },
      setTranslate: function setTranslate(e) {
        e.params.parallax.enabled && e.parallax.setTranslate();
      },
      setTransition: function setTransition(e, t) {
        e.params.parallax.enabled && e.parallax.setTransition(t);
      }
    }
  }, {
    name: "zoom",
    params: {
      zoom: {
        enabled: !1,
        maxRatio: 3,
        minRatio: 1,
        toggle: !0,
        containerClass: "swiper-zoom-container",
        zoomedSlideClass: "swiper-slide-zoomed"
      }
    },
    create: function create() {
      var e = this;
      S(e, {
        zoom: t({
          enabled: !1,
          scale: 1,
          currentScale: 1,
          isScaling: !1,
          gesture: {
            $slideEl: void 0,
            slideWidth: void 0,
            slideHeight: void 0,
            $imageEl: void 0,
            $imageWrapEl: void 0,
            maxRatio: 3
          },
          image: {
            isTouched: void 0,
            isMoved: void 0,
            currentX: void 0,
            currentY: void 0,
            minX: void 0,
            minY: void 0,
            maxX: void 0,
            maxY: void 0,
            width: void 0,
            height: void 0,
            startX: void 0,
            startY: void 0,
            touchesStart: {},
            touchesCurrent: {}
          },
          velocity: {
            x: void 0,
            y: void 0,
            prevPositionX: void 0,
            prevPositionY: void 0,
            prevTime: void 0
          }
        }, ee)
      });
      var i = 1;
      Object.defineProperty(e.zoom, "scale", {
        get: function get() {
          return i;
        },
        set: function set(t) {
          if (i !== t) {
            var a = e.zoom.gesture.$imageEl ? e.zoom.gesture.$imageEl[0] : void 0,
                r = e.zoom.gesture.$slideEl ? e.zoom.gesture.$slideEl[0] : void 0;
            e.emit("zoomChange", t, a, r);
          }

          i = t;
        }
      });
    },
    on: {
      init: function init(e) {
        e.params.zoom.enabled && e.zoom.enable();
      },
      destroy: function destroy(e) {
        e.zoom.disable();
      },
      touchStart: function touchStart(e, t) {
        e.zoom.enabled && e.zoom.onTouchStart(t);
      },
      touchEnd: function touchEnd(e, t) {
        e.zoom.enabled && e.zoom.onTouchEnd(t);
      },
      doubleTap: function doubleTap(e, t) {
        !e.animating && e.params.zoom.enabled && e.zoom.enabled && e.params.zoom.toggle && e.zoom.toggle(t);
      },
      transitionEnd: function transitionEnd(e) {
        e.zoom.enabled && e.params.zoom.enabled && e.zoom.onTransitionEnd();
      },
      slideChange: function slideChange(e) {
        e.zoom.enabled && e.params.zoom.enabled && e.params.cssMode && e.zoom.onTransitionEnd();
      }
    }
  }, {
    name: "lazy",
    params: {
      lazy: {
        checkInView: !1,
        enabled: !1,
        loadPrevNext: !1,
        loadPrevNextAmount: 1,
        loadOnTransitionStart: !1,
        scrollingElement: "",
        elementClass: "swiper-lazy",
        loadingClass: "swiper-lazy-loading",
        loadedClass: "swiper-lazy-loaded",
        preloaderClass: "swiper-lazy-preloader"
      }
    },
    create: function create() {
      S(this, {
        lazy: t({
          initialImageLoaded: !1
        }, te)
      });
    },
    on: {
      beforeInit: function beforeInit(e) {
        e.params.lazy.enabled && e.params.preloadImages && (e.params.preloadImages = !1);
      },
      init: function init(e) {
        e.params.lazy.enabled && !e.params.loop && 0 === e.params.initialSlide && (e.params.lazy.checkInView ? e.lazy.checkInViewOnLoad() : e.lazy.load());
      },
      scroll: function scroll(e) {
        e.params.freeMode && !e.params.freeModeSticky && e.lazy.load();
      },
      "scrollbarDragMove resize _freeModeNoMomentumRelease": function scrollbarDragMoveResize_freeModeNoMomentumRelease(e) {
        e.params.lazy.enabled && e.lazy.load();
      },
      transitionStart: function transitionStart(e) {
        e.params.lazy.enabled && (e.params.lazy.loadOnTransitionStart || !e.params.lazy.loadOnTransitionStart && !e.lazy.initialImageLoaded) && e.lazy.load();
      },
      transitionEnd: function transitionEnd(e) {
        e.params.lazy.enabled && !e.params.lazy.loadOnTransitionStart && e.lazy.load();
      },
      slideChange: function slideChange(e) {
        e.params.lazy.enabled && e.params.cssMode && e.lazy.load();
      }
    }
  }, {
    name: "controller",
    params: {
      controller: {
        control: void 0,
        inverse: !1,
        by: "slide"
      }
    },
    create: function create() {
      S(this, {
        controller: t({
          control: this.params.controller.control
        }, ie)
      });
    },
    on: {
      update: function update(e) {
        e.controller.control && e.controller.spline && (e.controller.spline = void 0, delete e.controller.spline);
      },
      resize: function resize(e) {
        e.controller.control && e.controller.spline && (e.controller.spline = void 0, delete e.controller.spline);
      },
      observerUpdate: function observerUpdate(e) {
        e.controller.control && e.controller.spline && (e.controller.spline = void 0, delete e.controller.spline);
      },
      setTranslate: function setTranslate(e, t, i) {
        e.controller.control && e.controller.setTranslate(t, i);
      },
      setTransition: function setTransition(e, t, i) {
        e.controller.control && e.controller.setTransition(t, i);
      }
    }
  }, {
    name: "a11y",
    params: {
      a11y: {
        enabled: !0,
        notificationClass: "swiper-notification",
        prevSlideMessage: "Previous slide",
        nextSlideMessage: "Next slide",
        firstSlideMessage: "This is the first slide",
        lastSlideMessage: "This is the last slide",
        paginationBulletMessage: "Go to slide {{index}}",
        containerMessage: null,
        containerRoleDescriptionMessage: null,
        itemRoleDescriptionMessage: null
      }
    },
    create: function create() {
      S(this, {
        a11y: t({}, ae, {
          liveRegion: v('<span class="' + this.params.a11y.notificationClass + '" aria-live="assertive" aria-atomic="true"></span>')
        })
      });
    },
    on: {
      afterInit: function afterInit(e) {
        e.params.a11y.enabled && (e.a11y.init(), e.a11y.updateNavigation());
      },
      toEdge: function toEdge(e) {
        e.params.a11y.enabled && e.a11y.updateNavigation();
      },
      fromEdge: function fromEdge(e) {
        e.params.a11y.enabled && e.a11y.updateNavigation();
      },
      paginationUpdate: function paginationUpdate(e) {
        e.params.a11y.enabled && e.a11y.updatePagination();
      },
      destroy: function destroy(e) {
        e.params.a11y.enabled && e.a11y.destroy();
      }
    }
  }, {
    name: "history",
    params: {
      history: {
        enabled: !1,
        replaceState: !1,
        key: "slides"
      }
    },
    create: function create() {
      S(this, {
        history: t({}, re)
      });
    },
    on: {
      init: function init(e) {
        e.params.history.enabled && e.history.init();
      },
      destroy: function destroy(e) {
        e.params.history.enabled && e.history.destroy();
      },
      transitionEnd: function transitionEnd(e) {
        e.history.initialized && e.history.setHistory(e.params.history.key, e.activeIndex);
      },
      slideChange: function slideChange(e) {
        e.history.initialized && e.params.cssMode && e.history.setHistory(e.params.history.key, e.activeIndex);
      }
    }
  }, {
    name: "hash-navigation",
    params: {
      hashNavigation: {
        enabled: !1,
        replaceState: !1,
        watchState: !1
      }
    },
    create: function create() {
      S(this, {
        hashNavigation: t({
          initialized: !1
        }, ne)
      });
    },
    on: {
      init: function init(e) {
        e.params.hashNavigation.enabled && e.hashNavigation.init();
      },
      destroy: function destroy(e) {
        e.params.hashNavigation.enabled && e.hashNavigation.destroy();
      },
      transitionEnd: function transitionEnd(e) {
        e.hashNavigation.initialized && e.hashNavigation.setHash();
      },
      slideChange: function slideChange(e) {
        e.hashNavigation.initialized && e.params.cssMode && e.hashNavigation.setHash();
      }
    }
  }, {
    name: "autoplay",
    params: {
      autoplay: {
        enabled: !1,
        delay: 3e3,
        waitForTransition: !0,
        disableOnInteraction: !0,
        stopOnLastSlide: !1,
        reverseDirection: !1
      }
    },
    create: function create() {
      S(this, {
        autoplay: t({}, se, {
          running: !1,
          paused: !1
        })
      });
    },
    on: {
      init: function init(e) {
        e.params.autoplay.enabled && (e.autoplay.start(), n().addEventListener("visibilitychange", e.autoplay.onVisibilityChange));
      },
      beforeTransitionStart: function beforeTransitionStart(e, t, i) {
        e.autoplay.running && (i || !e.params.autoplay.disableOnInteraction ? e.autoplay.pause(t) : e.autoplay.stop());
      },
      sliderFirstMove: function sliderFirstMove(e) {
        e.autoplay.running && (e.params.autoplay.disableOnInteraction ? e.autoplay.stop() : e.autoplay.pause());
      },
      touchEnd: function touchEnd(e) {
        e.params.cssMode && e.autoplay.paused && !e.params.autoplay.disableOnInteraction && e.autoplay.run();
      },
      destroy: function destroy(e) {
        e.autoplay.running && e.autoplay.stop(), n().removeEventListener("visibilitychange", e.autoplay.onVisibilityChange);
      }
    }
  }, {
    name: "effect-fade",
    params: {
      fadeEffect: {
        crossFade: !1
      }
    },
    create: function create() {
      S(this, {
        fadeEffect: t({}, oe)
      });
    },
    on: {
      beforeInit: function beforeInit(e) {
        if ("fade" === e.params.effect) {
          e.classNames.push(e.params.containerModifierClass + "fade");
          var t = {
            slidesPerView: 1,
            slidesPerColumn: 1,
            slidesPerGroup: 1,
            watchSlidesProgress: !0,
            spaceBetween: 0,
            virtualTranslate: !0
          };
          C(e.params, t), C(e.originalParams, t);
        }
      },
      setTranslate: function setTranslate(e) {
        "fade" === e.params.effect && e.fadeEffect.setTranslate();
      },
      setTransition: function setTransition(e, t) {
        "fade" === e.params.effect && e.fadeEffect.setTransition(t);
      }
    }
  }, {
    name: "effect-cube",
    params: {
      cubeEffect: {
        slideShadows: !0,
        shadow: !0,
        shadowOffset: 20,
        shadowScale: .94
      }
    },
    create: function create() {
      S(this, {
        cubeEffect: t({}, le)
      });
    },
    on: {
      beforeInit: function beforeInit(e) {
        if ("cube" === e.params.effect) {
          e.classNames.push(e.params.containerModifierClass + "cube"), e.classNames.push(e.params.containerModifierClass + "3d");
          var t = {
            slidesPerView: 1,
            slidesPerColumn: 1,
            slidesPerGroup: 1,
            watchSlidesProgress: !0,
            resistanceRatio: 0,
            spaceBetween: 0,
            centeredSlides: !1,
            virtualTranslate: !0
          };
          C(e.params, t), C(e.originalParams, t);
        }
      },
      setTranslate: function setTranslate(e) {
        "cube" === e.params.effect && e.cubeEffect.setTranslate();
      },
      setTransition: function setTransition(e, t) {
        "cube" === e.params.effect && e.cubeEffect.setTransition(t);
      }
    }
  }, {
    name: "effect-flip",
    params: {
      flipEffect: {
        slideShadows: !0,
        limitRotation: !0
      }
    },
    create: function create() {
      S(this, {
        flipEffect: t({}, de)
      });
    },
    on: {
      beforeInit: function beforeInit(e) {
        if ("flip" === e.params.effect) {
          e.classNames.push(e.params.containerModifierClass + "flip"), e.classNames.push(e.params.containerModifierClass + "3d");
          var t = {
            slidesPerView: 1,
            slidesPerColumn: 1,
            slidesPerGroup: 1,
            watchSlidesProgress: !0,
            spaceBetween: 0,
            virtualTranslate: !0
          };
          C(e.params, t), C(e.originalParams, t);
        }
      },
      setTranslate: function setTranslate(e) {
        "flip" === e.params.effect && e.flipEffect.setTranslate();
      },
      setTransition: function setTransition(e, t) {
        "flip" === e.params.effect && e.flipEffect.setTransition(t);
      }
    }
  }, {
    name: "effect-coverflow",
    params: {
      coverflowEffect: {
        rotate: 50,
        stretch: 0,
        depth: 100,
        scale: 1,
        modifier: 1,
        slideShadows: !0
      }
    },
    create: function create() {
      S(this, {
        coverflowEffect: t({}, ue)
      });
    },
    on: {
      beforeInit: function beforeInit(e) {
        "coverflow" === e.params.effect && (e.classNames.push(e.params.containerModifierClass + "coverflow"), e.classNames.push(e.params.containerModifierClass + "3d"), e.params.watchSlidesProgress = !0, e.originalParams.watchSlidesProgress = !0);
      },
      setTranslate: function setTranslate(e) {
        "coverflow" === e.params.effect && e.coverflowEffect.setTranslate();
      },
      setTransition: function setTransition(e, t) {
        "coverflow" === e.params.effect && e.coverflowEffect.setTransition(t);
      }
    }
  }, {
    name: "thumbs",
    params: {
      thumbs: {
        swiper: null,
        multipleActiveThumbs: !0,
        autoScrollOffset: 0,
        slideThumbActiveClass: "swiper-slide-thumb-active",
        thumbsContainerClass: "swiper-container-thumbs"
      }
    },
    create: function create() {
      S(this, {
        thumbs: t({
          swiper: null,
          initialized: !1
        }, pe)
      });
    },
    on: {
      beforeInit: function beforeInit(e) {
        var t = e.params.thumbs;
        t && t.swiper && (e.thumbs.init(), e.thumbs.update(!0));
      },
      slideChange: function slideChange(e) {
        e.thumbs.swiper && e.thumbs.update();
      },
      update: function update(e) {
        e.thumbs.swiper && e.thumbs.update();
      },
      resize: function resize(e) {
        e.thumbs.swiper && e.thumbs.update();
      },
      observerUpdate: function observerUpdate(e) {
        e.thumbs.swiper && e.thumbs.update();
      },
      setTransition: function setTransition(e, t) {
        var i = e.thumbs.swiper;
        i && i.setTransition(t);
      },
      beforeDestroy: function beforeDestroy(e) {
        var t = e.thumbs.swiper;
        t && e.thumbs.swiperCreated && t && t.destroy();
      }
    }
  }];
  return H.use(ce), H;
}), function (e, t) {
  "object" == (typeof exports === "undefined" ? "undefined" : _typeof(exports)) && "undefined" != typeof module ? t(exports) : "function" == typeof define && define.amd ? define(["exports"], t) : t((e = e || self).window = e.window || {});
}(void 0, function (e) {
  "use strict";

  function t(e, t) {
    e.prototype = Object.create(t.prototype), (e.prototype.constructor = e).__proto__ = t;
  }

  function i(e) {
    if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
    return e;
  }

  function a(e) {
    return "string" == typeof e;
  }

  function r(e) {
    return "function" == typeof e;
  }

  function n(e) {
    return "number" == typeof e;
  }

  function s(e) {
    return void 0 === e;
  }

  function o(e) {
    return "object" == _typeof(e);
  }

  function l(e) {
    return !1 !== e;
  }

  function d() {
    return "undefined" != typeof window;
  }

  function u(e) {
    return r(e) || a(e);
  }

  function p(e) {
    return (we = ft(e, nt)) && ci;
  }

  function c(e, t) {
    return console.warn("Invalid property", e, "set to", t, "Missing plugin? gsap.registerPlugin()");
  }

  function h(e, t) {
    return !t && console.warn(e);
  }

  function f(e, t) {
    return e && (nt[e] = t) && we && (we[e] = t) || nt;
  }

  function m() {
    return 0;
  }

  function v(e) {
    var t,
        i,
        a = e[0];

    if (o(a) || r(a) || (e = [e]), !(t = (a._gsap || {}).harness)) {
      for (i = ct.length; i-- && !ct[i].targetTest(a);) {
        ;
      }

      t = ct[i];
    }

    for (i = e.length; i--;) {
      e[i] && (e[i]._gsap || (e[i]._gsap = new It(e[i], t))) || e.splice(i, 1);
    }

    return e;
  }

  function g(e) {
    return e._gsap || v(wt(e))[0]._gsap;
  }

  function y(e, t, i) {
    return (i = e[t]) && r(i) ? e[t]() : s(i) && e.getAttribute && e.getAttribute(t) || i;
  }

  function w(e, t) {
    return (e = e.split(",")).forEach(t) || e;
  }

  function b(e) {
    return Math.round(1e5 * e) / 1e5 || 0;
  }

  function x(e, t) {
    for (var i = t.length, a = 0; e.indexOf(t[a]) < 0 && ++a < i;) {
      ;
    }

    return a < i;
  }

  function T(e, t, i) {
    var a,
        r = n(e[1]),
        s = (r ? 2 : 1) + (t < 2 ? 0 : 1),
        o = e[s];

    if (r && (o.duration = e[1]), o.parent = i, t) {
      for (a = o; i && !("immediateRender" in a);) {
        a = i.vars.defaults || {}, i = l(i.vars.inherit) && i.parent;
      }

      o.immediateRender = l(a.immediateRender), t < 2 ? o.runBackwards = 1 : o.startAt = e[s - 1];
    }

    return o;
  }

  function _() {
    var e,
        t,
        i = ot.length,
        a = ot.slice(0);

    for (lt = {}, e = ot.length = 0; e < i; e++) {
      (t = a[e]) && t._lazy && (t.render(t._lazy[0], t._lazy[1], !0)._lazy = 0);
    }
  }

  function E(e, t, i, a) {
    ot.length && _(), e.render(t, i, a), ot.length && _();
  }

  function C(e) {
    var t = parseFloat(e);
    return (t || 0 === t) && (e + "").match(at).length < 2 ? t : a(e) ? e.trim() : e;
  }

  function S(e) {
    return e;
  }

  function M(e, t) {
    for (var i in t) {
      i in e || (e[i] = t[i]);
    }

    return e;
  }

  function z(e, t) {
    for (var i in t) {
      i in e || "duration" === i || "ease" === i || (e[i] = t[i]);
    }
  }

  function k(e, t) {
    for (var i in t) {
      "__proto__" !== i && "constructor" !== i && "prototype" !== i && (e[i] = o(t[i]) ? k(e[i] || (e[i] = {}), t[i]) : t[i]);
    }

    return e;
  }

  function P(e, t) {
    var i,
        a = {};

    for (i in e) {
      i in t || (a[i] = e[i]);
    }

    return a;
  }

  function O(e) {
    var t = e.parent || me,
        i = e.keyframes ? z : M;
    if (l(e.inherit)) for (; t;) {
      i(e, t.vars.defaults), t = t.parent || t._dp;
    }
    return e;
  }

  function L(e, t, i, a) {
    void 0 === i && (i = "_first"), void 0 === a && (a = "_last");
    var r = t._prev,
        n = t._next;
    r ? r._next = n : e[i] === t && (e[i] = n), n ? n._prev = r : e[a] === t && (e[a] = r), t._next = t._prev = t.parent = null;
  }

  function A(e, t) {
    !e.parent || t && !e.parent.autoRemoveChildren || e.parent.remove(e), e._act = 0;
  }

  function $(e, t) {
    if (e && (!t || t._end > e._dur || t._start < 0)) for (var i = e; i;) {
      i._dirty = 1, i = i.parent;
    }
    return e;
  }

  function D(e) {
    return e._repeat ? mt(e._tTime, e = e.duration() + e._rDelay) * e : 0;
  }

  function I(e, t) {
    return (e - t._start) * t._ts + (0 <= t._ts ? 0 : t._dirty ? t.totalDuration() : t._tDur);
  }

  function B(e) {
    return e._end = b(e._start + (e._tDur / Math.abs(e._ts || e._rts || Ye) || 0));
  }

  function N(e, t) {
    var i = e._dp;
    return i && i.smoothChildTiming && e._ts && (e._start = b(i._time - (0 < e._ts ? t / e._ts : ((e._dirty ? e.totalDuration() : e._tDur) - t) / -e._ts)), B(e), i._dirty || $(i, e)), e;
  }

  function R(e, t) {
    var i;

    if ((t._time || t._initted && !t._dur) && (i = I(e.rawTime(), t), (!t._dur || gt(0, t.totalDuration(), i) - t._tTime > Ye) && t.render(i, !0)), $(e, t)._dp && e._initted && e._time >= e._dur && e._ts) {
      if (e._dur < e.duration()) for (i = e; i._dp;) {
        0 <= i.rawTime() && i.totalTime(i._tTime), i = i._dp;
      }
      e._zTime = -Ye;
    }
  }

  function G(e, t, i, a) {
    return t.parent && A(t), t._start = b(i + t._delay), t._end = b(t._start + (t.totalDuration() / Math.abs(t.timeScale()) || 0)), function (e, t, i, a, r) {
      void 0 === i && (i = "_first"), void 0 === a && (a = "_last");
      var n,
          s = e[a];
      if (r) for (n = t[r]; s && s[r] > n;) {
        s = s._prev;
      }
      s ? (t._next = s._next, s._next = t) : (t._next = e[i], e[i] = t), t._next ? t._next._prev = t : e[a] = t, t._prev = s, t.parent = t._dp = e;
    }(e, t, "_first", "_last", e._sort ? "_start" : 0), e._recent = t, a || R(e, t), e;
  }

  function F(e, t) {
    return (nt.ScrollTrigger || c("scrollTrigger", t)) && nt.ScrollTrigger.create(t, e);
  }

  function X(e, t, i, a) {
    return Yt(e, t), e._initted ? !i && e._pt && (e._dur && !1 !== e.vars.lazy || !e._dur && e.vars.lazy) && xe !== St.frame ? (ot.push(e), e._lazy = [t, a], 1) : void 0 : 1;
  }

  function Y(e, t, i, a) {
    var r = e._repeat,
        n = b(t) || 0,
        s = e._tTime / e._tDur;
    return s && !a && (e._time *= n / e._dur), e._dur = n, e._tDur = r ? r < 0 ? 1e10 : b(n * (r + 1) + e._rDelay * r) : n, s && !a ? N(e, e._tTime = e._tDur * s) : e.parent && B(e), i || $(e.parent, e), e;
  }

  function H(e) {
    return e instanceof Rt ? $(e) : Y(e, e._dur);
  }

  function V(e, t) {
    var i,
        r,
        n = e.labels,
        s = e._recent || vt,
        o = e.duration() >= Xe ? s.endTime(!1) : e._dur;
    return a(t) && (isNaN(t) || t in n) ? "<" === (i = t.charAt(0)) || ">" === i ? ("<" === i ? s._start : s.endTime(0 <= s._repeat)) + (parseFloat(t.substr(1)) || 0) : (i = t.indexOf("=")) < 0 ? (t in n || (n[t] = o), n[t]) : (r = +(t.charAt(i - 1) + t.substr(i + 1)), 1 < i ? V(e, t.substr(0, i - 1)) + r : o + r) : null == t ? o : +t;
  }

  function W(e, t) {
    return e || 0 === e ? t(e) : t;
  }

  function q(e) {
    if ("string" != typeof e) return "";
    var t = rt.exec(e);
    return t ? e.substr(t.index + t[0].length) : "";
  }

  function j(e, t) {
    return e && o(e) && "length" in e && (!t && !e.length || e.length - 1 in e && o(e[0])) && !e.nodeType && e !== ve;
  }

  function U(e) {
    return e.sort(function () {
      return .5 - Math.random();
    });
  }

  function K(e) {
    if (r(e)) return e;
    var t = o(e) ? e : {
      each: e
    },
        i = Lt(t.ease),
        n = t.from || 0,
        s = parseFloat(t.base) || 0,
        l = {},
        d = 0 < n && n < 1,
        u = isNaN(n) || d,
        p = t.axis,
        c = n,
        h = n;
    return a(n) ? c = h = {
      center: .5,
      edges: .5,
      end: 1
    }[n] || 0 : !d && u && (c = n[0], h = n[1]), function (e, a, r) {
      var o,
          d,
          f,
          m,
          v,
          g,
          y,
          w,
          x,
          T = (r || t).length,
          _ = l[T];

      if (!_) {
        if (!(x = "auto" === t.grid ? 0 : (t.grid || [1, Xe])[1])) {
          for (y = -Xe; y < (y = r[x++].getBoundingClientRect().left) && x < T;) {
            ;
          }

          x--;
        }

        for (_ = l[T] = [], o = u ? Math.min(x, T) * c - .5 : n % x, d = u ? T * h / x - .5 : n / x | 0, w = Xe, g = y = 0; g < T; g++) {
          f = g % x - o, m = d - (g / x | 0), _[g] = v = p ? Math.abs("y" === p ? m : f) : qe(f * f + m * m), y < v && (y = v), v < w && (w = v);
        }

        "random" === n && U(_), _.max = y - w, _.min = w, _.v = T = (parseFloat(t.amount) || parseFloat(t.each) * (T < x ? T - 1 : p ? "y" === p ? T / x : x : Math.max(x, T / x)) || 0) * ("edges" === n ? -1 : 1), _.b = T < 0 ? s - T : s, _.u = q(t.amount || t.each) || 0, i = i && T < 0 ? Ot(i) : i;
      }

      return T = (_[e] - _.min) / _.max || 0, b(_.b + (i ? i(T) : T) * _.v) + _.u;
    };
  }

  function Q(e) {
    var t = e < 1 ? Math.pow(10, (e + "").length - 2) : 1;
    return function (i) {
      var a = Math.round(parseFloat(i) / e) * e * t;
      return (a - a % 1) / t + (n(i) ? 0 : q(i));
    };
  }

  function Z(e, t) {
    var i,
        a,
        s = Qe(e);
    return !s && o(e) && (i = s = e.radius || Xe, e.values ? (e = wt(e.values), (a = !n(e[0])) && (i *= i)) : e = Q(e.increment)), W(t, s ? r(e) ? function (t) {
      return a = e(t), Math.abs(a - t) <= i ? a : t;
    } : function (t) {
      for (var r, s, o = parseFloat(a ? t.x : t), l = parseFloat(a ? t.y : 0), d = Xe, u = 0, p = e.length; p--;) {
        (r = a ? (r = e[p].x - o) * r + (s = e[p].y - l) * s : Math.abs(e[p] - o)) < d && (d = r, u = p);
      }

      return u = !i || d <= i ? e[u] : t, a || u === t || n(t) ? u : u + q(t);
    } : Q(e));
  }

  function J(e, t, i, a) {
    return W(Qe(e) ? !t : !0 === i ? !!(i = 0) : !a, function () {
      return Qe(e) ? e[~~(Math.random() * e.length)] : (i = i || 1e-5) && (a = i < 1 ? Math.pow(10, (i + "").length - 2) : 1) && Math.floor(Math.round((e - i / 2 + Math.random() * (t - e + .99 * i)) / i) * i * a) / a;
    });
  }

  function ee(e, t, i) {
    return W(i, function (i) {
      return e[~~t(i)];
    });
  }

  function te(e) {
    for (var t, i, a, r, n = 0, s = ""; ~(t = e.indexOf("random(", n));) {
      a = e.indexOf(")", t), r = "[" === e.charAt(t + 7), i = e.substr(t + 7, a - t - 7).match(r ? at : Ze), s += e.substr(n, t - n) + J(r ? i : +i[0], r ? 0 : +i[1], +i[2] || 1e-5), n = a + 1;
    }

    return s + e.substr(n, e.length - n);
  }

  function ie(e, t, i) {
    var a,
        r,
        n,
        s = e.labels,
        o = Xe;

    for (a in s) {
      (r = s[a] - t) < 0 == !!i && r && o > (r = Math.abs(r)) && (n = a, o = r);
    }

    return n;
  }

  function ae(e) {
    return A(e), e.progress() < 1 && xt(e, "onInterrupt"), e;
  }

  function re(e, t, i) {
    return (6 * (e = e < 0 ? e + 1 : 1 < e ? e - 1 : e) < 1 ? t + (i - t) * e * 6 : e < .5 ? i : 3 * e < 2 ? t + (i - t) * (2 / 3 - e) * 6 : t) * Tt + .5 | 0;
  }

  function ne(e, t, i) {
    var a,
        r,
        s,
        o,
        l,
        d,
        u,
        p,
        c,
        h,
        f = e ? n(e) ? [e >> 16, e >> 8 & Tt, e & Tt] : 0 : _t.black;

    if (!f) {
      if ("," === e.substr(-1) && (e = e.substr(0, e.length - 1)), _t[e]) f = _t[e];else if ("#" === e.charAt(0)) {
        if (e.length < 6 && (e = "#" + (a = e.charAt(1)) + a + (r = e.charAt(2)) + r + (s = e.charAt(3)) + s + (5 === e.length ? e.charAt(4) + e.charAt(4) : "")), 9 === e.length) return [(f = parseInt(e.substr(1, 6), 16)) >> 16, f >> 8 & Tt, f & Tt, parseInt(e.substr(7), 16) / 255];
        f = [(e = parseInt(e.substr(1), 16)) >> 16, e >> 8 & Tt, e & Tt];
      } else if ("hsl" === e.substr(0, 3)) {
        if (f = h = e.match(Ze), t) {
          if (~e.indexOf("=")) return f = e.match(Je), i && f.length < 4 && (f[3] = 1), f;
        } else o = +f[0] % 360 / 360, l = f[1] / 100, a = 2 * (d = f[2] / 100) - (r = d <= .5 ? d * (l + 1) : d + l - d * l), 3 < f.length && (f[3] *= 1), f[0] = re(o + 1 / 3, a, r), f[1] = re(o, a, r), f[2] = re(o - 1 / 3, a, r);
      } else f = e.match(Ze) || _t.transparent;
      f = f.map(Number);
    }

    return t && !h && (a = f[0] / Tt, r = f[1] / Tt, s = f[2] / Tt, d = ((u = Math.max(a, r, s)) + (p = Math.min(a, r, s))) / 2, u === p ? o = l = 0 : (c = u - p, l = .5 < d ? c / (2 - u - p) : c / (u + p), o = u === a ? (r - s) / c + (r < s ? 6 : 0) : u === r ? (s - a) / c + 2 : (a - r) / c + 4, o *= 60), f[0] = ~~(o + .5), f[1] = ~~(100 * l + .5), f[2] = ~~(100 * d + .5)), i && f.length < 4 && (f[3] = 1), f;
  }

  function se(e) {
    var t = [],
        i = [],
        a = -1;
    return e.split(Et).forEach(function (e) {
      var r = e.match(et) || [];
      t.push.apply(t, r), i.push(a += r.length + 1);
    }), t.c = i, t;
  }

  function oe(e, t, i) {
    var a,
        r,
        n,
        s,
        o = "",
        l = (e + o).match(Et),
        d = t ? "hsla(" : "rgba(",
        u = 0;
    if (!l) return e;
    if (l = l.map(function (e) {
      return (e = ne(e, t, 1)) && d + (t ? e[0] + "," + e[1] + "%," + e[2] + "%," + e[3] : e.join(",")) + ")";
    }), i && (n = se(e), (a = i.c).join(o) !== n.c.join(o))) for (s = (r = e.replace(Et, "1").split(et)).length - 1; u < s; u++) {
      o += r[u] + (~a.indexOf(u) ? l.shift() || d + "0,0,0,0)" : (n.length ? n : l.length ? l : i).shift());
    }
    if (!r) for (s = (r = e.split(Et)).length - 1; u < s; u++) {
      o += r[u] + l[u];
    }
    return o + r[s];
  }

  function le(e) {
    var t,
        i = e.join(" ");
    if (Et.lastIndex = 0, Et.test(i)) return t = Ct.test(i), e[1] = oe(e[1], t), e[0] = oe(e[0], t, se(e[1])), !0;
  }

  function de(e, t) {
    for (var i, a = e._first; a;) {
      a instanceof Rt ? de(a, t) : !a.vars.yoyoEase || a._yoyo && a._repeat || a._yoyo === t || (a.timeline ? de(a.timeline, t) : (i = a._ease, a._ease = a._yEase, a._yEase = i, a._yoyo = t)), a = a._next;
    }
  }

  function ue(e, t, i, a) {
    void 0 === i && (i = function i(e) {
      return 1 - t(1 - e);
    }), void 0 === a && (a = function a(e) {
      return e < .5 ? t(2 * e) / 2 : 1 - t(2 * (1 - e)) / 2;
    });
    var r,
        n = {
      easeIn: t,
      easeOut: i,
      easeInOut: a
    };
    return w(e, function (e) {
      for (var t in zt[e] = nt[e] = n, zt[r = e.toLowerCase()] = i, n) {
        zt[r + ("easeIn" === t ? ".in" : "easeOut" === t ? ".out" : ".inOut")] = zt[e + "." + t] = n[t];
      }
    }), n;
  }

  function pe(e) {
    return function (t) {
      return t < .5 ? (1 - e(1 - 2 * t)) / 2 : .5 + e(2 * (t - .5)) / 2;
    };
  }

  function ce(e, t, i) {
    function a(e) {
      return 1 === e ? 1 : r * Math.pow(2, -10 * e) * Ue((e - s) * n) + 1;
    }

    var r = 1 <= t ? t : 1,
        n = (i || (e ? .3 : .45)) / (t < 1 ? t : 1),
        s = n / He * (Math.asin(1 / r) || 0),
        o = "out" === e ? a : "in" === e ? function (e) {
      return 1 - a(1 - e);
    } : pe(a);
    return n = He / n, o.config = function (t, i) {
      return ce(e, t, i);
    }, o;
  }

  function he(e, t) {
    function i(e) {
      return e ? --e * e * ((t + 1) * e + t) + 1 : 0;
    }

    void 0 === t && (t = 1.70158);
    var a = "out" === e ? i : "in" === e ? function (e) {
      return 1 - i(1 - e);
    } : pe(i);
    return a.config = function (t) {
      return he(e, t);
    }, a;
  }

  var fe,
      me,
      ve,
      ge,
      ye,
      we,
      be,
      xe,
      Te,
      _e,
      Ee,
      Ce,
      Se,
      Me,
      ze,
      ke,
      Pe,
      Oe,
      Le,
      Ae,
      $e,
      De,
      Ie,
      Be,
      Ne,
      Re,
      Ge = {
    autoSleep: 120,
    force3D: "auto",
    nullTargetWarn: 1,
    units: {
      lineHeight: ""
    }
  },
      Fe = {
    duration: .5,
    overwrite: !1,
    delay: 0
  },
      Xe = 1e8,
      Ye = 1 / Xe,
      He = 2 * Math.PI,
      Ve = He / 4,
      We = 0,
      qe = Math.sqrt,
      je = Math.cos,
      Ue = Math.sin,
      Ke = "function" == typeof ArrayBuffer && ArrayBuffer.isView || function () {},
      Qe = Array.isArray,
      Ze = /(?:-?\.?\d|\.)+/gi,
      Je = /[-+=.]*\d+[.e\-+]*\d*[e\-+]*\d*/g,
      et = /[-+=.]*\d+[.e-]*\d*[a-z%]*/g,
      tt = /[-+=.]*\d+\.?\d*(?:e-|e\+)?\d*/gi,
      it = /[+-]=-?[.\d]+/,
      at = /[#\-+.]*\b[a-z\d-=+%.]+/gi,
      rt = /[\d.+\-=]+(?:e[-+]\d*)*/i,
      nt = {},
      st = {},
      ot = [],
      lt = {},
      dt = {},
      ut = {},
      pt = 30,
      ct = [],
      ht = "",
      ft = function ft(e, t) {
    for (var i in t) {
      e[i] = t[i];
    }

    return e;
  },
      mt = function mt(e, t) {
    var i = Math.floor(e /= t);
    return e && i === e ? i - 1 : i;
  },
      vt = {
    _start: 0,
    endTime: m
  },
      gt = function gt(e, t, i) {
    return i < e ? e : t < i ? t : i;
  },
      yt = [].slice,
      wt = function wt(e, t) {
    return !a(e) || t || !ge && Mt() ? Qe(e) ? function (e, t, i) {
      return void 0 === i && (i = []), e.forEach(function (e) {
        return a(e) && !t || j(e, 1) ? i.push.apply(i, wt(e)) : i.push(e);
      }) || i;
    }(e, t) : j(e) ? yt.call(e, 0) : e ? [e] : [] : yt.call(ye.querySelectorAll(e), 0);
  },
      bt = function bt(e, t, i, a, r) {
    var n = t - e,
        s = a - i;
    return W(r, function (t) {
      return i + ((t - e) / n * s || 0);
    });
  },
      xt = function xt(e, t, i) {
    var a,
        r,
        n = e.vars,
        s = n[t];
    if (s) return a = n[t + "Params"], r = n.callbackScope || e, i && ot.length && _(), a ? s.apply(r, a) : s.call(r);
  },
      Tt = 255,
      _t = {
    aqua: [0, Tt, Tt],
    lime: [0, Tt, 0],
    silver: [192, 192, 192],
    black: [0, 0, 0],
    maroon: [128, 0, 0],
    teal: [0, 128, 128],
    blue: [0, 0, Tt],
    navy: [0, 0, 128],
    white: [Tt, Tt, Tt],
    olive: [128, 128, 0],
    yellow: [Tt, Tt, 0],
    orange: [Tt, 165, 0],
    gray: [128, 128, 128],
    purple: [128, 0, 128],
    green: [0, 128, 0],
    red: [Tt, 0, 0],
    pink: [Tt, 192, 203],
    cyan: [0, Tt, Tt],
    transparent: [Tt, Tt, Tt, 0]
  },
      Et = function () {
    var e,
        t = "(?:\\b(?:(?:rgb|rgba|hsl|hsla)\\(.+?\\))|\\B#(?:[0-9a-f]{3,4}){1,2}\\b";

    for (e in _t) {
      t += "|" + e + "\\b";
    }

    return new RegExp(t + ")", "gi");
  }(),
      Ct = /hsl[a]?\(/,
      St = (Pe = Date.now, Oe = 500, Le = 33, Ae = Pe(), $e = Ae, Ie = De = 1e3 / 240, Me = {
    time: 0,
    frame: 0,
    tick: function tick() {
      At(!0);
    },
    deltaRatio: function deltaRatio(e) {
      return ze / (1e3 / (e || 60));
    },
    wake: function wake() {
      be && (!ge && d() && (ve = ge = window, ye = ve.document || {}, nt.gsap = ci, (ve.gsapVersions || (ve.gsapVersions = [])).push(ci.version), p(we || ve.GreenSockGlobals || !ve.gsap && ve || {}), Se = ve.requestAnimationFrame), Ee && Me.sleep(), Ce = Se || function (e) {
        return setTimeout(e, Ie - 1e3 * Me.time + 1 | 0);
      }, _e = 1, At(2));
    },
    sleep: function sleep() {
      (Se ? ve.cancelAnimationFrame : clearTimeout)(Ee), _e = 0, Ce = m;
    },
    lagSmoothing: function lagSmoothing(e, t) {
      Oe = e || 1e8, Le = Math.min(t, Oe, 0);
    },
    fps: function fps(e) {
      De = 1e3 / (e || 240), Ie = 1e3 * Me.time + De;
    },
    add: function add(e) {
      Be.indexOf(e) < 0 && Be.push(e), Mt();
    },
    remove: function remove(e) {
      var t;
      ~(t = Be.indexOf(e)) && Be.splice(t, 1) && t <= ke && ke--;
    },
    _listeners: Be = []
  }),
      Mt = function Mt() {
    return !_e && St.wake();
  },
      zt = {},
      kt = /^[\d.\-M][\d.\-,\s]/,
      Pt = /["']/g,
      Ot = function Ot(e) {
    return function (t) {
      return 1 - e(1 - t);
    };
  },
      Lt = function Lt(e, t) {
    return e && (r(e) ? e : zt[e] || function (e) {
      var t = (e + "").split("("),
          i = zt[t[0]];
      return i && 1 < t.length && i.config ? i.config.apply(null, ~e.indexOf("{") ? [function (e) {
        for (var t, i, a, r = {}, n = e.substr(1, e.length - 3).split(":"), s = n[0], o = 1, l = n.length; o < l; o++) {
          i = n[o], t = o !== l - 1 ? i.lastIndexOf(",") : i.length, a = i.substr(0, t), r[s] = isNaN(a) ? a.replace(Pt, "").trim() : +a, s = i.substr(t + 1).trim();
        }

        return r;
      }(t[1])] : function (e) {
        var t = e.indexOf("(") + 1,
            i = e.indexOf(")"),
            a = e.indexOf("(", t);
        return e.substring(t, ~a && a < i ? e.indexOf(")", i + 1) : i);
      }(e).split(",").map(C)) : zt._CE && kt.test(e) ? zt._CE("", e) : i;
    }(e)) || t;
  };

  function At(e) {
    var t,
        i,
        a,
        r,
        n = Pe() - $e,
        s = !0 === e;
    if (Oe < n && (Ae += n - Le), (0 < (t = (a = ($e += n) - Ae) - Ie) || s) && (r = ++Me.frame, ze = a - 1e3 * Me.time, Me.time = a /= 1e3, Ie += t + (De <= t ? 4 : De - t), i = 1), s || (Ee = Ce(At)), i) for (ke = 0; ke < Be.length; ke++) {
      Be[ke](a, ze, r, e);
    }
  }

  function $t(e) {
    return e < Re ? Ne * e * e : e < .7272727272727273 ? Ne * Math.pow(e - 1.5 / 2.75, 2) + .75 : e < .9090909090909092 ? Ne * (e -= 2.25 / 2.75) * e + .9375 : Ne * Math.pow(e - 2.625 / 2.75, 2) + .984375;
  }

  w("Linear,Quad,Cubic,Quart,Quint,Strong", function (e, t) {
    var i = t < 5 ? t + 1 : t;
    ue(e + ",Power" + (i - 1), t ? function (e) {
      return Math.pow(e, i);
    } : function (e) {
      return e;
    }, function (e) {
      return 1 - Math.pow(1 - e, i);
    }, function (e) {
      return e < .5 ? Math.pow(2 * e, i) / 2 : 1 - Math.pow(2 * (1 - e), i) / 2;
    });
  }), zt.Linear.easeNone = zt.none = zt.Linear.easeIn, ue("Elastic", ce("in"), ce("out"), ce()), Ne = 7.5625, Re = 1 / 2.75, ue("Bounce", function (e) {
    return 1 - $t(1 - e);
  }, $t), ue("Expo", function (e) {
    return e ? Math.pow(2, 10 * (e - 1)) : 0;
  }), ue("Circ", function (e) {
    return -(qe(1 - e * e) - 1);
  }), ue("Sine", function (e) {
    return 1 === e ? 1 : 1 - je(e * Ve);
  }), ue("Back", he("in"), he("out"), he()), zt.SteppedEase = zt.steps = nt.SteppedEase = {
    config: function config(e, t) {
      void 0 === e && (e = 1);
      var i = 1 / e,
          a = e + (t ? 0 : 1),
          r = t ? 1 : 0;
      return function (e) {
        return ((a * gt(0, .99999999, e) | 0) + r) * i;
      };
    }
  }, Fe.ease = zt["quad.out"], w("onComplete,onUpdate,onStart,onRepeat,onReverseComplete,onInterrupt", function (e) {
    return ht += e + "," + e + "Params,";
  });

  var Dt,
      It = function It(e, t) {
    this.id = We++, (e._gsap = this).target = e, this.harness = t, this.get = t ? t.get : y, this.set = t ? t.getSetter : Jt;
  },
      Bt = ((Dt = Nt.prototype).delay = function (e) {
    return e || 0 === e ? (this.parent && this.parent.smoothChildTiming && this.startTime(this._start + e - this._delay), this._delay = e, this) : this._delay;
  }, Dt.duration = function (e) {
    return arguments.length ? this.totalDuration(0 < this._repeat ? e + (e + this._rDelay) * this._repeat : e) : this.totalDuration() && this._dur;
  }, Dt.totalDuration = function (e) {
    return arguments.length ? (this._dirty = 0, Y(this, this._repeat < 0 ? e : (e - this._repeat * this._rDelay) / (this._repeat + 1))) : this._tDur;
  }, Dt.totalTime = function (e, t) {
    if (Mt(), !arguments.length) return this._tTime;
    var i = this._dp;

    if (i && i.smoothChildTiming && this._ts) {
      for (N(this, e), !i._dp || i.parent || R(i, this); i.parent;) {
        i.parent._time !== i._start + (0 <= i._ts ? i._tTime / i._ts : (i.totalDuration() - i._tTime) / -i._ts) && i.totalTime(i._tTime, !0), i = i.parent;
      }

      !this.parent && this._dp.autoRemoveChildren && (0 < this._ts && e < this._tDur || this._ts < 0 && 0 < e || !this._tDur && !e) && G(this._dp, this, this._start - this._delay);
    }

    return (this._tTime !== e || !this._dur && !t || this._initted && Math.abs(this._zTime) === Ye || !e && !this._initted && (this.add || this._ptLookup)) && (this._ts || (this._pTime = e), E(this, e, t)), this;
  }, Dt.time = function (e, t) {
    return arguments.length ? this.totalTime(Math.min(this.totalDuration(), e + D(this)) % this._dur || (e ? this._dur : 0), t) : this._time;
  }, Dt.totalProgress = function (e, t) {
    return arguments.length ? this.totalTime(this.totalDuration() * e, t) : this.totalDuration() ? Math.min(1, this._tTime / this._tDur) : this.ratio;
  }, Dt.progress = function (e, t) {
    return arguments.length ? this.totalTime(this.duration() * (!this._yoyo || 1 & this.iteration() ? e : 1 - e) + D(this), t) : this.duration() ? Math.min(1, this._time / this._dur) : this.ratio;
  }, Dt.iteration = function (e, t) {
    var i = this.duration() + this._rDelay;

    return arguments.length ? this.totalTime(this._time + (e - 1) * i, t) : this._repeat ? mt(this._tTime, i) + 1 : 1;
  }, Dt.timeScale = function (e) {
    if (!arguments.length) return this._rts === -Ye ? 0 : this._rts;
    if (this._rts === e) return this;
    var t = this.parent && this._ts ? I(this.parent._time, this) : this._tTime;
    return this._rts = +e || 0, this._ts = this._ps || e === -Ye ? 0 : this._rts, function (e) {
      for (var t = e.parent; t && t.parent;) {
        t._dirty = 1, t.totalDuration(), t = t.parent;
      }

      return e;
    }(this.totalTime(gt(-this._delay, this._tDur, t), !0));
  }, Dt.paused = function (e) {
    return arguments.length ? (this._ps !== e && ((this._ps = e) ? (this._pTime = this._tTime || Math.max(-this._delay, this.rawTime()), this._ts = this._act = 0) : (Mt(), this._ts = this._rts, this.totalTime(this.parent && !this.parent.smoothChildTiming ? this.rawTime() : this._tTime || this._pTime, 1 === this.progress() && (this._tTime -= Ye) && Math.abs(this._zTime) !== Ye))), this) : this._ps;
  }, Dt.startTime = function (e) {
    if (arguments.length) {
      this._start = e;
      var t = this.parent || this._dp;
      return !t || !t._sort && this.parent || G(t, this, e - this._delay), this;
    }

    return this._start;
  }, Dt.endTime = function (e) {
    return this._start + (l(e) ? this.totalDuration() : this.duration()) / Math.abs(this._ts);
  }, Dt.rawTime = function (e) {
    var t = this.parent || this._dp;
    return t ? e && (!this._ts || this._repeat && this._time && this.totalProgress() < 1) ? this._tTime % (this._dur + this._rDelay) : this._ts ? I(t.rawTime(e), this) : this._tTime : this._tTime;
  }, Dt.globalTime = function (e) {
    for (var t = this, i = arguments.length ? e : t.rawTime(); t;) {
      i = t._start + i / (t._ts || 1), t = t._dp;
    }

    return i;
  }, Dt.repeat = function (e) {
    return arguments.length ? (this._repeat = e === 1 / 0 ? -2 : e, H(this)) : -2 === this._repeat ? 1 / 0 : this._repeat;
  }, Dt.repeatDelay = function (e) {
    return arguments.length ? (this._rDelay = e, H(this)) : this._rDelay;
  }, Dt.yoyo = function (e) {
    return arguments.length ? (this._yoyo = e, this) : this._yoyo;
  }, Dt.seek = function (e, t) {
    return this.totalTime(V(this, e), l(t));
  }, Dt.restart = function (e, t) {
    return this.play().totalTime(e ? -this._delay : 0, l(t));
  }, Dt.play = function (e, t) {
    return null != e && this.seek(e, t), this.reversed(!1).paused(!1);
  }, Dt.reverse = function (e, t) {
    return null != e && this.seek(e || this.totalDuration(), t), this.reversed(!0).paused(!1);
  }, Dt.pause = function (e, t) {
    return null != e && this.seek(e, t), this.paused(!0);
  }, Dt.resume = function () {
    return this.paused(!1);
  }, Dt.reversed = function (e) {
    return arguments.length ? (!!e !== this.reversed() && this.timeScale(-this._rts || (e ? -Ye : 0)), this) : this._rts < 0;
  }, Dt.invalidate = function () {
    return this._initted = this._act = 0, this._zTime = -Ye, this;
  }, Dt.isActive = function () {
    var e,
        t = this.parent || this._dp,
        i = this._start;
    return !(t && !(this._ts && this._initted && t.isActive() && (e = t.rawTime(!0)) >= i && e < this.endTime(!0) - Ye));
  }, Dt.eventCallback = function (e, t, i) {
    var a = this.vars;
    return 1 < arguments.length ? (t ? (a[e] = t, i && (a[e + "Params"] = i), "onUpdate" === e && (this._onUpdate = t)) : delete a[e], this) : a[e];
  }, Dt.then = function (e) {
    var t = this;
    return new Promise(function (i) {
      function a() {
        var e = t.then;
        t.then = null, r(n) && (n = n(t)) && (n.then || n === t) && (t.then = e), i(n), t.then = e;
      }

      var n = r(e) ? e : S;
      t._initted && 1 === t.totalProgress() && 0 <= t._ts || !t._tTime && t._ts < 0 ? a() : t._prom = a;
    });
  }, Dt.kill = function () {
    ae(this);
  }, Nt);

  function Nt(e, t) {
    var i = e.parent || me;
    this.vars = e, this._delay = +e.delay || 0, (this._repeat = e.repeat === 1 / 0 ? -2 : e.repeat || 0) && (this._rDelay = e.repeatDelay || 0, this._yoyo = !!e.yoyo || !!e.yoyoEase), this._ts = 1, Y(this, +e.duration, 1, 1), this.data = e.data, _e || St.wake(), i && G(i, this, t || 0 === t ? t : i._time, 1), e.reversed && this.reverse(), e.paused && this.paused(!0);
  }

  M(Bt.prototype, {
    _time: 0,
    _start: 0,
    _end: 0,
    _tTime: 0,
    _tDur: 0,
    _dirty: 0,
    _repeat: 0,
    _yoyo: !1,
    parent: null,
    _initted: !1,
    _rDelay: 0,
    _ts: 1,
    _dp: 0,
    ratio: 0,
    _zTime: -Ye,
    _prom: 0,
    _ps: !1,
    _rts: 1
  });

  var Rt = function (e) {
    function s(t, a) {
      var r;
      return void 0 === t && (t = {}), (r = e.call(this, t, a) || this).labels = {}, r.smoothChildTiming = !!t.smoothChildTiming, r.autoRemoveChildren = !!t.autoRemoveChildren, r._sort = l(t.sortChildren), r.parent && R(r.parent, i(r)), t.scrollTrigger && F(i(r), t.scrollTrigger), r;
    }

    t(s, e);
    var o = s.prototype;
    return o.to = function (e, t, i, a) {
      return new qt(e, T(arguments, 0, this), V(this, n(t) ? a : i)), this;
    }, o.from = function (e, t, i, a) {
      return new qt(e, T(arguments, 1, this), V(this, n(t) ? a : i)), this;
    }, o.fromTo = function (e, t, i, a, r) {
      return new qt(e, T(arguments, 2, this), V(this, n(t) ? r : a)), this;
    }, o.set = function (e, t, i) {
      return t.duration = 0, t.parent = this, O(t).repeatDelay || (t.repeat = 0), t.immediateRender = !!t.immediateRender, new qt(e, t, V(this, i), 1), this;
    }, o.call = function (e, t, i) {
      return G(this, qt.delayedCall(0, e, t), V(this, i));
    }, o.staggerTo = function (e, t, i, a, r, n, s) {
      return i.duration = t, i.stagger = i.stagger || a, i.onComplete = n, i.onCompleteParams = s, i.parent = this, new qt(e, i, V(this, r)), this;
    }, o.staggerFrom = function (e, t, i, a, r, n, s) {
      return i.runBackwards = 1, O(i).immediateRender = l(i.immediateRender), this.staggerTo(e, t, i, a, r, n, s);
    }, o.staggerFromTo = function (e, t, i, a, r, n, s, o) {
      return a.startAt = i, O(a).immediateRender = l(a.immediateRender), this.staggerTo(e, t, a, r, n, s, o);
    }, o.render = function (e, t, i) {
      var a,
          r,
          n,
          s,
          o,
          l,
          d,
          u,
          p,
          c,
          h,
          f,
          m = this._time,
          v = this._dirty ? this.totalDuration() : this._tDur,
          g = this._dur,
          y = this !== me && v - Ye < e && 0 <= e ? v : e < Ye ? 0 : e,
          w = this._zTime < 0 != e < 0 && (this._initted || !g);

      if (y !== this._tTime || i || w) {
        if (m !== this._time && g && (y += this._time - m, e += this._time - m), a = y, p = this._start, l = !(u = this._ts), w && (g || (m = this._zTime), !e && t || (this._zTime = e)), this._repeat) {
          if (h = this._yoyo, o = g + this._rDelay, this._repeat < -1 && e < 0) return this.totalTime(100 * o + e, t, i);

          if (a = b(y % o), y === v ? (s = this._repeat, a = g) : ((s = ~~(y / o)) && s === y / o && (a = g, s--), g < a && (a = g)), c = mt(this._tTime, o), !m && this._tTime && c !== s && (c = s), h && 1 & s && (a = g - a, f = 1), s !== c && !this._lock) {
            var x = h && 1 & c,
                T = x === (h && 1 & s);
            if (s < c && (x = !x), m = x ? 0 : g, this._lock = 1, this.render(m || (f ? 0 : b(s * o)), t, !g)._lock = 0, !t && this.parent && xt(this, "onRepeat"), this.vars.repeatRefresh && !f && (this.invalidate()._lock = 1), m !== this._time || l != !this._ts) return this;
            if (g = this._dur, v = this._tDur, T && (this._lock = 2, m = x ? g : -1e-4, this.render(m, !0), this.vars.repeatRefresh && !f && this.invalidate()), this._lock = 0, !this._ts && !l) return this;
            de(this, f);
          }
        }

        if (this._hasPause && !this._forcing && this._lock < 2 && (d = function (e, t, i) {
          var a;
          if (t < i) for (a = e._first; a && a._start <= i;) {
            if (!a._dur && "isPause" === a.data && a._start > t) return a;
            a = a._next;
          } else for (a = e._last; a && a._start >= i;) {
            if (!a._dur && "isPause" === a.data && a._start < t) return a;
            a = a._prev;
          }
        }(this, b(m), b(a))) && (y -= a - (a = d._start)), this._tTime = y, this._time = a, this._act = !u, this._initted || (this._onUpdate = this.vars.onUpdate, this._initted = 1, this._zTime = e, m = 0), m || !(a || !g && 0 <= e) || t || xt(this, "onStart"), m <= a && 0 <= e) for (r = this._first; r;) {
          if (n = r._next, (r._act || a >= r._start) && r._ts && d !== r) {
            if (r.parent !== this) return this.render(e, t, i);

            if (r.render(0 < r._ts ? (a - r._start) * r._ts : (r._dirty ? r.totalDuration() : r._tDur) + (a - r._start) * r._ts, t, i), a !== this._time || !this._ts && !l) {
              d = 0, n && (y += this._zTime = -Ye);
              break;
            }
          }

          r = n;
        } else {
          r = this._last;

          for (var _ = e < 0 ? e : a; r;) {
            if (n = r._prev, (r._act || _ <= r._end) && r._ts && d !== r) {
              if (r.parent !== this) return this.render(e, t, i);

              if (r.render(0 < r._ts ? (_ - r._start) * r._ts : (r._dirty ? r.totalDuration() : r._tDur) + (_ - r._start) * r._ts, t, i), a !== this._time || !this._ts && !l) {
                d = 0, n && (y += this._zTime = _ ? -Ye : Ye);
                break;
              }
            }

            r = n;
          }
        }
        if (d && !t && (this.pause(), d.render(m <= a ? 0 : -Ye)._zTime = m <= a ? 1 : -1, this._ts)) return this._start = p, B(this), this.render(e, t, i);
        this._onUpdate && !t && xt(this, "onUpdate", !0), (y === v && v >= this.totalDuration() || !y && m) && (p !== this._start && Math.abs(u) === Math.abs(this._ts) || this._lock || (!e && g || !(y === v && 0 < this._ts || !y && this._ts < 0) || A(this, 1), t || e < 0 && !m || !y && !m || (xt(this, y === v ? "onComplete" : "onReverseComplete", !0), !this._prom || y < v && 0 < this.timeScale() || this._prom())));
      }

      return this;
    }, o.add = function (e, t) {
      var i = this;

      if (n(t) || (t = V(this, t)), !(e instanceof Bt)) {
        if (Qe(e)) return e.forEach(function (e) {
          return i.add(e, t);
        }), this;
        if (a(e)) return this.addLabel(e, t);
        if (!r(e)) return this;
        e = qt.delayedCall(0, e);
      }

      return this !== e ? G(this, e, t) : this;
    }, o.getChildren = function (e, t, i, a) {
      void 0 === e && (e = !0), void 0 === t && (t = !0), void 0 === i && (i = !0), void 0 === a && (a = -Xe);

      for (var r = [], n = this._first; n;) {
        n._start >= a && (n instanceof qt ? t && r.push(n) : (i && r.push(n), e && r.push.apply(r, n.getChildren(!0, t, i)))), n = n._next;
      }

      return r;
    }, o.getById = function (e) {
      for (var t = this.getChildren(1, 1, 1), i = t.length; i--;) {
        if (t[i].vars.id === e) return t[i];
      }
    }, o.remove = function (e) {
      return a(e) ? this.removeLabel(e) : r(e) ? this.killTweensOf(e) : (L(this, e), e === this._recent && (this._recent = this._last), $(this));
    }, o.totalTime = function (t, i) {
      return arguments.length ? (this._forcing = 1, !this._dp && this._ts && (this._start = b(St.time - (0 < this._ts ? t / this._ts : (this.totalDuration() - t) / -this._ts))), e.prototype.totalTime.call(this, t, i), this._forcing = 0, this) : this._tTime;
    }, o.addLabel = function (e, t) {
      return this.labels[e] = V(this, t), this;
    }, o.removeLabel = function (e) {
      return delete this.labels[e], this;
    }, o.addPause = function (e, t, i) {
      var a = qt.delayedCall(0, t || m, i);
      return a.data = "isPause", this._hasPause = 1, G(this, a, V(this, e));
    }, o.removePause = function (e) {
      var t = this._first;

      for (e = V(this, e); t;) {
        t._start === e && "isPause" === t.data && A(t), t = t._next;
      }
    }, o.killTweensOf = function (e, t, i) {
      for (var a = this.getTweensOf(e, i), r = a.length; r--;) {
        Ft !== a[r] && a[r].kill(e, t);
      }

      return this;
    }, o.getTweensOf = function (e, t) {
      for (var i, a = [], r = wt(e), s = this._first, o = n(t); s;) {
        s instanceof qt ? x(s._targets, r) && (o ? (!Ft || s._initted && s._ts) && s.globalTime(0) <= t && s.globalTime(s.totalDuration()) > t : !t || s.isActive()) && a.push(s) : (i = s.getTweensOf(r, t)).length && a.push.apply(a, i), s = s._next;
      }

      return a;
    }, o.tweenTo = function (e, t) {
      t = t || {};
      var i = this,
          a = V(i, e),
          r = t.startAt,
          n = t.onStart,
          s = t.onStartParams,
          o = t.immediateRender,
          l = qt.to(i, M({
        ease: "none",
        lazy: !1,
        immediateRender: !1,
        time: a,
        overwrite: "auto",
        duration: t.duration || Math.abs((a - (r && "time" in r ? r.time : i._time)) / i.timeScale()) || Ye,
        onStart: function onStart() {
          i.pause();
          var e = t.duration || Math.abs((a - i._time) / i.timeScale());
          l._dur !== e && Y(l, e, 0, 1).render(l._time, !0, !0), n && n.apply(l, s || []);
        }
      }, t));
      return o ? l.render(0) : l;
    }, o.tweenFromTo = function (e, t, i) {
      return this.tweenTo(t, M({
        startAt: {
          time: V(this, e)
        }
      }, i));
    }, o.recent = function () {
      return this._recent;
    }, o.nextLabel = function (e) {
      return void 0 === e && (e = this._time), ie(this, V(this, e));
    }, o.previousLabel = function (e) {
      return void 0 === e && (e = this._time), ie(this, V(this, e), 1);
    }, o.currentLabel = function (e) {
      return arguments.length ? this.seek(e, !0) : this.previousLabel(this._time + Ye);
    }, o.shiftChildren = function (e, t, i) {
      void 0 === i && (i = 0);

      for (var a, r = this._first, n = this.labels; r;) {
        r._start >= i && (r._start += e, r._end += e), r = r._next;
      }

      if (t) for (a in n) {
        n[a] >= i && (n[a] += e);
      }
      return $(this);
    }, o.invalidate = function () {
      var t = this._first;

      for (this._lock = 0; t;) {
        t.invalidate(), t = t._next;
      }

      return e.prototype.invalidate.call(this);
    }, o.clear = function (e) {
      void 0 === e && (e = !0);

      for (var t, i = this._first; i;) {
        t = i._next, this.remove(i), i = t;
      }

      return this._dp && (this._time = this._tTime = this._pTime = 0), e && (this.labels = {}), $(this);
    }, o.totalDuration = function (e) {
      var t,
          i,
          a,
          r = 0,
          n = this,
          s = n._last,
          o = Xe;
      if (arguments.length) return n.timeScale((n._repeat < 0 ? n.duration() : n.totalDuration()) / (n.reversed() ? -e : e));

      if (n._dirty) {
        for (a = n.parent; s;) {
          t = s._prev, s._dirty && s.totalDuration(), o < (i = s._start) && n._sort && s._ts && !n._lock ? (n._lock = 1, G(n, s, i - s._delay, 1)._lock = 0) : o = i, i < 0 && s._ts && (r -= i, (!a && !n._dp || a && a.smoothChildTiming) && (n._start += i / n._ts, n._time -= i, n._tTime -= i), n.shiftChildren(-i, !1, -1 / 0), o = 0), s._end > r && s._ts && (r = s._end), s = t;
        }

        Y(n, n === me && n._time > r ? n._time : r, 1, 1), n._dirty = 0;
      }

      return n._tDur;
    }, s.updateRoot = function (e) {
      if (me._ts && (E(me, I(e, me)), xe = St.frame), St.frame >= pt) {
        pt += Ge.autoSleep || 120;
        var t = me._first;

        if ((!t || !t._ts) && Ge.autoSleep && St._listeners.length < 2) {
          for (; t && !t._ts;) {
            t = t._next;
          }

          t || St.sleep();
        }
      }
    }, s;
  }(Bt);

  function Gt(e, t, i, n, s, l) {
    var d, u, p, c;
    if (dt[e] && !1 !== (d = new dt[e]()).init(s, d.rawVars ? t[e] : function (e, t, i, n, s) {
      if (r(e) && (e = Ht(e, s, t, i, n)), !o(e) || e.style && e.nodeType || Qe(e) || Ke(e)) return a(e) ? Ht(e, s, t, i, n) : e;
      var l,
          d = {};

      for (l in e) {
        d[l] = Ht(e[l], s, t, i, n);
      }

      return d;
    }(t[e], n, s, l, i), i, n, l) && (i._pt = u = new oi(i._pt, s, e, 0, 1, d.render, d, 0, d.priority), i !== Te)) for (p = i._ptLookup[i._targets.indexOf(s)], c = d._props.length; c--;) {
      p[d._props[c]] = u;
    }
    return d;
  }

  M(Rt.prototype, {
    _lock: 0,
    _hasPause: 0,
    _forcing: 0
  });

  var Ft,
      Xt = function Xt(e, t, i, n, s, o, l, d, u) {
    r(n) && (n = n(s || 0, e, o));
    var p,
        h = e[t],
        f = "get" !== i ? i : r(h) ? u ? e[t.indexOf("set") || !r(e["get" + t.substr(3)]) ? t : "get" + t.substr(3)](u) : e[t]() : h,
        m = r(h) ? u ? Zt : Qt : Kt;
    if (a(n) && (~n.indexOf("random(") && (n = te(n)), "=" === n.charAt(1) && (n = parseFloat(f) + parseFloat(n.substr(2)) * ("-" === n.charAt(0) ? -1 : 1) + (q(f) || 0))), f !== n) return isNaN(f * n) ? (h || t in e || c(t, n), function (e, t, i, a, r, n, s) {
      var o,
          l,
          d,
          u,
          p,
          c,
          h,
          f,
          m = new oi(this._pt, e, t, 0, 1, ii, null, r),
          v = 0,
          g = 0;

      for (m.b = i, m.e = a, i += "", (h = ~(a += "").indexOf("random(")) && (a = te(a)), n && (n(f = [i, a], e, t), i = f[0], a = f[1]), l = i.match(tt) || []; o = tt.exec(a);) {
        u = o[0], p = a.substring(v, o.index), d ? d = (d + 1) % 5 : "rgba(" === p.substr(-5) && (d = 1), u !== l[g++] && (c = parseFloat(l[g - 1]) || 0, m._pt = {
          _next: m._pt,
          p: p || 1 === g ? p : ",",
          s: c,
          c: "=" === u.charAt(1) ? parseFloat(u.substr(2)) * ("-" === u.charAt(0) ? -1 : 1) : parseFloat(u) - c,
          m: d && d < 4 ? Math.round : 0
        }, v = tt.lastIndex);
      }

      return m.c = v < a.length ? a.substring(v, a.length) : "", m.fp = s, (it.test(a) || h) && (m.e = 0), this._pt = m;
    }.call(this, e, t, f, n, m, d || Ge.stringFilter, u)) : (p = new oi(this._pt, e, t, +f || 0, n - (f || 0), "boolean" == typeof h ? ti : ei, 0, m), u && (p.fp = u), l && p.modifier(l, this, e), this._pt = p);
  },
      Yt = function e(t, i) {
    var a,
        r,
        n,
        s,
        o,
        d,
        u,
        p,
        c,
        h,
        f,
        m,
        y,
        w = t.vars,
        b = w.ease,
        x = w.startAt,
        T = w.immediateRender,
        E = w.lazy,
        C = w.onUpdate,
        S = w.onUpdateParams,
        z = w.callbackScope,
        k = w.runBackwards,
        O = w.yoyoEase,
        L = w.keyframes,
        $ = w.autoRevert,
        D = t._dur,
        I = t._startAt,
        B = t._targets,
        N = t.parent,
        R = N && "nested" === N.data ? N.parent._targets : B,
        G = "auto" === t._overwrite && !fe,
        F = t.timeline;

    if (!F || L && b || (b = "none"), t._ease = Lt(b, Fe.ease), t._yEase = O ? Ot(Lt(!0 === O ? b : O, Fe.ease)) : 0, O && t._yoyo && !t._repeat && (O = t._yEase, t._yEase = t._ease, t._ease = O), !F) {
      if (m = (p = B[0] ? g(B[0]).harness : 0) && w[p.prop], a = P(w, st), I && I.render(-1, !0).kill(), x) {
        if (A(t._startAt = qt.set(B, M({
          data: "isStart",
          overwrite: !1,
          parent: N,
          immediateRender: !0,
          lazy: l(E),
          startAt: null,
          delay: 0,
          onUpdate: C,
          onUpdateParams: S,
          callbackScope: z,
          stagger: 0
        }, x))), T) if (0 < i) $ || (t._startAt = 0);else if (D && !(i < 0 && I)) return void (i && (t._zTime = i));
      } else if (k && D) if (I) $ || (t._startAt = 0);else if (i && (T = !1), n = M({
        overwrite: !1,
        data: "isFromStart",
        lazy: T && l(E),
        immediateRender: T,
        stagger: 0,
        parent: N
      }, a), m && (n[p.prop] = m), A(t._startAt = qt.set(B, n)), T) {
        if (!i) return;
      } else e(t._startAt, Ye);

      for (t._pt = 0, E = D && l(E) || E && !D, r = 0; r < B.length; r++) {
        if (u = (o = B[r])._gsap || v(B)[r]._gsap, t._ptLookup[r] = h = {}, lt[u.id] && ot.length && _(), f = R === B ? r : R.indexOf(o), p && !1 !== (c = new p()).init(o, m || a, t, f, R) && (t._pt = s = new oi(t._pt, o, c.name, 0, 1, c.render, c, 0, c.priority), c._props.forEach(function (e) {
          h[e] = s;
        }), c.priority && (d = 1)), !p || m) for (n in a) {
          dt[n] && (c = Gt(n, a, t, f, o, R)) ? c.priority && (d = 1) : h[n] = s = Xt.call(t, o, n, "get", a[n], f, R, 0, w.stringFilter);
        }
        t._op && t._op[r] && t.kill(o, t._op[r]), G && t._pt && (Ft = t, me.killTweensOf(o, h, t.globalTime(0)), y = !t.parent, Ft = 0), t._pt && E && (lt[u.id] = 1);
      }

      d && si(t), t._onInit && t._onInit(t);
    }

    t._from = !F && !!w.runBackwards, t._onUpdate = C, t._initted = (!t._op || t._pt) && !y;
  },
      Ht = function Ht(e, t, i, n, s) {
    return r(e) ? e.call(t, i, n, s) : a(e) && ~e.indexOf("random(") ? te(e) : e;
  },
      Vt = ht + "repeat,repeatDelay,yoyo,repeatRefresh,yoyoEase",
      Wt = (Vt + ",id,stagger,delay,duration,paused,scrollTrigger").split(","),
      qt = function (e) {
    function r(t, a, r, s) {
      var d;
      "number" == typeof a && (r.duration = a, a = r, r = null);
      var p,
          c,
          f,
          g,
          y,
          w,
          x,
          T,
          _ = (d = e.call(this, s ? a : O(a), r) || this).vars,
          E = _.duration,
          C = _.delay,
          S = _.immediateRender,
          z = _.stagger,
          k = _.overwrite,
          P = _.keyframes,
          L = _.defaults,
          A = _.scrollTrigger,
          $ = _.yoyoEase,
          D = d.parent,
          I = (Qe(t) || Ke(t) ? n(t[0]) : "length" in a) ? [t] : wt(t);

      if (d._targets = I.length ? v(I) : h("GSAP target " + t + " not found. https://greensock.com", !Ge.nullTargetWarn) || [], d._ptLookup = [], d._overwrite = k, P || z || u(E) || u(C)) {
        if (a = d.vars, (p = d.timeline = new Rt({
          data: "nested",
          defaults: L || {}
        })).kill(), p.parent = p._dp = i(d), p._start = 0, P) M(p.vars.defaults, {
          ease: "none"
        }), P.forEach(function (e) {
          return p.to(I, e, ">");
        });else {
          if (g = I.length, x = z ? K(z) : m, o(z)) for (y in z) {
            ~Vt.indexOf(y) && ((T = T || {})[y] = z[y]);
          }

          for (c = 0; c < g; c++) {
            for (y in f = {}, a) {
              Wt.indexOf(y) < 0 && (f[y] = a[y]);
            }

            f.stagger = 0, $ && (f.yoyoEase = $), T && ft(f, T), w = I[c], f.duration = +Ht(E, i(d), c, w, I), f.delay = (+Ht(C, i(d), c, w, I) || 0) - d._delay, !z && 1 === g && f.delay && (d._delay = C = f.delay, d._start += C, f.delay = 0), p.to(w, f, x(c, w, I));
          }

          p.duration() ? E = C = 0 : d.timeline = 0;
        }
        E || d.duration(E = p.duration());
      } else d.timeline = 0;

      return !0 !== k || fe || (Ft = i(d), me.killTweensOf(I), Ft = 0), D && R(D, i(d)), (S || !E && !P && d._start === b(D._time) && l(S) && function e(t) {
        return !t || t._ts && e(t.parent);
      }(i(d)) && "nested" !== D.data) && (d._tTime = -Ye, d.render(Math.max(0, -C))), A && F(i(d), A), d;
    }

    t(r, e);
    var s = r.prototype;
    return s.render = function (e, t, i) {
      var a,
          r,
          n,
          s,
          o,
          l,
          d,
          u,
          p,
          c = this._time,
          h = this._tDur,
          f = this._dur,
          m = h - Ye < e && 0 <= e ? h : e < Ye ? 0 : e;

      if (f) {
        if (m !== this._tTime || !e || i || !this._initted && this._tTime || this._startAt && this._zTime < 0 != e < 0) {
          if (a = m, u = this.timeline, this._repeat) {
            if (s = f + this._rDelay, this._repeat < -1 && e < 0) return this.totalTime(100 * s + e, t, i);
            if (a = b(m % s), m === h ? (n = this._repeat, a = f) : ((n = ~~(m / s)) && n === m / s && (a = f, n--), f < a && (a = f)), (l = this._yoyo && 1 & n) && (p = this._yEase, a = f - a), o = mt(this._tTime, s), a === c && !i && this._initted) return this;
            n !== o && (u && this._yEase && de(u, l), !this.vars.repeatRefresh || l || this._lock || (this._lock = i = 1, this.render(b(s * n), !0).invalidate()._lock = 0));
          }

          if (!this._initted) {
            if (X(this, e < 0 ? e : a, i, t)) return this._tTime = 0, this;
            if (f !== this._dur) return this.render(e, t, i);
          }

          for (this._tTime = m, this._time = a, !this._act && this._ts && (this._act = 1, this._lazy = 0), this.ratio = d = (p || this._ease)(a / f), this._from && (this.ratio = d = 1 - d), !a || c || t || xt(this, "onStart"), r = this._pt; r;) {
            r.r(d, r.d), r = r._next;
          }

          u && u.render(e < 0 ? e : !a && l ? -Ye : u._dur * d, t, i) || this._startAt && (this._zTime = e), this._onUpdate && !t && (e < 0 && this._startAt && this._startAt.render(e, !0, i), xt(this, "onUpdate")), this._repeat && n !== o && this.vars.onRepeat && !t && this.parent && xt(this, "onRepeat"), m !== this._tDur && m || this._tTime !== m || (e < 0 && this._startAt && !this._onUpdate && this._startAt.render(e, !0, !0), !e && f || !(m === this._tDur && 0 < this._ts || !m && this._ts < 0) || A(this, 1), t || e < 0 && !c || !m && !c || (xt(this, m === h ? "onComplete" : "onReverseComplete", !0), !this._prom || m < h && 0 < this.timeScale() || this._prom()));
        }
      } else !function (e, t, i, a) {
        var r,
            n,
            s,
            o = e.ratio,
            l = t < 0 || !t && (!e._start && function e(t) {
          var i = t.parent;
          return i && i._ts && i._initted && !i._lock && (i.rawTime() < 0 || e(i));
        }(e) || (e._ts < 0 || e._dp._ts < 0) && "isFromStart" !== e.data && "isStart" !== e.data) ? 0 : 1,
            d = e._rDelay,
            u = 0;

        if (d && e._repeat && (u = gt(0, e._tDur, t), n = mt(u, d), s = mt(e._tTime, d), e._yoyo && 1 & n && (l = 1 - l), n !== s && (o = 1 - l, e.vars.repeatRefresh && e._initted && e.invalidate())), l !== o || a || e._zTime === Ye || !t && e._zTime) {
          if (!e._initted && X(e, t, a, i)) return;

          for (s = e._zTime, e._zTime = t || (i ? Ye : 0), i = i || t && !s, e.ratio = l, e._from && (l = 1 - l), e._time = 0, e._tTime = u, i || xt(e, "onStart"), r = e._pt; r;) {
            r.r(l, r.d), r = r._next;
          }

          e._startAt && t < 0 && e._startAt.render(t, !0, !0), e._onUpdate && !i && xt(e, "onUpdate"), u && e._repeat && !i && e.parent && xt(e, "onRepeat"), (t >= e._tDur || t < 0) && e.ratio === l && (l && A(e, 1), i || (xt(e, l ? "onComplete" : "onReverseComplete", !0), e._prom && e._prom()));
        } else e._zTime || (e._zTime = t);
      }(this, e, t, i);

      return this;
    }, s.targets = function () {
      return this._targets;
    }, s.invalidate = function () {
      return this._pt = this._op = this._startAt = this._onUpdate = this._lazy = this.ratio = 0, this._ptLookup = [], this.timeline && this.timeline.invalidate(), e.prototype.invalidate.call(this);
    }, s.kill = function (e, t) {
      if (void 0 === t && (t = "all"), !(e || t && "all" !== t)) return this._lazy = this._pt = 0, this.parent ? ae(this) : this;

      if (this.timeline) {
        var i = this.timeline.totalDuration();
        return this.timeline.killTweensOf(e, t, Ft && !0 !== Ft.vars.overwrite)._first || ae(this), this.parent && i !== this.timeline.totalDuration() && Y(this, this._dur * this.timeline._tDur / i, 0, 1), this;
      }

      var r,
          n,
          s,
          o,
          l,
          d,
          u,
          p = this._targets,
          c = e ? wt(e) : p,
          h = this._ptLookup,
          f = this._pt;
      if ((!t || "all" === t) && function (e, t) {
        for (var i = e.length, a = i === t.length; a && i-- && e[i] === t[i];) {
          ;
        }

        return i < 0;
      }(p, c)) return "all" === t && (this._pt = 0), ae(this);

      for (r = this._op = this._op || [], "all" !== t && (a(t) && (l = {}, w(t, function (e) {
        return l[e] = 1;
      }), t = l), t = function (e, t) {
        var i,
            a,
            r,
            n,
            s = e[0] ? g(e[0]).harness : 0,
            o = s && s.aliases;
        if (!o) return t;

        for (a in i = ft({}, t), o) {
          if ((a in i)) for (r = (n = o[a].split(",")).length; r--;) {
            i[n[r]] = i[a];
          }
        }

        return i;
      }(p, t)), u = p.length; u--;) {
        if (~c.indexOf(p[u])) for (l in n = h[u], "all" === t ? (r[u] = t, o = n, s = {}) : (s = r[u] = r[u] || {}, o = t), o) {
          (d = n && n[l]) && ("kill" in d.d && !0 !== d.d.kill(l) || L(this, d, "_pt"), delete n[l]), "all" !== s && (s[l] = 1);
        }
      }

      return this._initted && !this._pt && f && ae(this), this;
    }, r.to = function (e, t, i) {
      return new r(e, t, i);
    }, r.from = function (e, t) {
      return new r(e, T(arguments, 1));
    }, r.delayedCall = function (e, t, i, a) {
      return new r(t, 0, {
        immediateRender: !1,
        lazy: !1,
        overwrite: !1,
        delay: e,
        onComplete: t,
        onReverseComplete: t,
        onCompleteParams: i,
        onReverseCompleteParams: i,
        callbackScope: a
      });
    }, r.fromTo = function (e, t, i) {
      return new r(e, T(arguments, 2));
    }, r.set = function (e, t) {
      return t.duration = 0, t.repeatDelay || (t.repeat = 0), new r(e, t);
    }, r.killTweensOf = function (e, t, i) {
      return me.killTweensOf(e, t, i);
    }, r;
  }(Bt);

  function jt(e, t, i) {
    return e.setAttribute(t, i);
  }

  function Ut(e, t, i, a) {
    a.mSet(e, t, a.m.call(a.tween, i, a.mt), a);
  }

  M(qt.prototype, {
    _targets: [],
    _lazy: 0,
    _startAt: 0,
    _op: 0,
    _onInit: 0
  }), w("staggerTo,staggerFrom,staggerFromTo", function (e) {
    qt[e] = function () {
      var t = new Rt(),
          i = yt.call(arguments, 0);
      return i.splice("staggerFromTo" === e ? 5 : 4, 0, 0), t[e].apply(t, i);
    };
  });

  var Kt = function Kt(e, t, i) {
    return e[t] = i;
  },
      Qt = function Qt(e, t, i) {
    return e[t](i);
  },
      Zt = function Zt(e, t, i, a) {
    return e[t](a.fp, i);
  },
      Jt = function Jt(e, t) {
    return r(e[t]) ? Qt : s(e[t]) && e.setAttribute ? jt : Kt;
  },
      ei = function ei(e, t) {
    return t.set(t.t, t.p, Math.round(1e4 * (t.s + t.c * e)) / 1e4, t);
  },
      ti = function ti(e, t) {
    return t.set(t.t, t.p, !!(t.s + t.c * e), t);
  },
      ii = function ii(e, t) {
    var i = t._pt,
        a = "";
    if (!e && t.b) a = t.b;else if (1 === e && t.e) a = t.e;else {
      for (; i;) {
        a = i.p + (i.m ? i.m(i.s + i.c * e) : Math.round(1e4 * (i.s + i.c * e)) / 1e4) + a, i = i._next;
      }

      a += t.c;
    }
    t.set(t.t, t.p, a, t);
  },
      ai = function ai(e, t) {
    for (var i = t._pt; i;) {
      i.r(e, i.d), i = i._next;
    }
  },
      ri = function ri(e, t, i, a) {
    for (var r, n = this._pt; n;) {
      r = n._next, n.p === a && n.modifier(e, t, i), n = r;
    }
  },
      ni = function ni(e) {
    for (var t, i, a = this._pt; a;) {
      i = a._next, a.p === e && !a.op || a.op === e ? L(this, a, "_pt") : a.dep || (t = 1), a = i;
    }

    return !t;
  },
      si = function si(e) {
    for (var t, i, a, r, n = e._pt; n;) {
      for (t = n._next, i = a; i && i.pr > n.pr;) {
        i = i._next;
      }

      (n._prev = i ? i._prev : r) ? n._prev._next = n : a = n, (n._next = i) ? i._prev = n : r = n, n = t;
    }

    e._pt = a;
  },
      oi = (li.prototype.modifier = function (e, t, i) {
    this.mSet = this.mSet || this.set, this.set = Ut, this.m = e, this.mt = i, this.tween = t;
  }, li);

  function li(e, t, i, a, r, n, s, o, l) {
    this.t = t, this.s = a, this.c = r, this.p = i, this.r = n || ei, this.d = s || this, this.set = o || Kt, this.pr = l || 0, (this._next = e) && (e._prev = this);
  }

  w(ht + "parent,duration,ease,delay,overwrite,runBackwards,startAt,yoyo,immediateRender,repeat,repeatDelay,data,paused,reversed,lazy,callbackScope,stringFilter,id,yoyoEase,stagger,inherit,repeatRefresh,keyframes,autoRevert,scrollTrigger", function (e) {
    return st[e] = 1;
  }), nt.TweenMax = nt.TweenLite = qt, nt.TimelineLite = nt.TimelineMax = Rt, me = new Rt({
    sortChildren: !1,
    defaults: Fe,
    autoRemoveChildren: !0,
    id: "root",
    smoothChildTiming: !0
  }), Ge.stringFilter = le;
  var di = {
    registerPlugin: function registerPlugin() {
      for (var e = arguments.length, t = new Array(e), i = 0; i < e; i++) {
        t[i] = arguments[i];
      }

      t.forEach(function (e) {
        return function (e) {
          var t = (e = !e.name && e["default"] || e).name,
              i = r(e),
              a = t && !i && e.init ? function () {
            this._props = [];
          } : e,
              n = {
            init: m,
            render: ai,
            add: Xt,
            kill: ni,
            modifier: ri,
            rawVars: 0
          },
              s = {
            targetTest: 0,
            get: 0,
            getSetter: Jt,
            aliases: {},
            register: 0
          };

          if (Mt(), e !== a) {
            if (dt[t]) return;
            M(a, M(P(e, n), s)), ft(a.prototype, ft(n, P(e, s))), dt[a.prop = t] = a, e.targetTest && (ct.push(a), st[t] = 1), t = ("css" === t ? "CSS" : t.charAt(0).toUpperCase() + t.substr(1)) + "Plugin";
          }

          f(t, a), e.register && e.register(ci, a, oi);
        }(e);
      });
    },
    timeline: function timeline(e) {
      return new Rt(e);
    },
    getTweensOf: function getTweensOf(e, t) {
      return me.getTweensOf(e, t);
    },
    getProperty: function getProperty(e, t, i, r) {
      a(e) && (e = wt(e)[0]);
      var n = g(e || {}).get,
          s = i ? S : C;
      return "native" === i && (i = ""), e ? t ? s((dt[t] && dt[t].get || n)(e, t, i, r)) : function (t, i, a) {
        return s((dt[t] && dt[t].get || n)(e, t, i, a));
      } : e;
    },
    quickSetter: function quickSetter(e, t, i) {
      if (1 < (e = wt(e)).length) {
        var a = e.map(function (e) {
          return ci.quickSetter(e, t, i);
        }),
            r = a.length;
        return function (e) {
          for (var t = r; t--;) {
            a[t](e);
          }
        };
      }

      e = e[0] || {};
      var n = dt[t],
          s = g(e),
          o = s.harness && (s.harness.aliases || {})[t] || t,
          l = n ? function (t) {
        var a = new n();
        Te._pt = 0, a.init(e, i ? t + i : t, Te, 0, [e]), a.render(1, a), Te._pt && ai(1, Te);
      } : s.set(e, o);
      return n ? l : function (t) {
        return l(e, o, i ? t + i : t, s, 1);
      };
    },
    isTweening: function isTweening(e) {
      return 0 < me.getTweensOf(e, !0).length;
    },
    defaults: function defaults(e) {
      return e && e.ease && (e.ease = Lt(e.ease, Fe.ease)), k(Fe, e || {});
    },
    config: function config(e) {
      return k(Ge, e || {});
    },
    registerEffect: function registerEffect(e) {
      var t = e.name,
          i = e.effect,
          a = e.plugins,
          r = e.defaults,
          n = e.extendTimeline;
      (a || "").split(",").forEach(function (e) {
        return e && !dt[e] && !nt[e] && h(t + " effect requires " + e + " plugin.");
      }), ut[t] = function (e, t, a) {
        return i(wt(e), M(t || {}, r), a);
      }, n && (Rt.prototype[t] = function (e, i, a) {
        return this.add(ut[t](e, o(i) ? i : (a = i) && {}, this), a);
      });
    },
    registerEase: function registerEase(e, t) {
      zt[e] = Lt(t);
    },
    parseEase: function parseEase(e, t) {
      return arguments.length ? Lt(e, t) : zt;
    },
    getById: function getById(e) {
      return me.getById(e);
    },
    exportRoot: function exportRoot(e, t) {
      void 0 === e && (e = {});
      var i,
          a,
          r = new Rt(e);

      for (r.smoothChildTiming = l(e.smoothChildTiming), me.remove(r), r._dp = 0, r._time = r._tTime = me._time, i = me._first; i;) {
        a = i._next, !t && !i._dur && i instanceof qt && i.vars.onComplete === i._targets[0] || G(r, i, i._start - i._delay), i = a;
      }

      return G(me, r, 0), r;
    },
    utils: {
      wrap: function e(t, i, a) {
        var r = i - t;
        return Qe(t) ? ee(t, e(0, t.length), i) : W(a, function (e) {
          return (r + (e - t) % r) % r + t;
        });
      },
      wrapYoyo: function e(t, i, a) {
        var r = i - t,
            n = 2 * r;
        return Qe(t) ? ee(t, e(0, t.length - 1), i) : W(a, function (e) {
          return t + (r < (e = (n + (e - t) % n) % n || 0) ? n - e : e);
        });
      },
      distribute: K,
      random: J,
      snap: Z,
      normalize: function normalize(e, t, i) {
        return bt(e, t, 0, 1, i);
      },
      getUnit: q,
      clamp: function clamp(e, t, i) {
        return W(i, function (i) {
          return gt(e, t, i);
        });
      },
      splitColor: ne,
      toArray: wt,
      mapRange: bt,
      pipe: function pipe() {
        for (var e = arguments.length, t = new Array(e), i = 0; i < e; i++) {
          t[i] = arguments[i];
        }

        return function (e) {
          return t.reduce(function (e, t) {
            return t(e);
          }, e);
        };
      },
      unitize: function unitize(e, t) {
        return function (i) {
          return e(parseFloat(i)) + (t || q(i));
        };
      },
      interpolate: function e(t, i, r, n) {
        var s = isNaN(t + i) ? 0 : function (e) {
          return (1 - e) * t + e * i;
        };

        if (!s) {
          var o,
              l,
              d,
              u,
              p,
              c = a(t),
              h = {};
          if (!0 === r && (n = 1) && (r = null), c) t = {
            p: t
          }, i = {
            p: i
          };else if (Qe(t) && !Qe(i)) {
            for (d = [], u = t.length, p = u - 2, l = 1; l < u; l++) {
              d.push(e(t[l - 1], t[l]));
            }

            u--, s = function s(e) {
              e *= u;
              var t = Math.min(p, ~~e);
              return d[t](e - t);
            }, r = i;
          } else n || (t = ft(Qe(t) ? [] : {}, t));

          if (!d) {
            for (o in i) {
              Xt.call(h, t, o, "get", i[o]);
            }

            s = function s(e) {
              return ai(e, h) || (c ? t.p : t);
            };
          }
        }

        return W(r, s);
      },
      shuffle: U
    },
    install: p,
    effects: ut,
    ticker: St,
    updateRoot: Rt.updateRoot,
    plugins: dt,
    globalTimeline: me,
    core: {
      PropTween: oi,
      globals: f,
      Tween: qt,
      Timeline: Rt,
      Animation: Bt,
      getCache: g,
      _removeLinkedListItem: L,
      suppressOverwrites: function suppressOverwrites(e) {
        return fe = e;
      }
    }
  };

  function ui(e, t) {
    for (var i = e._pt; i && i.p !== t && i.op !== t && i.fp !== t;) {
      i = i._next;
    }

    return i;
  }

  function pi(e, t) {
    return {
      name: e,
      rawVars: 1,
      init: function init(e, i, r) {
        r._onInit = function (e) {
          var r, n;

          if (a(i) && (r = {}, w(i, function (e) {
            return r[e] = 1;
          }), i = r), t) {
            for (n in r = {}, i) {
              r[n] = t(i[n]);
            }

            i = r;
          }

          !function (e, t) {
            var i,
                a,
                r,
                n = e._targets;

            for (i in t) {
              for (a = n.length; a--;) {
                (r = (r = e._ptLookup[a][i]) && r.d) && (r._pt && (r = ui(r, i)), r && r.modifier && r.modifier(t[i], e, n[a], i));
              }
            }
          }(e, i);
        };
      }
    };
  }

  w("to,from,fromTo,delayedCall,set,killTweensOf", function (e) {
    return di[e] = qt[e];
  }), St.add(Rt.updateRoot), Te = di.to({}, {
    duration: 0
  });
  var ci = di.registerPlugin({
    name: "attr",
    init: function init(e, t, i, a, r) {
      var n, s;

      for (n in t) {
        (s = this.add(e, "setAttribute", (e.getAttribute(n) || 0) + "", t[n], a, r, 0, 0, n)) && (s.op = n), this._props.push(n);
      }
    }
  }, {
    name: "endArray",
    init: function init(e, t) {
      for (var i = t.length; i--;) {
        this.add(e, i, e[i] || 0, t[i]);
      }
    }
  }, pi("roundProps", Q), pi("modifiers"), pi("snap", Z)) || di;

  function hi(e, t) {
    return t.set(t.t, t.p, Math.round(1e4 * (t.s + t.c * e)) / 1e4 + t.u, t);
  }

  function fi(e, t) {
    return t.set(t.t, t.p, 1 === e ? t.e : Math.round(1e4 * (t.s + t.c * e)) / 1e4 + t.u, t);
  }

  function mi(e, t) {
    return t.set(t.t, t.p, e ? Math.round(1e4 * (t.s + t.c * e)) / 1e4 + t.u : t.b, t);
  }

  function vi(e, t) {
    var i = t.s + t.c * e;
    t.set(t.t, t.p, ~~(i + (i < 0 ? -.5 : .5)) + t.u, t);
  }

  function gi(e, t) {
    return t.set(t.t, t.p, e ? t.e : t.b, t);
  }

  function yi(e, t) {
    return t.set(t.t, t.p, 1 !== e ? t.b : t.e, t);
  }

  function wi(e, t, i) {
    return e.style[t] = i;
  }

  function bi(e, t, i) {
    return e.style.setProperty(t, i);
  }

  function xi(e, t, i) {
    return e._gsap[t] = i;
  }

  function Ti(e, t, i) {
    return e._gsap.scaleX = e._gsap.scaleY = i;
  }

  function _i(e, t, i, a, r) {
    var n = e._gsap;
    n.scaleX = n.scaleY = i, n.renderTransform(r, n);
  }

  function Ei(e, t, i, a, r) {
    var n = e._gsap;
    n[t] = i, n.renderTransform(r, n);
  }

  function Ci(e, t) {
    var i = qi.createElementNS ? qi.createElementNS((t || "http://www.w3.org/1999/xhtml").replace(/^https/, "http"), e) : qi.createElement(e);
    return i.style ? i : qi.createElement(e);
  }

  function Si(e, t, i) {
    var a = getComputedStyle(e);
    return a[t] || a.getPropertyValue(t.replace(Ta, "-$1").toLowerCase()) || a.getPropertyValue(t) || !i && Si(e, ka(t) || t, 1) || "";
  }

  function Mi() {
    "undefined" != typeof window && window.document && (Wi = window, qi = Wi.document, ji = qi.documentElement, Ki = Ci("div") || {
      style: {}
    }, Qi = Ci("div"), Sa = ka(Sa), Ma = Sa + "Origin", Ki.style.cssText = "border-width:0;line-height:0;position:absolute;padding:0", Ji = !!ka("perspective"), Ui = 1);
  }

  function zi(e) {
    var t,
        i = Ci("svg", this.ownerSVGElement && this.ownerSVGElement.getAttribute("xmlns") || "http://www.w3.org/2000/svg"),
        a = this.parentNode,
        r = this.nextSibling,
        n = this.style.cssText;
    if (ji.appendChild(i), i.appendChild(this), this.style.display = "block", e) try {
      t = this.getBBox(), this._gsapBBox = this.getBBox, this.getBBox = zi;
    } catch (e) {} else this._gsapBBox && (t = this._gsapBBox());
    return a && (r ? a.insertBefore(this, r) : a.appendChild(this)), ji.removeChild(i), this.style.cssText = n, t;
  }

  function ki(e, t) {
    for (var i = t.length; i--;) {
      if (e.hasAttribute(t[i])) return e.getAttribute(t[i]);
    }
  }

  function Pi(e) {
    var t;

    try {
      t = e.getBBox();
    } catch (i) {
      t = zi.call(e, !0);
    }

    return t && (t.width || t.height) || e.getBBox === zi || (t = zi.call(e, !0)), !t || t.width || t.x || t.y ? t : {
      x: +ki(e, ["x", "cx", "x1"]) || 0,
      y: +ki(e, ["y", "cy", "y1"]) || 0,
      width: 0,
      height: 0
    };
  }

  function Oi(e) {
    return !(!e.getCTM || e.parentNode && !e.ownerSVGElement || !Pi(e));
  }

  function Li(e, t) {
    if (t) {
      var i = e.style;
      t in ya && t !== Ma && (t = Sa), i.removeProperty ? ("ms" !== t.substr(0, 2) && "webkit" !== t.substr(0, 6) || (t = "-" + t), i.removeProperty(t.replace(Ta, "-$1").toLowerCase())) : i.removeAttribute(t);
    }
  }

  function Ai(e, t, i, a, r, n) {
    var s = new oi(e._pt, t, i, 0, 1, n ? yi : gi);
    return (e._pt = s).b = a, s.e = r, e._props.push(i), s;
  }

  function $i(e, t, i, a) {
    var r,
        n,
        s,
        o,
        l = parseFloat(i) || 0,
        d = (i + "").trim().substr((l + "").length) || "px",
        u = Ki.style,
        p = _a.test(t),
        c = "svg" === e.tagName.toLowerCase(),
        h = (c ? "client" : "offset") + (p ? "Width" : "Height"),
        f = "px" === a,
        m = "%" === a;

    return a === d || !l || Pa[a] || Pa[d] ? l : ("px" === d || f || (l = $i(e, t, i, "px")), o = e.getCTM && Oi(e), !m && "%" !== d || !ya[t] && !~t.indexOf("adius") ? (u[p ? "width" : "height"] = 100 + (f ? d : a), n = ~t.indexOf("adius") || "em" === a && e.appendChild && !c ? e : e.parentNode, o && (n = (e.ownerSVGElement || {}).parentNode), n && n !== qi && n.appendChild || (n = qi.body), (s = n._gsap) && m && s.width && p && s.time === St.time ? b(l / s.width * 100) : (!m && "%" !== d || (u.position = Si(e, "position")), n === e && (u.position = "static"), n.appendChild(Ki), r = Ki[h], n.removeChild(Ki), u.position = "absolute", p && m && ((s = g(n)).time = St.time, s.width = n[h]), b(f ? r * l / 100 : r && l ? 100 / r * l : 0))) : (r = o ? e.getBBox()[p ? "width" : "height"] : e[h], b(m ? l / r * 100 : l / 100 * r)));
  }

  function Di(e, t, i, a) {
    var r;
    return Ui || Mi(), t in Ca && "transform" !== t && ~(t = Ca[t]).indexOf(",") && (t = t.split(",")[0]), ya[t] && "transform" !== t ? (r = Da(e, a), r = "transformOrigin" !== t ? r[t] : Ia(Si(e, Ma)) + " " + r.zOrigin + "px") : (r = e.style[t]) && "auto" !== r && !a && !~(r + "").indexOf("calc(") || (r = La[t] && La[t](e, t, i) || Si(e, t) || y(e, t) || ("opacity" === t ? 1 : 0)), i && !~(r + "").trim().indexOf(" ") ? $i(e, t, r, i) + i : r;
  }

  function Ii(e, t, i, a) {
    if (!i || "none" === i) {
      var r = ka(t, e, 1),
          n = r && Si(e, r, 1);
      n && n !== i ? (t = r, i = n) : "borderColor" === t && (i = Si(e, "borderTopColor"));
    }

    var s,
        o,
        l,
        d,
        u,
        p,
        c,
        h,
        f,
        m,
        v,
        g,
        y = new oi(this._pt, e.style, t, 0, 1, ii),
        w = 0,
        b = 0;

    if (y.b = i, y.e = a, i += "", "auto" == (a += "") && (e.style[t] = a, a = Si(e, t) || a, e.style[t] = i), le(s = [i, a]), a = s[1], l = (i = s[0]).match(et) || [], (a.match(et) || []).length) {
      for (; o = et.exec(a);) {
        c = o[0], f = a.substring(w, o.index), u ? u = (u + 1) % 5 : "rgba(" !== f.substr(-5) && "hsla(" !== f.substr(-5) || (u = 1), c !== (p = l[b++] || "") && (d = parseFloat(p) || 0, v = p.substr((d + "").length), (g = "=" === c.charAt(1) ? +(c.charAt(0) + "1") : 0) && (c = c.substr(2)), h = parseFloat(c), m = c.substr((h + "").length), w = et.lastIndex - m.length, m || (m = m || Ge.units[t] || v, w === a.length && (a += m, y.e += m)), v !== m && (d = $i(e, t, p, m) || 0), y._pt = {
          _next: y._pt,
          p: f || 1 === b ? f : ",",
          s: d,
          c: g ? g * h : h - d,
          m: u && u < 4 || "zIndex" === t ? Math.round : 0
        });
      }

      y.c = w < a.length ? a.substring(w, a.length) : "";
    } else y.r = "display" === t && "none" === a ? yi : gi;

    return it.test(a) && (y.e = 0), this._pt = y;
  }

  function Bi(e) {
    var t = e.split(" "),
        i = t[0],
        a = t[1] || "50%";
    return "top" !== i && "bottom" !== i && "left" !== a && "right" !== a || (e = i, i = a, a = e), t[0] = Oa[i] || i, t[1] = Oa[a] || a, t.join(" ");
  }

  function Ni(e, t) {
    if (t.tween && t.tween._time === t.tween._dur) {
      var i,
          a,
          r,
          n = t.t,
          s = n.style,
          o = t.u,
          l = n._gsap;
      if ("all" === o || !0 === o) s.cssText = "", a = 1;else for (r = (o = o.split(",")).length; -1 < --r;) {
        i = o[r], ya[i] && (a = 1, i = "transformOrigin" === i ? Ma : Sa), Li(n, i);
      }
      a && (Li(n, Sa), l && (l.svg && n.removeAttribute("transform"), Da(n, 1), l.uncache = 1));
    }
  }

  function Ri(e) {
    return "matrix(1, 0, 0, 1, 0, 0)" === e || "none" === e || !e;
  }

  function Gi(e) {
    var t = Si(e, Sa);
    return Ri(t) ? Aa : t.substr(7).match(Je).map(b);
  }

  function Fi(e, t) {
    var i,
        a,
        r,
        n,
        s = e._gsap || g(e),
        o = e.style,
        l = Gi(e);
    return s.svg && e.getAttribute("transform") ? "1,0,0,1,0,0" === (l = [(r = e.transform.baseVal.consolidate().matrix).a, r.b, r.c, r.d, r.e, r.f]).join(",") ? Aa : l : (l !== Aa || e.offsetParent || e === ji || s.svg || (r = o.display, o.display = "block", (i = e.parentNode) && e.offsetParent || (n = 1, a = e.nextSibling, ji.appendChild(e)), l = Gi(e), r ? o.display = r : Li(e, "display"), n && (a ? i.insertBefore(e, a) : i ? i.appendChild(e) : ji.removeChild(e))), t && 6 < l.length ? [l[0], l[1], l[4], l[5], l[12], l[13]] : l);
  }

  function Xi(e, t, i, a, r, n) {
    var s,
        o,
        l,
        d = e._gsap,
        u = r || Fi(e, !0),
        p = d.xOrigin || 0,
        c = d.yOrigin || 0,
        h = d.xOffset || 0,
        f = d.yOffset || 0,
        m = u[0],
        v = u[1],
        g = u[2],
        y = u[3],
        w = u[4],
        b = u[5],
        x = t.split(" "),
        T = parseFloat(x[0]) || 0,
        _ = parseFloat(x[1]) || 0;

    i ? u !== Aa && (o = m * y - v * g) && (l = T * (-v / o) + _ * (m / o) - (m * b - v * w) / o, T = T * (y / o) + _ * (-g / o) + (g * b - y * w) / o, _ = l) : (T = (s = Pi(e)).x + (~x[0].indexOf("%") ? T / 100 * s.width : T), _ = s.y + (~(x[1] || x[0]).indexOf("%") ? _ / 100 * s.height : _)), a || !1 !== a && d.smooth ? (w = T - p, b = _ - c, d.xOffset = h + (w * m + b * g) - w, d.yOffset = f + (w * v + b * y) - b) : d.xOffset = d.yOffset = 0, d.xOrigin = T, d.yOrigin = _, d.smooth = !!a, d.origin = t, d.originIsAbsolute = !!i, e.style[Ma] = "0px 0px", n && (Ai(n, d, "xOrigin", p, T), Ai(n, d, "yOrigin", c, _), Ai(n, d, "xOffset", h, d.xOffset), Ai(n, d, "yOffset", f, d.yOffset)), e.setAttribute("data-svg-origin", T + " " + _);
  }

  function Yi(e, t, i) {
    var a = q(t);
    return b(parseFloat(t) + parseFloat($i(e, "x", i + "px", a))) + a;
  }

  function Hi(e, t, i, r, n, s) {
    var o,
        l,
        d = 360,
        u = a(n),
        p = parseFloat(n) * (u && ~n.indexOf("rad") ? wa : 1),
        c = s ? p * s : p - r,
        h = r + c + "deg";
    return u && ("short" === (o = n.split("_")[1]) && (c %= d) != c % 180 && (c += c < 0 ? d : -d), "cw" === o && c < 0 ? c = (c + 36e9) % d - ~~(c / d) * d : "ccw" === o && 0 < c && (c = (c - 36e9) % d - ~~(c / d) * d)), e._pt = l = new oi(e._pt, t, i, r, c, fi), l.e = h, l.u = "deg", e._props.push(i), l;
  }

  function Vi(e, t, i) {
    var a,
        r,
        n,
        s,
        o,
        l,
        d,
        u = Qi.style,
        p = i._gsap;

    for (r in u.cssText = getComputedStyle(i).cssText + ";position:absolute;display:block;", u[Sa] = t, qi.body.appendChild(Qi), a = Da(Qi, 1), ya) {
      (n = p[r]) !== (s = a[r]) && "perspective,force3D,transformOrigin,svgOrigin".indexOf(r) < 0 && (o = q(n) !== (d = q(s)) ? $i(i, r, n, d) : parseFloat(n), l = parseFloat(s), e._pt = new oi(e._pt, p, r, o, l - o, hi), e._pt.u = d || 0, e._props.push(r));
    }

    qi.body.removeChild(Qi);
  }

  qt.version = Rt.version = ci.version = "3.6.0", be = 1, d() && Mt();

  var Wi,
      qi,
      ji,
      Ui,
      Ki,
      Qi,
      Zi,
      Ji,
      ea = zt.Power0,
      ta = zt.Power1,
      ia = zt.Power2,
      aa = zt.Power3,
      ra = zt.Power4,
      na = zt.Linear,
      sa = zt.Quad,
      oa = zt.Cubic,
      la = zt.Quart,
      da = zt.Quint,
      ua = zt.Strong,
      pa = zt.Elastic,
      ca = zt.Back,
      ha = zt.SteppedEase,
      fa = zt.Bounce,
      ma = zt.Sine,
      va = zt.Expo,
      ga = zt.Circ,
      ya = {},
      wa = 180 / Math.PI,
      ba = Math.PI / 180,
      xa = Math.atan2,
      Ta = /([A-Z])/g,
      _a = /(?:left|right|width|margin|padding|x)/i,
      Ea = /[\s,\(]\S/,
      Ca = {
    autoAlpha: "opacity,visibility",
    scale: "scaleX,scaleY",
    alpha: "opacity"
  },
      Sa = "transform",
      Ma = Sa + "Origin",
      za = "O,Moz,ms,Ms,Webkit".split(","),
      ka = function ka(e, t, i) {
    var a = (t || Ki).style,
        r = 5;
    if (e in a && !i) return e;

    for (e = e.charAt(0).toUpperCase() + e.substr(1); r-- && !(za[r] + e in a);) {
      ;
    }

    return r < 0 ? null : (3 === r ? "ms" : 0 <= r ? za[r] : "") + e;
  },
      Pa = {
    deg: 1,
    rad: 1,
    turn: 1
  },
      Oa = {
    top: "0%",
    bottom: "100%",
    left: "0%",
    right: "100%",
    center: "50%"
  },
      La = {
    clearProps: function clearProps(e, t, i, a, r) {
      if ("isFromStart" !== r.data) {
        var n = e._pt = new oi(e._pt, t, i, 0, 0, Ni);
        return n.u = a, n.pr = -10, n.tween = r, e._props.push(i), 1;
      }
    }
  },
      Aa = [1, 0, 0, 1, 0, 0],
      $a = {},
      Da = function Da(e, t) {
    var i = e._gsap || new It(e);
    if ("x" in i && !t && !i.uncache) return i;

    var a,
        r,
        n,
        s,
        o,
        l,
        d,
        u,
        p,
        c,
        h,
        f,
        m,
        v,
        g,
        y,
        w,
        x,
        T,
        _,
        E,
        C,
        S,
        M,
        z,
        k,
        P,
        O,
        L,
        A,
        $,
        D,
        I = e.style,
        B = i.scaleX < 0,
        N = "deg",
        R = Si(e, Ma) || "0";

    return a = r = n = l = d = u = p = c = h = 0, s = o = 1, i.svg = !(!e.getCTM || !Oi(e)), v = Fi(e, i.svg), i.svg && (M = !i.uncache && e.getAttribute("data-svg-origin"), Xi(e, M || R, !!M || i.originIsAbsolute, !1 !== i.smooth, v)), f = i.xOrigin || 0, m = i.yOrigin || 0, v !== Aa && (x = v[0], T = v[1], _ = v[2], E = v[3], a = C = v[4], r = S = v[5], 6 === v.length ? (s = Math.sqrt(x * x + T * T), o = Math.sqrt(E * E + _ * _), l = x || T ? xa(T, x) * wa : 0, (p = _ || E ? xa(_, E) * wa + l : 0) && (o *= Math.cos(p * ba)), i.svg && (a -= f - (f * x + m * _), r -= m - (f * T + m * E))) : (D = v[6], A = v[7], P = v[8], O = v[9], L = v[10], $ = v[11], a = v[12], r = v[13], n = v[14], d = (g = xa(D, L)) * wa, g && (M = C * (y = Math.cos(-g)) + P * (w = Math.sin(-g)), z = S * y + O * w, k = D * y + L * w, P = C * -w + P * y, O = S * -w + O * y, L = D * -w + L * y, $ = A * -w + $ * y, C = M, S = z, D = k), u = (g = xa(-_, L)) * wa, g && (y = Math.cos(-g), $ = E * (w = Math.sin(-g)) + $ * y, x = M = x * y - P * w, T = z = T * y - O * w, _ = k = _ * y - L * w), l = (g = xa(T, x)) * wa, g && (M = x * (y = Math.cos(g)) + T * (w = Math.sin(g)), z = C * y + S * w, T = T * y - x * w, S = S * y - C * w, x = M, C = z), d && 359.9 < Math.abs(d) + Math.abs(l) && (d = l = 0, u = 180 - u), s = b(Math.sqrt(x * x + T * T + _ * _)), o = b(Math.sqrt(S * S + D * D)), g = xa(C, S), p = 2e-4 < Math.abs(g) ? g * wa : 0, h = $ ? 1 / ($ < 0 ? -$ : $) : 0), i.svg && (M = e.getAttribute("transform"), i.forceCSS = e.setAttribute("transform", "") || !Ri(Si(e, Sa)), M && e.setAttribute("transform", M))), 90 < Math.abs(p) && Math.abs(p) < 270 && (B ? (s *= -1, p += l <= 0 ? 180 : -180, l += l <= 0 ? 180 : -180) : (o *= -1, p += p <= 0 ? 180 : -180)), i.x = a - ((i.xPercent = a && (i.xPercent || (Math.round(e.offsetWidth / 2) === Math.round(-a) ? -50 : 0))) ? e.offsetWidth * i.xPercent / 100 : 0) + "px", i.y = r - ((i.yPercent = r && (i.yPercent || (Math.round(e.offsetHeight / 2) === Math.round(-r) ? -50 : 0))) ? e.offsetHeight * i.yPercent / 100 : 0) + "px", i.z = n + "px", i.scaleX = b(s), i.scaleY = b(o), i.rotation = b(l) + N, i.rotationX = b(d) + N, i.rotationY = b(u) + N, i.skewX = p + N, i.skewY = c + N, i.transformPerspective = h + "px", (i.zOrigin = parseFloat(R.split(" ")[2]) || 0) && (I[Ma] = Ia(R)), i.xOffset = i.yOffset = 0, i.force3D = Ge.force3D, i.renderTransform = i.svg ? Xa : Ji ? Fa : Ba, i.uncache = 0, i;
  },
      Ia = function Ia(e) {
    return (e = e.split(" "))[0] + " " + e[1];
  },
      Ba = function Ba(e, t) {
    t.z = "0px", t.rotationY = t.rotationX = "0deg", t.force3D = 0, Fa(e, t);
  },
      Na = "0deg",
      Ra = "0px",
      Ga = ") ",
      Fa = function Fa(e, t) {
    var i = t || this,
        a = i.xPercent,
        r = i.yPercent,
        n = i.x,
        s = i.y,
        o = i.z,
        l = i.rotation,
        d = i.rotationY,
        u = i.rotationX,
        p = i.skewX,
        c = i.skewY,
        h = i.scaleX,
        f = i.scaleY,
        m = i.transformPerspective,
        v = i.force3D,
        g = i.target,
        y = i.zOrigin,
        w = "",
        b = "auto" === v && e && 1 !== e || !0 === v;

    if (y && (u !== Na || d !== Na)) {
      var x,
          T = parseFloat(d) * ba,
          _ = Math.sin(T),
          E = Math.cos(T);

      T = parseFloat(u) * ba, n = Yi(g, n, _ * (x = Math.cos(T)) * -y), s = Yi(g, s, -Math.sin(T) * -y), o = Yi(g, o, E * x * -y + y);
    }

    m !== Ra && (w += "perspective(" + m + Ga), (a || r) && (w += "translate(" + a + "%, " + r + "%) "), !b && n === Ra && s === Ra && o === Ra || (w += o !== Ra || b ? "translate3d(" + n + ", " + s + ", " + o + ") " : "translate(" + n + ", " + s + Ga), l !== Na && (w += "rotate(" + l + Ga), d !== Na && (w += "rotateY(" + d + Ga), u !== Na && (w += "rotateX(" + u + Ga), p === Na && c === Na || (w += "skew(" + p + ", " + c + Ga), 1 === h && 1 === f || (w += "scale(" + h + ", " + f + Ga), g.style[Sa] = w || "translate(0, 0)";
  },
      Xa = function Xa(e, t) {
    var i,
        a,
        r,
        n,
        s,
        o = t || this,
        l = o.xPercent,
        d = o.yPercent,
        u = o.x,
        p = o.y,
        c = o.rotation,
        h = o.skewX,
        f = o.skewY,
        m = o.scaleX,
        v = o.scaleY,
        g = o.target,
        y = o.xOrigin,
        w = o.yOrigin,
        x = o.xOffset,
        T = o.yOffset,
        _ = o.forceCSS,
        E = parseFloat(u),
        C = parseFloat(p);
    c = parseFloat(c), h = parseFloat(h), (f = parseFloat(f)) && (h += f = parseFloat(f), c += f), c || h ? (c *= ba, h *= ba, i = Math.cos(c) * m, a = Math.sin(c) * m, r = Math.sin(c - h) * -v, n = Math.cos(c - h) * v, h && (f *= ba, s = Math.tan(h - f), r *= s = Math.sqrt(1 + s * s), n *= s, f && (s = Math.tan(f), i *= s = Math.sqrt(1 + s * s), a *= s)), i = b(i), a = b(a), r = b(r), n = b(n)) : (i = m, n = v, a = r = 0), (E && !~(u + "").indexOf("px") || C && !~(p + "").indexOf("px")) && (E = $i(g, "x", u, "px"), C = $i(g, "y", p, "px")), (y || w || x || T) && (E = b(E + y - (y * i + w * r) + x), C = b(C + w - (y * a + w * n) + T)), (l || d) && (E = b(E + l / 100 * (s = g.getBBox()).width), C = b(C + d / 100 * s.height)), s = "matrix(" + i + "," + a + "," + r + "," + n + "," + E + "," + C + ")", g.setAttribute("transform", s), _ && (g.style[Sa] = s);
  };

  w("padding,margin,Width,Radius", function (e, t) {
    var i = "Right",
        a = "Bottom",
        r = "Left",
        n = (t < 3 ? ["Top", i, a, r] : ["Top" + r, "Top" + i, a + i, a + r]).map(function (i) {
      return t < 2 ? e + i : "border" + i + e;
    });

    La[1 < t ? "border" + e : e] = function (e, t, i, a, r) {
      var s, o;
      if (arguments.length < 4) return s = n.map(function (t) {
        return Di(e, t, i);
      }), 5 === (o = s.join(" ")).split(s[0]).length ? s[0] : o;
      s = (a + "").split(" "), o = {}, n.forEach(function (e, t) {
        return o[e] = s[t] = s[t] || s[(t - 1) / 2 | 0];
      }), e.init(t, o, r);
    };
  });
  var Ya,
      Ha,
      Va = {
    name: "css",
    register: Mi,
    targetTest: function targetTest(e) {
      return e.style && e.nodeType;
    },
    init: function init(e, t, i, a, r) {
      var n,
          s,
          o,
          l,
          d,
          u,
          p,
          h,
          f,
          m,
          v,
          g,
          y,
          w,
          b,
          x = this._props,
          T = e.style,
          _ = i.vars.startAt;

      for (p in Ui || Mi(), t) {
        if ("autoRound" !== p && (s = t[p], !dt[p] || !Gt(p, t, i, a, e, r))) if (d = _typeof(s), u = La[p], "function" === d && (d = _typeof(s = s.call(i, a, e, r))), "string" === d && ~s.indexOf("random(") && (s = te(s)), u) u(this, e, p, s, i) && (b = 1);else if ("--" === p.substr(0, 2)) s += "", h = q(n = (getComputedStyle(e).getPropertyValue(p) + "").trim()), (f = q(s)) ? h !== f && (n = $i(e, p, n, f) + f) : h && (s += h), this.add(T, "setProperty", n, s, a, r, 0, 0, p);else if ("undefined" !== d) {
          if (_ && p in _ ? (n = "function" == typeof _[p] ? _[p].call(i, a, e, r) : _[p], p in Ge.units && !q(n) && (n += Ge.units[p]), "=" === (n + "").charAt(1) && (n = Di(e, p))) : n = Di(e, p), l = parseFloat(n), (m = "string" === d && "=" === s.charAt(1) ? +(s.charAt(0) + "1") : 0) && (s = s.substr(2)), o = parseFloat(s), p in Ca && ("autoAlpha" === p && (1 === l && "hidden" === Di(e, "visibility") && o && (l = 0), Ai(this, T, "visibility", l ? "inherit" : "hidden", o ? "inherit" : "hidden", !o)), "scale" !== p && "transform" !== p && ~(p = Ca[p]).indexOf(",") && (p = p.split(",")[0])), v = p in ya) {
            if (g || ((y = e._gsap).renderTransform && !t.parseTransform || Da(e, t.parseTransform), w = !1 !== t.smoothOrigin && y.smooth, (g = this._pt = new oi(this._pt, T, Sa, 0, 1, y.renderTransform, y, 0, -1)).dep = 1), "scale" === p) this._pt = new oi(this._pt, y, "scaleY", y.scaleY, m ? m * o : o - y.scaleY), x.push("scaleY", p), p += "X";else {
              if ("transformOrigin" === p) {
                s = Bi(s), y.svg ? Xi(e, s, 0, w, 0, this) : ((f = parseFloat(s.split(" ")[2]) || 0) !== y.zOrigin && Ai(this, y, "zOrigin", y.zOrigin, f), Ai(this, T, p, Ia(n), Ia(s)));
                continue;
              }

              if ("svgOrigin" === p) {
                Xi(e, s, 1, w, 0, this);
                continue;
              }

              if (p in $a) {
                Hi(this, y, p, l, s, m);
                continue;
              }

              if ("smoothOrigin" === p) {
                Ai(this, y, "smooth", y.smooth, s);
                continue;
              }

              if ("force3D" === p) {
                y[p] = s;
                continue;
              }

              if ("transform" === p) {
                Vi(this, s, e);
                continue;
              }
            }
          } else p in T || (p = ka(p) || p);
          if (v || (o || 0 === o) && (l || 0 === l) && !Ea.test(s) && p in T) o = o || 0, (h = (n + "").substr((l + "").length)) !== (f = q(s) || (p in Ge.units ? Ge.units[p] : h)) && (l = $i(e, p, n, f)), this._pt = new oi(this._pt, v ? y : T, p, l, m ? m * o : o - l, v || "px" !== f && "zIndex" !== p || !1 === t.autoRound ? hi : vi), this._pt.u = f || 0, h !== f && (this._pt.b = n, this._pt.r = mi);else if (p in T) Ii.call(this, e, p, n, s);else {
            if (!(p in e)) {
              c(p, s);
              continue;
            }

            this.add(e, p, e[p], s, a, r);
          }
          x.push(p);
        }
      }

      b && si(this);
    },
    get: Di,
    aliases: Ca,
    getSetter: function getSetter(e, t, i) {
      var a = Ca[t];
      return a && a.indexOf(",") < 0 && (t = a), t in ya && t !== Ma && (e._gsap.x || Di(e, "x")) ? i && Zi === i ? "scale" === t ? Ti : xi : (Zi = i || {}) && ("scale" === t ? _i : Ei) : e.style && !s(e.style[t]) ? wi : ~t.indexOf("-") ? bi : Jt(e, t);
    },
    core: {
      _removeProperty: Li,
      _getMatrix: Fi
    }
  };
  ci.utils.checkPrefix = ka, Ha = w("x,y,z,scale,scaleX,scaleY,xPercent,yPercent" + "," + (Ya = "rotation,rotationX,rotationY,skewX,skewY") + ",transform,transformOrigin,svgOrigin,force3D,smoothOrigin,transformPerspective", function (e) {
    ya[e] = 1;
  }), w(Ya, function (e) {
    Ge.units[e] = "deg", $a[e] = 1;
  }), Ca[Ha[13]] = "x,y,z,scale,scaleX,scaleY,xPercent,yPercent," + Ya, w("0:translateX,1:translateY,2:translateZ,8:rotate,8:rotationZ,8:rotateZ,9:rotateX,10:rotateY", function (e) {
    var t = e.split(":");
    Ca[t[1]] = Ha[t[0]];
  }), w("x,y,z,top,right,bottom,left,width,height,fontSize,padding,margin,perspective", function (e) {
    Ge.units[e] = "px";
  }), ci.registerPlugin(Va);
  var Wa = ci.registerPlugin(Va) || ci,
      qa = Wa.core.Tween;
  e.Back = ca, e.Bounce = fa, e.CSSPlugin = Va, e.Circ = ga, e.Cubic = oa, e.Elastic = pa, e.Expo = va, e.Linear = na, e.Power0 = ea, e.Power1 = ta, e.Power2 = ia, e.Power3 = aa, e.Power4 = ra, e.Quad = sa, e.Quart = la, e.Quint = da, e.Sine = ma, e.SteppedEase = ha, e.Strong = ua, e.TimelineLite = Rt, e.TimelineMax = Rt, e.TweenLite = qt, e.TweenMax = qa, e["default"] = Wa, e.gsap = Wa, "undefined" == typeof window || window !== e ? Object.defineProperty(e, "__esModule", {
    value: !0
  }) : delete e["default"];
});
}).call(this,require("e/U+97"),typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : {},require("buffer").Buffer,arguments[3],arguments[4],arguments[5],arguments[6],"/fake_e74bdf65.js","/")
},{"buffer":2,"e/U+97":4}]},{},[5])